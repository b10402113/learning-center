> > Nina Zakharenko: So let's take a look at our write endpoints.Which now that we have a database, we can actually add to themWe need to implement theroutes that modify the database.And based on CRUD, what would those routes be?Create, update, delete, yeah.Select, read. So create is going to map to POST,update. We're going to use PATCH and then delete is DELETE or don't delete ordon't delete, delete. Yeah, it's entirely in production.Sometimes you don't want to delete, and so deletion can be managed by using a flag,you know, for projects, maybe something like dot archive to say like,hey, don't show this on the front end, but we want these records available.So that's a great call out.Now, when we're using our relational databaseand we fetch a record, we're going to need to handle that data validation.We're going to need to sometimes unpack incoming JSON payloads.We need to manage the database's session and transaction state.That all happens fairly nicely with FastAPI.So I'm going to go ahead and create a new little slugifyfunction here at the top.And it's going to take a string and return a string, and we're going to use a generator comprehensionhere to do a little bit of extra cleanup.So we're going to say we want c for cin value.lower. We only want to make sure that we handle alphanumeric characters.So if the project has like a question mark or period or something in it,we don't want that as part of the slug.And we're going to use if c.isalnumas a built-in method to do that, orif c is a space.And we're going to pass this all in to empty string join.So that's kind of going to condense everything and then we're just going to return.We want to return all of the values in this joined with a dashSo we're going to joinLet's make a variable forthis. And we're going to split that.And we're also going to provide a default value here of project.FYI, in production, you generally don't want to do this.There are libraries called, I think one of them is Python slugify,or use a regular expression. Slugs are kind of a can of worms,and here specifically because itwould be kind of too much to explain, we are not checking to see if slug is unique when we'recreating these records. So if you want to add on to the project with time,that is something that you can add in on your own.But yeah, python-slugify, that's a special case one.It handles Unicode and a whole lot more.And you're also going to need to add that checking before you add a project in to make sure slugis unique. So we're going to need to add a few more modelsfor our writes. So we have Project, we have ProjectRead.We also need a ProjectCreate.And we need ProjectUpdate. So those are our schemas.And we're going to need a new delete project that's goingto return a 204 status code.We're going to add project routes to create projectupdate and delete.Let's go ahead and add the route.So for this one, it's going to be app.post,and the route is going to be projects here.And remember, we need to set our response model.And that's going to be ProjectRead, because we're returning the whole project.And lastly, the status code for this is going tobe status.HTTP_201_CREATED. And then we'regoing to define the method here called create_project.And it's going to take a payload, which is going toneed to have the format of ProjectCreate, and we're going to take in a session,which is going to be ourSessionDep. So first off, we want to make sure that the values that were provided to the APIvalidate, and we do that by saying project.model_validate.And we're going to pass in that payload.And we're also going to say when we update,set the slug to slugify our payload.name, whichmeans that we no longer have to manually set the slug when we are trying to create projects nowNow we got to doa little bit of database wrangling.So, who remembers from the seed script what we need to do to makesure that the session is aware of this new model we're trying to create?To create a seed object.Yeah, so we used session.add when we created a seed object.So we're going to add that newly created project to our session.We're going to go ahead and commit it.And then we have to do one more thing that's kind of important here.We need to refresh the project from the sessionbecause if we didn't, wewould not have an ID set on this model at this point.So we've gone ahead and committed the project.Now we have to say explicitly, hey, we want to, when somebody creates a project,we want to pass that primary key that was generated back.So go ahead and refresh this from the session to make sure the key is set.And then lastly, we're going to return our project.So that model_validate creates that project modelinstance from that ProjectCreate payload.It's going to validate that data and map the fields, and the update parameter herelets us inject additional fields that were not necessarily in the payload,like the slug that we generated.Lastly, let's look at what a PATCH request looks likeand what a DELETE request looks like.So starting with delete, because it's a little bit simpler.We're going to say that the HTTP method that this supports here is delete,and the path is going to be project slash project ID.And the status code is going to be 204 for no content.For a function definition for delete_project, we're going to need aproject_id which is an int, and we're going to need a session SessionDepAnd then we're going totry to get that project and say session.get Project andproject_id. And let's spell this correctly.So that should get us our project. What happens if somebody's trying to delete a project that doesn'texist? What is this going to return?Nothing, right? So we need to check for that and throw an error.So we're going to say if not project, we're going to raise that HTTPException like we saw earlierand the status code isgoing to be status.HTTP_404_NOT_FOUND. And we're goingto say, let's make this a little easier to read.We're going to say that the detail is project not foundSo that kind of guardsagainst that condition. Now we can say session.delete project,session.commit to make sure that it actually happens,and we're going to return a response.And our status code is going to be 204no content. And let's make sure that we have thatimported as well. It's pretty straightforward.I'm going to talk about the updateproject endpoint quickly. I'm not going to type that all out for you kind of for the sake oftime, but I do want to show it to you so that we can discuss it step by stepSo let me go aheadand grab that. And this is going to be the update forour CRUD. So what thisproject takes, first, we need to define the patch.The response model is going to be ProjectRead.We have a project ID. We have a payload, which could include some fields on project,but not all. And then again, that session dependency.We're going to try to get the project. If we don't have it, we're going to return that 404 notfound error. Updated fields is going to be,get everything that has been updated, and this exclude_unset flag saysI don't care about stuffthat hasn't changed. Don't give me that.And then we're going to use SQLModel.update with the updated fields that we got toupdate the project and make sure thatit's been validated. Then just a quick check if the name was updated,we need a new slug. So go ahead and update that.I noticed that we're setting project.slug here to the value of slugify,so we're actually actively modifying that Pythoninstance of project, which is a lot more convenient than having to figure out,oh, how do I write the SQL query?Then we add it to the session, we commit it, we refresh it,so we make sure we have all the updated data, and then we return project.Now, we are duplicating a lot of code here.There's a lot going on. We're kind of constantly checking like,hey, is this a 404 or not? It's really making the code a little bit challenging to read.So, our route handlers are really doing a lot of heavy lifting right now.They're calling the database and managing database stateinline, which is not great.Our routes are thinking about stuff like slug generation, committing,refreshing, not necessarily things that should be our route's responsibility.So as our apps grow, these kinds of bloated handlers become a big liabilityWe only have 5 endpointsand 1 helper function, but there's inline database logic in every handler.So our route handler is doing two jobs at once.It's doing HTTP work. It cares about status codes and error responses and data access work queriesetc. So, in the exercisecoming up, we are going to separate some of these concerns and refactor our project.But very quickly, let's take a look andsee how we can make ourmain route a little bit nicer. So if you remember earlier,when I ran fastapi dev.Where is, see, I havea typo here. Let me look. POST string.Aha, I have a little extra character there.Let's just run that again. OK, that's gone ahead and cleared that error.We have a, when we hit our localhost here at 8000,we have a detail not found. That's not great.So let's add a new route to our appand we're just going to add a route for GET and the root.And it's going to, we're going to call it read_root.And it's going to return a dictionary with a string and a string.And what we're going to return here, just a little bit ofJSON, and we're going to say this is our app release tracker API.And we're going to provide a nice handy link to the docs as well.
