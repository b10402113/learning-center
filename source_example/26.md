> > Nina Zakharenko: So for this exercise you're going to do a few things.You're going to add a 404 to GET project.Because right now when we request projects, we GET a 500 error back because our mock database justreturns none. That doesn't satisfy that requirement that we set inject read.So you're going to fill in exercise step one toraise an HTTP exception with a 404 status code instead.And you can either use the just 404 as a number,or from FastAPI you can import status and do status.HTTP andit'll show you all of the HTTP status codes.It provides like HTTP 404 NOT found, so you don't have to keep all of the numerical statuscodes in memory. And you're also going to add a slug-based search endpoint.So where we had list projects to filter by their name,we're going to add a slug-based search endpoint instead.And then I've provided a test stub for you here.You're going to update the test list projects by slug, so that you can use this test to send slugAPI equals V2 instead of name APIV2 to match that new slug based endpointSo once your test passyou'll know that you havesuccessfully completed the exercise, and if you need to compare with the solution,you can do that with this command or just visit that solution repository on GitHub.Just a reminder, in this exercise, we just touched on some of the really basicfeatures of FastAPI in our main.py.And let me close this and make sure I have thelatest copy open here. We have our simple mock database and we areusing we're going to use the status code import, so.If we don't have a project, if trying to GET our project from our mock database didn't work,we want to raise an HTTP exception.And so I want to say if NOT, if NOT project.Raise HTTP exception and for status codeinstead of we could just say 404 and that'll workwith this detailed project NOT found.But it's going to be a lot clearer in our project if we use status.HTTPNo, my overzealous rough formattinghere, deleted my status import.So we're going to use status.HTTP 404, just so that it's super clear inour code. I mean, 404, that's when you see all the time every day.It's a lot of other HTTP statuses where it's much nicer to not have to rack your brain.So for step two, we want to see if we don't have a slug.Go ahead and return all those projects.Otherwise we only want to return theprojects where that slug is configured, right?So we are returning alist comprehension here. So we want to checkfor project in projects. And we want to return the projectitself. And then we also want to checkif the project.slug is equal to the slugthat was passed in. So this list comprehensiongives us a little bit of free filtering for our mock database.And lastly, we have a test to fill in.So let's take a look at testing.And for step 3, we want to update this test so that it sends a slug equals APIV2So we change that inour parameters here. Instead ofname equals APIV2. And see if our testpass all green 4 test pass.So that looks pretty good.And that means that we are done with the chapter where we've kind of builtthe most basic API. Unfortunately, most productioncodebases don't use mock databases.So we're going to have to learn about some data layer foundations and figure out how to work withdatabases. Yes. One quick testing related question, is there an easy way torun and debug just a single test?Yeah, you just need to provide a path to that test.I don't remember exactly how the path is formatted.But it's, that's a pretty easy thing to look up.Yeah, you can run like a specific tests, you can run specific files.There's a few ways to break that down.
