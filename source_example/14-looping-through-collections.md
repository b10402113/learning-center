> > Nina Zakharenko: Let's take a look at some data structures and very quickly go over howwe would loop over them. So I can do for fruit and fruits here.When I press enter, I get that indent and I'm gonna print fruit,and that's going to print, it's gonna go through every value in this fruits list,give it a handy variable name for me that only exists.That will get updated in the context of this for loop andit does exist kind of past the context of that for loop, but it just is going to be thelast value in that list. So something to be mindful of.Same thing for tuples and sets, you would use the same syntax to loop over thoseAs we talked about, indictionaries, sometimes in dictionaries, you want to loop over just the keys,sometimes just the values. Oftentimes you're looping over both.And to do that, you would say for key, comma value in person.itemsAnd what's that going tothat we saw that itemsreturns the key and the value. This is like a little shortcut here that's actually going to unpackthat key value pair into like basically a tuple,and now you can use that key and that value in this for loop.They will always be, they both get updated at the same time.So we can go ahead and print our key value pair here.You'll see these data structures combined in a lot of different ways.Before we introduce a database into our app, we're going to use a mock database,and we're going to have a list of instantiated classes,or you might have a list of dictionaries. So these data structures can get,I'm not gonna say infinitely complex, but they can get very complicated and complex.So just to wrap this up, what you want to keep in mind,lists preserve order. They can be changed, modified.Tuples do preserve order, but they are immutable,so you can't change the values in them after they're created.Sets will remove duplicates and give you access to set operations,and dictionaries are going to map names to values.They're the most common shape for data that looks like JSON.So, list comprehensions and comprehensions in general areone of Python's best features. They let you take these simple repetitive type ofloops and just put them in one line.It's a way to build a new list from an existing iterable orcollection. So, let's see, do I still have my project names here?I have some project names and let's say I wanted to generate some slugsfor these in line. I can just say, I'm gonna use that list syntax hereAnd then the first thingthat I'm going to put in is, what do I want to see in the list that's being output inSo I'm gonna say, name.lowerwe'll start there for namein project names. And whatthis does is goes through every name and project names and calls the lower methodon that name and returns a new list.Yeah, this is super handy because I can also, the logic on that left hand side can be fairly complicatedAnd then just like wesaw in that for loop, this is the name of the variable that you useto modify data or you can have conditional statementsin your list comprehensions as well.So I can also say replace space with dash.And that will give me something that looks a little bit more slug shaped.So, very useful shortcut, you can have list,you can have comprehensions of any of the existing data types,and they'll look pretty similar.Let me show you what filtering looks like in a comprehension.So, I'm gonna have this list ofdictionaries here and, called tasks, and let's say that I wantedto only look at tasks that were not done.I can say that the open tasks area list, and I'm gonna say for task in tasksand I'm gonna check to seethe thing that I want to grab is the title, so I'm gonna use that.And let's just quickly see what this returns.So that's not actually doing any filtering, because thisone's already done, but it still ended up in here.So I would add my conditional statementto the end. And I'm gonna sayif task done and nothere. And that's going to filter out.And this is a Python syntax for how to access a value ina dictionary by its keys.So let me quickly show you that.We have, let's say single task is this,I can say single task titleSingle task done, but ifI try to get something that's not there, I'm gonna get a key.I can use the get operator hereon the dictionary, and that will return none if the key isn't present.You can also pass in a default value hereto this get method to say like, if this key is not in the dictionary,return this instead. There are some examplesof dictionary and set comprehensions in the course material.I'm not going to go through them because each one has a slightly different syntax.But something that I do wanna show you becauseit's gonna come up and y'all are gonna have questions about it islet's see, we have taskshere. So, I'm gonna use our list comprehension syntaxto return the number one for every task in this list if it's notdone. And if I wanted to figure out,what is the count of tasks that aren't done, I can pass this whole comprehension into the built-insum method to return to.This is fine for small data sets. When you have really large data sets,you don't necessarily want to generate all of this for every item in a collection.And for that, you would use something called a generatorcomprehension. So instead of that square bracket syntax around this code,we would use just parentheses, or if you're putting it somewhere where,you know, like a method or something, you might not even see these added parentheses,and you're going to get this thing back, generator, object,something something. We can't look at the numbers like we did when we were using a list comprehensionThat's because a generator isnot going to build that full list in memory, just going to produce one value at a time,at the time that value is requested.It finishes when the values run out.So we can pass this into sum, and itstill works the same way that we expect it to.It's really good for using it for things like sum, any,all, or any for loop where you're just kind of walking through the values once,you don't need to hold on to them all at the same time.It's more efficient. So ifyou do need to kind of save that value for anything,list comprehension is a better option.So using that built in all keyword and Python, it will tell me,are there any tasks that are not done?And yeah, there are still some.So I'm gonna save a more challenging example forthe exercise, but just kind of a general tip.Use comprehensions when you are looking to transform data,when that logic is short, if there's a conditional statement,so I want some of the values in this list, but not other ones.And if the comprehension is easy to read, if your comprehension is longer than two lines,maybe take a step back and just use a normal for loop.There's another kind of handy functioncalled zip that will taketwo separate lists, and it's going tocombine them in a way thatmakes key kind of key value pairs.So the 0th item will be paired with the 0th item in that second list,so on and so forth.You're going to get back a zip object looks kind of like agenerator. But you can pass that into the dict constructor to build you a nicequick and easy dictionary.
