> > Nina Zakharenko: So, very quickly to talk about .env, we have a few environment variables now that we need.The first two are kind of just for us. The third is actually a real secret..env is NOT a secret store. It makes working in dev easier.You don't want to use it in production.There's, you know, you don't necessarily want a file floating around with all your valuable secrets.We use pydantic settings to read it on startup.This kind of lets us avoid polluting our shell with a lot of project-specific exports,which is nice. We don't have to source that .env file every time to have our terminalyou know, know what thevalues are before starting the server, but what .env is NOT,NOT where production secrets live, NOT in version control,and it is NOT a documentation file either.So you want to make sure it's in your .gitignore and if you are working on a project thatother people are also going to be working on, it's nice to provide a .env.example so that folksso other developers know whatneeds, what secrets need to be in place, or provide nice defaults as wellSo, a new contributor, youwould just copy the .env.example to .env and then follow those,follow these instructions and create a key.So, what counts as a secret?If a leak would force you to rotate credentials, then it's a secret.So JWT secret, database URL usually a secret,you know, can have a database password embedded into it.Local dev, you know, harmless production form like with a managed Postgres password needs tobe handled the same way as that JWT key.Debug, NOT a secret, but in production, have it default, you know,in production if debug is true, your tracebacks might be too verbose,your logs might get overwhelmed, and you might also be logging things that should NOT ever makeit past the development environment.So, very quickly, once you'removing your app to a deployment platform, don't use .env.So how do you want to set up your secret storage?Generally with platform-native environment variables, so Cloud providers will let you set environmentvariables in the configuration. Yeah, Heroku, Netlifywhatever it is, they'll havea UI for that, and the app is going to read them the same way because of that pydantic settingsthat we're using. You could use that managed secret service,AWS Secrets Manager is one, HashiCorp Vault, you know,tons of different providers offer the service.You could use something like Kubernetes Secrets stored in a cluster mounted as environment variablesso it's best to haveyour app set up in a way where it doesn't matter where the environment variables are coming from,just that they're there, that code should stay unchanged no matter what environment it's on.And lastly, validating at startup, we want to make sure that settingsfield that we're using it correctly.So we have our database URL here with a default,debug with a default, secret key has no default,so our app does NOT even run if that JWT secret key is missing.And that also lets us validate some things that like the key is long enough.And to read it, we use this get_secret_value.Tests can't rely on .env because that CI is going to run in a clean shell,so you might want to set those environment variables just for tests
