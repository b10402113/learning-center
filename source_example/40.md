> > Nina Zakharenko: So we have this config here as the smallest possible config.It works, but the format's not great.The level is hard quoted, and we probably want more stuff in our logs in production,you know, maybe a timestamp, what the source module was, and make sure that our level iscontrolled by an environment variable so that when we're runninglocally, we get those debug messages, but those don't surface in production.And we're going to take a look at a new file.So we have a small helper function in our config.pyand that's going to letus pull in settings from environment variables if they're presentor here I'm just settinga default database URL so that if no environment variable is there,we can, you know, have a default that will probably work based on your configurationin your Docker Compose. So something that we're adding here is kind of a high level configis our log format, and we're also addinga new variable that could possibly be set.Lastly, we're going to do our logging config now programmatically and say,take the level from the environment fileand use this log format here that we've specified.So configure logging explicitly takes debug as a boolean or not,and we're going to say, if debug is in our .env file, then our logging level is going to bedebug, otherwise set it to info.And we can go ahead and make a new .env file.So I'm going to touch, I'm going to make sure that I'm at the root directory here and createa new .env file. Turnthat on later and open that up, and I'm going to set debug equals true.Let's go ahead and turn this on. Also, this handy little pop up here says,do we want our Python terminals to use our .env files in VS Code?And I'm going to say, yeah, sure.So, I'm going to use command and comma to open up my settings and look for my workspacesettings and turn on this Python terminal use .env file.So we're going to get a much nicer logging experience now.Our logs are going to be a lot more structured.And then when we're using this in the codebase,doing this is good enough, because we've done that configuration kind of at ahigher level, and we don't have to reconfigure constantly.So there are a few other libraries that you might want to use for logging.Structlog has about 5000 starson GitHub. It produces structured JSON style log records.I would say probably the more popular one is loguru,L O G U R U, about 24,000 stars on GitHub,and it wraps the standard library API in something a lot more ergonomic,so one import sensible defaults, and if you find thestandard library logger getting in your way, I recommend looking up one of those other libraries.
> > Track current time
