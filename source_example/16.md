> > Nina Zakharenko: We're going to talk about context managers next.We're not going to be writing our own, but I do want you to be familiar with the syntax.And let me show you really quickwhat it might look like to open a file in Python without context managersIf you needed to openthat file and make sure that the file was closed,even if something happened while reading it, you know, maybe a disk hiccup orpermissions issue, if you just ran this code, that file could have a lock on it,which definitely is not something you want in an application that's openingpotentially a lot of files.And there are other types of resources that could need this type of cleanup.Database connections, a lock that has to be unlocked,even if there's a failure.And this is kind of a lot of code for something that youare generally doing a fair amount. And this finally is part of the try statement to sayno matter what happens, evenif an exception is raised, still run this code.So if you put this f.close in here and some exception happened,this would never run. So that's what that finally block does.So using Python's built-in context managersthis is what opening afile looks like. And we're using this with keywordto say, using this built-in context manager, open the filename it f, and thenjust let me read it. And if somethinggoes wrong, if this block exits either normally or because an exception,Python is going to call the underlying cleanup code on f withouthaving to manually call it.And a quick example of what that kind ofcontext manager looks like. Solet's say we have this class called section.And we are going to define an enter method that says like,what do we do before we call, and this exit method that says,what are we going to do after we call? And if we run this and print it out,it's going to say, we're going to have this line here.The method that's being called and this piece of code right here.So let me show that to you quickly.In Python, there's a library called contextlib that does this for you.So just like data classes, you can use this typeof syntax to create your own context manager.I'm not going to go over this in too much detail,but keep in mind that the try finally around this yield statement isimportant. If you don't have it, any exceptions that are raisedin that with block would skip the cleanup.So try to run the operation, finallydo this last no matter what.And I'm not going to dive too deep into this.Just know that when we work with database connections later on, we're going to use that same patternof using a with block to open that session, run a few queries,and have that clean up, so a commit or a rollbackbefore closing the session. So that helps us keep those database interactionsa little bit smaller and a little bit more guarded so that we don't always constantly have toremember to call, you know, file.close every time we open a file.Decorators give you a little bit of syntax for wrapping a function with extra behavior.Yes. Does the value I put after withneed dunder methods implemented for the context to work?Not if you're using that, not if you're using this contextlib contextmanager class, but I will say like for your code base to need tohave custom context managers, like you'd have to be pretty far ahead.There are not necessarily many cases where you'd want to be writing code likethis. But it's something that libraries use a lot.And so it's a syntax that you'll see frequently for using context managersYou're not necessarily going tobe writing your own. Does that make sense?So just kind of a little bit of interesting Python.Let me make sure this is saved.We have a function called shout here that takes a piece of text,translates it to uppercase, and adds an exclamation.We can assign this function to a variable called yell.And just to quickly show you what that looks like,if we look at yell, we'll see that yell is a function.So we can now call shoutwith Nina or yell with Nina, they point to the same thing.A decorator is a function that takes a function and returns another function.And it's usually a wrapped version of that function that adds behavior either before orafter the original function. So let's look quickly ata function called log call.And we're going to define a second function inside this functioncalled wrapper, takes arguments and keyword arguments, and then it's going to print out what is the nameof the function that we're calling.And then actually call it with the provided arguments.Outside of this function definition, we're going to return this inner function.If we wanted to print this outand we had a function called normalized title that would takewhatever we passed in, strip it, make it uppercase.The way we would do that is by having this normalized title functionand adding a decorator ontop. And we'll notice herethat we're not passing any parameters to log call.Log call takes basically a function, which is going to be this one,and has that inner wrapper where it's going to do the printing.So to normalize the title, we're going to want to return the title,we're going to want to strip it, and we're going to want to title case it and then print itout. So running that, Inow see calling normalized title, which we did here.And we can wrap now any, add this decorator to any Python function.And it will log the call.So you'll see decorators used all the time.They look like magic. It's fairly important to know what they do,know how they work. You'll probably start writing some eventually,but many, many Python libraries provide decorators as shortcutsI have a little bitmore information about how to write decorators in the course material if you do want to go back thereand use that as reference, but kind of throughout the course,the decorators that you'll end up using is at app.getand specify a project route in FastAPI.We're also going to use a decorator called at field_validator later on when we introduce Pydanticand that's going to insertsome custom validation on a field.>> Nina Zakharenko: So let's go ahead and do a quick exercise with some ofthe concepts that we've learned.We're going to do an exercise on comprehensions, exceptions,and data classes. So for part one, rewrite this for loop asa comprehension. Next you're going to takethis list of titles and build a list of titles for tasks that aren't done.You're going to write a function that raises and catches an exception,and then you're going to define a simple data class called project with 3 fields.You're going to print it and confirm that archive defaults to false.If you don't remember the syntax for this,you can refer back to the previous lesson.Yes. Do we have to worry about decorator functions not being apure function and creating side effects in our code?Yeah, decorators do create side effects.Do you have to worry about them?Not necessarily, especially if you're using decorators provided by a librarybut yeah. Do decorators addadditional functionality to an already existing function?Why not create another function and call that?Yes, decorators do add existing functionality.They also, you'll frequently see decorators stacked, so you can have multiple decoratorsfor one function that do different things.The reason that you want to use them instead of another function is because herewe're just adding this log call decorator.We don't care what arguments this function takes.We don't even care about what this function's name is, because of this syntaxwe're kind of doing stuffwith the metadata of the function itself.We can also, decorators can do a lot.They can change values. They can log stuff.But they're definitely kind of thecleaner syntax for kind of this flavor of reusable code.In this exercise, you looked at transforming for loops into comprehensionsYou looked at throwing exceptionsand catching them and then going back to our previous lesson on data classes,you talked about, you learned how to write your own data class or practiced how to writeyour own data class in this example.So let's take a quick look at the solution here.I'm not gonna go through all of this because this is allkind of code that you've already seen me demonstrate,and I do want to make sure that we have plenty of time toreview types and also really dive into our FastAPI project.So let's check, take alook at main.py here. In thisexercise, we had a list of project names,we had the list comprehension that we wrotefor slugs, where we looked at every single name in those project names.And what we returned was a new list that made each value lowercase and replacedevery space with a dash.Looking at a tasks list of dictionaries, you created a comprehension where yougrabbed the title for every task in this list of tasks,where the status of that task, done, whether it was done or not,was false. So this comprehension givesyou all of the tasks that are still open.You validated a project name and we had our type hints here that name was a string and it returnsa string. If this through a value error,you raised an exception and provided a detailed message saying that that project name can't be blankAnd lastly, you wrote adata class project, name is a string, slug is a string,archived as a boolean with a default value of false.If you look at the project archive status on a project where you did not pass that variable in,you verified that it was set to false.
