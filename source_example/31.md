> > Nina Zakharenko: Final project, we're going to build our release tracker V1 and we'regoing to actually put some, we have stuff in our database.We're going to wire that up. We're going to wire up our app to PostgreSQL.We're going to implement CRUD, create, read, update, delete on project.We're going to read some endpoints. We're going to replace our dictionaries and I'm going to show youhow to do some debugging in VS Code.We're going to write some endpoints, including, like I mentioned,we're going to need to generate that slug.We don't want to have to constantly pass it in. That's very error prone and I'm going to mention whenyou might want to use session.refresh.And lastly, we're going to do a refactor at the end.So we're going to extract the database logic from our route handlers into a crud.py andthen you'll have an app that's shaped like a real app.It's going to read from a database. It's going to have some routes, and we're going to actually havepersistence. So it's going to be very exciting.So we've defined these read and write schemasin our models and now that our database has migrated,we can actually go ahead and use them and replace this with real database queries.And if we look at our app here,we still have this mock database here.If we want to go ahead and connect toour real database, we're going to need to use a database session within our app.And FastAPI's solution to this, to having the session available is dependency injectionAnd it means that theframework is going to call a function before that endpoint runs and pass the result inSo, taking a quick lookat database.py, we've created already a getsession function and what this does is it saysget the database engine, make sure it's configured from our settings.We have a default database URL here, or we can overwrite that and then go ahead andget me back a session.So we can now use one of the features of FastAPI,which calls Depends to inject that session directly into our routehandlers. And we already saw annotated a little bit.FastAPI highly encourages that sort of annotated syntax to make our functionsignatures really clean. So, let's take a look back at main andwe're going to delete this mock database here.We don't need that anymore.Going to have a few errors, but that's OK.So we want to make sure that weare as descriptive as possible when we create our API in our app.So we're going to add another field here with a description.This is going to show up in that slash docs endpoint, and I'm going to say an API for trackingproject milestones and tasks for devsSo that's just kind ofa nicety. It doesn't really necessarily do anything.We also want to make sure that we import annotated.So that's going to be imported from that Python typing package likewe saw before. So we've imported annotated from typing andwe've deleted that mock database. Now we're going to go ahead and use annotated to get that sessionor demonstrate how our route should get that session,and we're going to say annotated and that type of session,and we're going to use something called Depends, which is imported fromFastAPI, to say that in order to have a sessionwe're depending on this getsession method from our models.Sorry, from our database. Andwe will see import error here.We also need to import session from SQLModel.So, and now we havethis annotated statement that says, hey, every time you need a session,make sure that you're calling that get session method that we specified.We're going to give this a name.Just a quick word of warning, our tests right now are still targeting our mock database.They're not going to pass, so let's go ahead and delete our tests for now,just so that we know that our project is kind ofchanging shape. So let's see what this looks likefor our get method for projects, and I'm just going to move this one up here.This list projects, we need a route that's going to fetch all of the projects from the database,and we're going to use that, we're going to use SQLModel to do that by creating a selectstatement, and then we're going to execute that select via that injected session that we have.So we're still using ProjectRead here that hasn't changed.Our schema definition hasn't changed very much between the mock database that we were using andthe table that we were using.We just added a few things like table equals true and a few more kind of things of validation,but otherwise the shape looks fairly similar.So, we're going to change this a little bit,and this method is now going to takeas a requirement, a session, andwe're going to use that session Depends to make sure that we're getting our session in the right wayThat means that we can now use the session withinthe method. So, first I'm going to write my statement.I'm going to say select project dot order by projectname. So, if you have select missing here,it's imported from SQLModel.And we want to make sure that we are also importing a project from our models hereas well. So right now, all this does is get us all the projects,order them by project name, and to get those projects,we're going to say session.exec, which is execute thisthe statement, and give usback everything. And then we're going to return a list of those projects.So this is the SQL running against the database, in this line we're actually executing that statementand then we're going toreturn the list. So this is more of aSQLAlchemy 2.0 style statement, butyou can use SQLAlchemy when you need yourselections to be a little bit more complex.There is a lot of kind of niceties in SQLModel,but sometimes by using them you're kind of obfuscating what you're actually trying to do.So we're going to change our get projects by project ID endpoint tooand we're still using that response model of ProjectRead.We still have our route set correctly, but our method signature has to change becausewhat do we need in addition to the project ID session?Yes, we need a session.And that's going to have a type hint of a session DependsSo, I'm just going tocomment this out so you can see the difference.The way that we get our project now is we're going to say project equals session.get.And we're looking for a type of project, and we're passing in a project ID.Same thing here. If it's not present in the database, we're going to raise that HTTP404 not found exception and return the project.So because we have our schemas definedthis switchover in particular wasfairly simple and straightforward, I think.One quick note for anybody who has used Django ORM,in Django ORM specifically get, if you don't find it,it raises an exception like does not exist or something like that.SQLAlchemy made the opposite choice, which means SQLModel made the opposite choice.Instead it just returns none. I think that's nice,makes the code read a little bit cleaner, but just FYI.When your queries get more complex, just know that if you're using the oneinstead of all, let's sayhere we went with one, this is going to throw an exception.It'll raise an exception if nothing is found with this statement,saying something like no results found, it will also throw an exception if more than one result is foundto say, hey, you wanted one, but actually there were multiple matches to this.So just FYI that even with SQLModel, SQLAlchemy,there are cases where you need to specifically catch exceptions that could be throwndepending on your database queries.So let's take a look at thisby running a uv run FastAPI devand let's go to that docs endpoint and see if we can get our projects from the database and theseed data that we used before.And hey, look at that.We're now actually switched over to getting from the database and there's3 primary keys here 1, 2, and 3, which also means that we can then try outgetting the project by ID instead. So in this slash docs I can hit try it out description.Sorry, project ID is going to be one here, and this will actually go ahead and get us our exact
