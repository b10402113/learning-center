> > Nina Zakharenko: So hashing is going to kind of solve our password storage problem for now in this iteration of our APIThe next thing we wantto think about is how do we keep the user identified across requests,because if somebody had to log in every time they try to access your API,they probably wouldn't use it.And so we're going to do that with JSON Web Token.HTTP requests are stateless, so everyrequest is going to be independent. The server doesn't really know if it was the same client that hadjust logged in a moment ago. So to carry that identity across requests,the server is going to give the client a token after login,and the client is then going to send that token with their authenticated requests.And an important note about that token is it has to be cheap to verify,hard to forge, and self-contained.So let's take a look at how to generate tokens with JWTAnd before we dive intothis, I just want to kind of talk a little bit about what's in a JWTand you're going to havea string, and it's going to have three parts that are separated by dots.There's going to be a header, a payload, and a signature.Each part is encoded with base64.So in that header, you're going to have some metadata about that token, including what the signingalgorithm is. The payload is going to have some claims in it,so it's going to be a JSON object with what the application might need.A few claims that you might see are sub, which is subject, which isthe user that the token is for, and exp for expiration time stampor any other custom claimsthat you might want, and then a signature. So that's going to be that hash of the header and thepayload using a secret key that is only known to the server,so it's really important to keep that secret keyprivate. You know that the header and that payload,they're encoded, but they're not encrypted, right?So anyone with a token could possibly decode them and read the contents,but they can't necessarily change them because if they change anything about them,any modification to that payload, that's going to make the signature invalid,and then the server could then reject that token on the next request.So that's kind of a little bit about how this design works.The token has, we could consider the token as being a little tamper evidentThe signature is going todepend on that secret, and the secret is what stays on that server.So let's go ahead and add PyJWT.So I'm going to do a uv add PyJWT,and we're going to talk about the tokengenerating function, and that's also going to live in security.py with these passwordhelpers as well. And we'regoing to add a few more imports here as well,so we're going to be doing some date logic, and for that we're going to have to import from datetimeAnd we're going to importUTC. We're going to import datetime as well as timedelta,which is some helper methods for doing math between dates.We're going to import jwt.And then from our config, we're going to importget_settings. And we're going to add a few config filesor sorry, we're going toadd a few config flags in here as well.You might want to have these in a file or as an environment variable on the server.They don't need to be hard coded, and in many cases probably,yeah, they might not need to be hard coded,but it's kind of up to you and your application and your use case.So we're going to name one access_token_expire_minutes, and I'm going tosay 60, so your token is good for60 minutes, and for our JWT algorithmwe're going to use HS256The method to do thisis maybe a little bit more complicated or complex than what we've seen so far,but I'm going to show you and then we can kind of talk through it a little bit.So to create that access token, what we need,and I'm going to put a star here to say that any argument after this is going to be akeyword only argument, I'm going to look for a subject,I'm going to want expires_delta, and that's going to be atimedelta, or it could be None with a default of None.And then this is going to return a string,and let me make sure that I'm spelling access correctly here.So we want to check what the expiration is.So we're going to do a little bit of date math here,and we're going to say datetime.now in UTCplus the expires_delta or thetimedelta in minutes of with the access_token_expire_minutesAnd we want this logicto kind of happen. This is one or here,so we're going to add that into parentheses here to make sure that this gets evaluated,and just to kind of make this line a little bit nicer, I'm going to wrap this whole thing inparentheses too, just to kind of have itformatted a little bit better.Now for our payload, what we want is a dictionary,and we're going to have a subject which is the subject,and the exp, which is the expires at.And then lastly, we're going to encode it.So we're going to encode the payload, and we're going to get our settingsmodule here, and we're going to use our settings to set a JWT,sorry, a JWT secret key.I want to read it how it's spelled.And we're going to say get_secret_value, and we'll kind of come back to setting that setting in justa minute, and we're going to set our algorithm here is the JWT algorithm.So just to note here, subject is keyword only in string type.That's going to be the user ID stringified,and then the expires_delta, that's going to let thecaller override the default token time, so maybe some tests want like a deliberately short or an expiredtoken, and that lets us do that here.The fallback is one hour, and again that JWT secret that's going to come from the settings,it's not a constant, and we're going to talk about that in a little bit.But there are some secretsthat belong in settings, and that signing secret is one of them,because your source code might get leaked.You want to make sure that your signing secret is protected, and you definitelydefinitely want different keys fordifferent environments. So if somebody, you know, anattacker or bad actor learns your staging key, that doesn't mean that they can then create valid productiontokens. So we're going to add a little bit to our config.pyhere. And we have oursettings class here where we have our database URL, our debug URL.We're going to add one more.And we're going to add a setting for our JWT secret key,and we're going to use a new type of Pydantic field that we're going to importcalled a SecretStr.So by using this as the validatortype, it tells Pydantic, hey, never log this,never return this in a request, like treat this asa secret. We're going to say that has a minimum length of 32.So if that settings instance, if it's logged or somebody tries to print it out,it's just going to render as a bunch of stars instead of leaking that actual key.And this minimum length is going to ensure that the key islong enough, because we don't want a key that's too short.Now, this is the first setting that we've introduced wherethere's no default for it. So if it's missing, our app is just not going to start at all.It's just going to crash at startup with a Pydantic validation error.So that is intentional, because if you forget a secret that is worse than missinga database URL, because the app might work locally,or it might use, you know, a key that is just like super secret test key accidentally,not something you want. Sounfortunately, the tooling for checking out chapter exerciseshas .env as an ignored file, and sothe way that our chapter exercises work, that file is unfortunately going toget blown away for the next few exercises.It's a little bit of extra work for y'all, but there is asample file that you can grab, or if you just want to make a .env file stored outside of yourenvironment and just move it back and forth for the exercises,that should work. Apologies for that.I did not want to commit a .env even with,you know, just a whatever random made up secret because it's just such a bad practice.So that means a little bit of extra work for us.So let's take a look at what our .envfile might look like, so you could have that,optionally, a database URL. We don't really need this,so I'll delete that for now. You do want to set debug to true, and then we're going to haveto come up with a secret key.We can use Python to do this. So from the command line, you can just say you run Python -cwhich means just run thisnext text that's in the parentheses, and we're going to import secrets,and we're going to print a call to secrets.token_urlsafeand give it a length of 32 here.And that should print out a secret for us that we can then copy and paste into our environment.And just a reminder, try not to check this .env file into Git.It should be in the .gitignore of your project if it's not already any kindof files that might contain secrets.The reason being is that onceyou commit something in Git without a lot of extra work,that key is going to be available in the history.And it's kind of a non-obvious issue.Someone might think, oh, hey, I committed my key.I'm just going to delete it in the next commit.Well, that history is still there and it's available.And a lot of modern tooling now has checks against this.Like, when I was deploying my sample application to Netlify to try to show all of you,it was like, hey, you know, you can't, I've detected a secretin your code. Are you sure you want to do this?So modern tooling has some niceties.Don't depend on it though. So be mindful not to commit keys.And we're going to talk a little bit about how this looks on the production side.Your production application should not have a .env.Your secret should be managed in a much more secure,less error-prone sort of way.So now our app can create tokens.We want to verify them on requests that need to be authenticated.
