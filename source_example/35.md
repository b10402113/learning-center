> > Nina Zakharenko: So we have this defined, that means that route that touches the database has thatclean way to ask for a session, and that's a lot of kind of the boilerplate that we've been usingWe do still have, ifwe look at crud.py, thisrepeating pattern of, sorry, if we look in main.pythis repeating pattern of, ohI don't have a projectraise a 404.Oh, I don't have a project, raise a 404.So we can use this new kind of dependency annotatedidea to pull that out and say, hey, every time we actually need a project,let's create a project dep so thatwe know that that guarantee is met in every route.And we're going to add that into our dependencies.py.So we're going to add a new method here.It's called def get project or 404, and it's going to take a project ID whichis an int. It's going to take a session because we need our session dep.And what type hint should I put here for what this method returns?Is that? Yes, it returns project, yeah.So to implement this, we are actually going to callthat method on crud. And we're gonna say crud,get me the project. Oops, sorry.And we see that get project takes a session,which we have, and it also takes project ID.So those are the two arguments that we're going to pass into this get project or 404.And now what we can do is take this logic,if not project, I'll start with the delete route here and move itup right here, which means that we alsoneed to import HTTP exception here and status as well.And lastly, we need to return our project, whichclears out that error for mypy saying, hey, you said this was going to return a project.Well, where is it? Thatmeans that we can now define a variable for this.And I'm going to call this a project dep, and I'm going to say this is annotated.And what we are looking for is a project, and what it depends on is the result from get projector 404. So that removes a lot ofthe boilerplate. Do I nothave, what's going on here?I think we're going to do some modifications,but this is just a mypy error.It's complaining. So this lets us clean up ourroutes even more. For ourproject ID here, instead of saying, hey, what we need is a projectwe're instead going to movethat to a project dep.So I'm gonna import that from our dependencies.And for this get projects route, for example,we're not going to be taking aproject necessarily here anymore. We can say, hey,we want to use project dep instead.And I'm gonna talk about that in a little bitbecause we want to make sure that weset that right here. So that would kind of be project,project dep. That's gonna bekind of getting injected right here, so we wouldn't actually need any of that because that projectdep relies on get project or 404, which is gonna need a square bracket after annotated.What's that annotated should have a square bracket after it.Oh, did I miss? Thank you.And then at the end too, yeah.Thank you. Sorry, typo. Does that make sense here how pulling in a project dep nowmeans that all of this is kind of guaranteed to happen?It was one question why the square bracket mattered because versus the pen.This is like a typing syntax.So the parentheses are usually like, hey, you are instantiating an object or callinga method. This is the syntax for type hint.So for example, when we had variables and we wanted to say likethis takes a list, but we wanted to specify, hey, it's a list of strings or it's adictionary of strings and integers, that's that same syntax right there.So, good question and whoever caught my typo, thank you.So there's a little bit of nesting happening here as well,because our get project or 404 herehas kind of this nested dependency to session.So FastAPI is going to resolve those nested dependencies automatically.It fetches that session first, then it passes it to get project or 404,then it passes that resulting project to the route handler, and that same pattern works with differentchains of dependencies, including, let's say an authentication handler that needs to use thecurrent session to make sure, to look up the current user,for example. So right now, get project or 404 is tied intoproject. It's hard coded. The detail here is project not foundLater on we're gonna adda get task or 404.There's a way to generalize, sorry, obfuscate isthe wrong word, generalize itI would say there's away to generalize this type of logic with a lot of kind of fancy type annotations that we'renot going to discuss here today, butit is something if you're interested, you might want to look that up because for larger APIs these kindsof patterns are yet another thing that can be refactored out,
