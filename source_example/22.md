> > Nina Zakharenko: So we're going to move on to our next exercise where we add a few type hints and we writeour first test. And we're going to annotatesome functions that you already wrote: is overdue and next status.We're going to add a Pydantic model for computer that takes a brand,that's a string, RAM gigabytes as an integer,and hard drive gigabytes as an integer.And try out a valid one and an invalid one.And lastly, you're going to write some tests and run the test suite.So in this exercise, we added type hints and we wrote our first test.So just to quickly review, you added a type hint for is overdue and next statusand we used a duedate as the type hint of date or None.And the status is that task status type that we alreadyused in chapter one. For next status, the type hint for status is also taskstatus, and that next status returns a task status.We already looked at how to use Pydantic.And let's take a quick look at that.So for this, for defining a Pydantic model for computer,you import from base model, andwhen we use it, we want to catch a validation error.So here our computer has a brand that's a string, RAM gigabytes,that's an int, hard drive gigabytes, that's an int.And in our code here, we tested, we created a laptop that should work,all the values validate. And then we tried to make a computer with RAM gigabytesof the text string 32 that failed with a validation error,which we then caught and printed out.Now, lastly, when we're testing test_computer.py weare, when we create this computer, we're asserting that all the valuesmatch what we expect. And we're also testingfor our validation, our Pydantic validation rejectingthe string, and we use the pytest raises here to say,hey pytest, this calling this should raise a validationerror. And lastly, we're using that pytest raises syntaxagain to check and make sure that when we create a computer brand is required.So if you ran uv run pytest and your test pass,you've successfully completed this exercise.One quick thing about VS Code before we move on.So, initially when we set up VS Code, I gave you some examplesettings for Ruff to format on save.I think we're maybe a little bit too aggressive here.So to open this up again, hit command and comma,and then click on this thing to open the file.And we want to make sure that source.fixall.ruff explicithere, we're going to turn that off. We don't want Ruff to fix all of our issues like unusedimports when maybe we have the habit of saving files after adding an import.So if we add an import, we don't use it and we save our file, that's going to automatically removeit. So let's go ahead and turn that off for the remainder of the course.
