> > Nina Zakharenko: We have a little bit of a problem with our API.It's currently just totally wide open.Anyone with the URL can create projects, change task status,delete data, you know, maybe that's OK for something on an intranet,but still, probably not. You might want to do a little bit of logging to be like,hey, why did that disgruntled employee who's on their way out delete all the tasks in our databaseSo we have a littlebit of an issue here.What we're going to do in this chapter is we're going to look at some password-based authentication whereusers can sign up, they can log in, they can get a JWT token that isincluded with every authenticated request.We're going to talk just a little bit about authentication and authorization.That is a deep topic, and I think there is actually a Frontend Masters coursejust dedicated to this one topic, is that right?Yeah, we have several kind of security and authentication courses we've got.Some that kind of cover authentication strategies from the client side,ways between, you know, passwords and passkeys and that aspect,and then also same like Backend system design with, covering like,you know, more authentication strategies on the other end.Yeah, so just to note here, this is not a deep dive.There are whole courses on this topic in particular,but I just kind of want to give you a little bit of an overview of how this might looklike in a FastAPI app.So very briefly going to talk about password hashing and how to handlethose hashed passwords with a Python library.We're going to talk about JSON Web tokens,also for some inexplicable reason, pronounced JWT but written as JWTWhat it is about theirsignature that makes it tamper evident and how we can make one in our Python code.We're going to talk about protecting some of our endpoints.So we're going to add a dependency called get_current_user that's going to enforce thatthere is a valid user on some of our routes and acurrent_user_dep alias that we can use to do that.Lastly, we're going to talk about password hash and some token generation andhelpers, login endpoint and how to protect the right routes.That's what you're going to be implementing in your exercise.Just a quick note, the way that we're doing it in the sample app,all of the read endpoints are going to remain unauthenticated,and that will allow us to demo that kind of dashboard that I showed you earlier in the classwhile all of the endpointsthat write or actually make any changes in the database are going to be protected by authentication.Let's talk about authentication. So a real API probably doesn't want to justhand its data and write paths to anybody who's finding that URL,right? So there's some problems that show up.They're different but related. And that is one,knowing who a request is from and deciding what that person is allowedto do. So that's usually called authentication versus authorization.Authentication is your identity. So that's confirming that the person sending a request iswho they claim to be. Authorization meanwhile is focused on permissions.So once that identity is established, figuring out which actions arethey actually allowed to take.So if we're going to assume that we're running on an intranet,maybe anyone can see those tasks and projects,but if you want to make changes, you need to log in.We can have your authorization system can be infinitelycomplex, you know, you might want to have something likeper project roles or maybe only owner deletes,or you know, an admin, maybe an admin is the only one who can delete.Out of the scope, but I do recommend some of those other authentication and authorization courses onFrontend Masters. It's really kind of an interesting topic.So most API authentication today is kind of going to fall intoone of two shapes, like either a single sign-on and OAuth 2 or,and when you use single sign-on or OAuth 2, that's going to delegate that authenticationto a third party, whether that be Google, GitHub, Okta.Users are going to click a log in with X, and that third party handler is going to managethe password storage and password resets, account recovery.You can also use managed identity providers.There are really a fair amount of choices, or it could be a first party email and a password.So that means that your app is going to store those hashed passwords.It's going to verify them by itself, and that flow is a lot simpler to ship.There's no third party setup. You don't have to pay a vendor.There's no callback URLs, you know, nothing that is provider specific that could trip you up.But the tradeoff for that is fairly extreme,which is that it is now your responsibility to safely store those passwordsand that the credential securityis going to REST entirely on your app. So kind of for you to decide that tradeoff.If you have a simple app, you know, play app, and it doesn't really have any users,maybe you don't have to go all the way.If you're, you know, a banking service, for example,maybe you do want to roll your own auth so that you can have,you know, a lot of extra checks and, you know,verify that your security processes are being followed,but everyone else is probably somewhere in the middle there.So, and I really do want to stress thateven though we're using this as an example, we're going to learn about hashing,tokens, dependency-based protection. Really like rolling your own auth is often a poorchoice. Libraries can go out of date.Your database could get compromised.There are a lot of things that could happen. So you can use API keys,SSO, other managed identity providers that really for production shape data is the bestchoice. So for our auth flow we're going to have 5 steps.We're going to have registration, so user can be created with a user email and password.That password is going to be hashed before it touches the database.We're going to allow login, so the client is going to POST that email and plain text passwordto /auth/token. And we're going to verify.So that server is then going to look up the user by email, hash the submitted password,and compare that against the stored hash.And if that matches, the server is going to sign a JWT,and it's going to send it back, and then that token is going to encode some information about the userand the expiration of that token.And then lastly, for those protected requests, the client isgoing to set the JWT in a specific type of header,and the server is then going to verify the signature,decode that user ID, and it's going to decide, do I let that request through or do I return a401 unauthorized error. FastAPI does ship with some tooling built in for step 4 and 5They have an OAuth2PasswordBearer forthat security scheme and an OAuth2PasswordRequestForm for parsing the login form,and there's some nice kind of dependency injection for plugging that into
