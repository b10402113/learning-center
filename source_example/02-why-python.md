> > Nina Zakharenko: So a little bit about why Python.And you're going to notice that Python shows up in a lot of places,like really kind of almost anywhere in modern software work.You've probably heard of Django, Flask, FastAPI, the data science stackpandas, NumPy, SciPy, Jupyter notebooksThere's a lot of AIwork that happens as well. You can kind of think of Python as really being on the forefront of kindof the AI movement, and it's also used a lot in infrastructure work,so you can kind of think about it as the glue for a lot of scripting,automation, pipelines, etc. One of thestrengths of Python is also PyPI, which is the Python Package Index.So there's hundreds of thousands of packages on there.A lot of the popular ones are really well maintained, well documented,really battle tested in production as well.If you need to talk to Postgres, talk to an API on the internet,there's a lot of kind of really solid libraries on there as well.One note about Python, I think that's kind of important.It's not necessarily the fastest language.So if you're doing a lot of CPU-bound work, you might notice that,where Python comes in is, you know, really the strength of PyPI,and it's easy to read, it's easy to write.It's really kind of robust. It's been around for a long time.And you can really kind of get something up and running fairly quickly as well.But if you really need to write something that's highly performant,and like, you should actually, you know, verify that you need something highly performing,because a lot of times that optimization happens before it needs to happen.You know, think about Rust or C instead.So some thoughts on AI and vibe coding.In my previous courses, I was all about having everybody type along with me and getting that musclememory and kind of really having a solid concept of the syntaxWe're not necessarily doing thattoday. Kind of in the modern day and age,that's a lot less important.But I do want to kind of throw out a few warnings about using LLMsWith great power comes greatresponsibility. I've seen LLMs generate a lotof really junk Python, and some of that is because Pythonhas been heavily represented in the training data for LLMs,and a lot of times that can be out of date.If you let the LLM decide how to write things,it might not put in types. It might not use the PEP 8 standard,which is the standard for what Python code should look like.It might use some library that hasn't been maintained in three years andhas kind of a lot of big security issues with it.So the goal of this course is kind of to help build a foundationthat lets you read AI output with a critical lens and accept what'scorrect and reject what isn't.It's really important for your agents to know how Python should be structured,what tools should be used, how to install packages,etc. So like having a really strong, you know, agents file,claude.md, whatever it is that you're using, that tells the LLM how you should readwrite, run Python, install packagesis really critical because ifyou try to create a Python app with an LLM that has zero configurationyou're going to have abad time. So I'll share some resources about how to do that at the end of the course.Just know that shipping code to production that you don't understand can get you in trouble.So write and read Python that you understand,whether you typed it in or a model did.We're going to be doing a lot less typing in this course than we would have otherwise.But I really kind of want to focus on the concepts and the understanding.
