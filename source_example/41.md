> > Nina Zakharenko: Middleware is code that runs before and after every request that is processed by yourapplication. It kind of acts as a bridge between the client and the route handler,and you can use middleware to intercept incoming requests,you to maybe you want to log them, check off, modify headers,before they reach your route logic, and you can also use them to intercept outgoing responsesbefore they're returned to the client. So, incredibly useful.And the way that you kind of set them up is by using app.middlewarethat decorator. We're gonna saythis one's for HTTP and we need to use async def here.And it's going to take a request and something called callnext. So inside this methodwhat we have to doto make sure we define it under our app, so that it's context aware there.What kind of has to happen in here is we're gonna say that response is equal to await of callnext request. And then we return the response.And any pre-route work goeshere. Because that is before the endpoint iscalled. And any post-route work, andthat's after the endpoint finishes.So a little bit more magic.We take the request and call next function is what actually passeswhat passes the request downthe chain to that route handler or to the next middleware.And once your route handler finishes, call next is going to return the responseobject. So that lets you modify it, change it, log it before it goes back to the user.And if we register multiple middlewares, each one is going towrap the next. So if I did app.add middleware,let's say first middleware, and we had a bunch of other ones,and I did an app.add middleware for last middleware,it's going to be executed in this order. So on requests,it's going to be last, then anything in the middle,then the first, and then the route.And on responses, we go the opposite way.It's going to be the route, then first,then anything specified here, then last.So you can add lots of middleware, and many projects often do.Just a quick note about why middleware has to be async when our exceptionhandler was not. So, under the hood fast API runson async IO event loop, and call next actually returnsa coroutine. And what that means is it needs to be awaited for toactually run what's next, the next middleware, the route handler,building the response. So await is only allowed to be used inside of an asyncdef function. Synchronous middleware just wouldn't work.It has no way to call and await for a call next and get that response back.And we don't have that constraint, and exception handlers,those handlers don't await for anything by default.So those can be sync or async, middleware always async.So, let's say that we wanted to add a new middleware,knowing what we know here, that is going to do some logging.We want to figure out how long did each request take and what was the status code,the method, the path, and have that information as infoin our logs. So what we would do is sayapp.middleware on HTTP and we'regonna do our async def. And we're going to call this one log requests.And it's going to take a request and a call next.And we already know, this is a bad habit,by the way, that I have. I don't know if anybody else has bad habits.I just want to type colon after decorators all thetime, still to this day.I also started as a Java developer.So every once in a while I'll throw in that semicolon at the end of the line,and I'm like, why? I haven't written Java in a decade.Where is this muscle memory coming from?So we already know kind of what we want to do here at the core, right?That's going to be the response, await call next with our request.But now we also want to do something before and after.So I'm gonna use something called a perf timerand in order to dothat, we're going to have to import it from the standard library,so I'm importing the time module here.And I'm gonna say that our start time is equal to time.perf counter.And our process time is going to be equal to time.perf counterminus the start time, and times 1000because we're gonna use, I think times milliseconds,milliseconds, yeah. And then we're going to return our response.But of course, we actually have to do our logging here,so I'm gonna use this log line here.So we're using our lazy formatting here again.Here we're specifying a float of milliseconds, so string,string, float, status code.Every HTTP request in the app is now going to produce a timing log line.And that's without having to add that type of logging to the request,to the route handlers themselves, that would be so bloated,just all here in one place.And a quick call out on time.perf counter.For short durations, time works weird.Time on your computer can drift depending on how it's set.Perf counter is the most accurate way to count very,very short durations in Python, andyou might want to use it for something like this when you're logging a requestthat probably won't take a lot of time, that will help you get that very accurate time resolution.So we wrote this middleware, a few other custom middlewares that you'll probablycome across. One is CORS middleware.And it's so common that it's just provided by fast API.So that tells our browser that if our API is hosted at,you know, API.releasetracker.com, and our front end is just atreleasetracker.com, don't go to that website.I don't know what's on there. I didn't buy that domain.That's just an example. That tells our front end that,hey, we're on a different origin, but we still want to communicate to the API and in this casewe specifically want to allowit. Talking about CORS is out of scope for this course,but it is something that you will come across and it's something that you'll probably have issueswith at some point if you're doing a lot of talking between different domains oryou know, API in afront end comes up a lot.And there are a few other types used very regularly.
