> > Nina Zakharenko: So we learned all about type hints.Now we kind of get to one of thebest use cases for having those type hints,which is data validation with Pydantic.So Pydantic is not part of the standard library.We're going to have to add that as a dependency to our package.Let's quickly look at Pydantic in PyPI, which is the official Python Package Index.And we'll kind of see here, here's a command for how to install it with pip,which we're not going to use, tells us what version the library is on.Gives us a release history so we can kind of take a look and see like,hey, is this project actively maintained?And then it'll also provide project links usually to the home page or to GitHubThese can be configured, soif you're looking for a package that's not legitimate,you know, be mindful of that.And also provides some GitHub statistics here.So 27,000 stars. And Pydantic is datavalidation that uses those Python type hints to enforce that validation in runtime.There's a lot of modern Python packages that now use Pydantic as well.It's a really great thing to learn to make your codea lot more error-proof.There are a few scenarios when you might want touse Pydantic. It's going to be useful when data,especially data that you don't control, crosses a trust boundary.So like an API request body, you know,somebody, you have an API that supports getting something by primary key,maybe somebody wants to get it by slug, and that's not supported.And you can quickly even see like, hey, I'm expecting an int and you passed me a string.So that's very basic validation.You might want to use it when you have a configuration file to make surethat all required values are present.If you're checking values in a file somebody uploaded and you expect it to look a certain wayand match a certain spec, using Pydantic is a really good way to check that andsay, hey, is my data shaped in the way that I expected to?So let's go ahead and add Pydantic as a dependency,and we're going to do uv add pydantic.This time we're not installing it with that dev flag because we want Pydantic acrossall of our environments, and we'll see here that it was installed.Close this. So now that we have Pydantic added,we can do from pydantic import.And BaseModel is kind of what you'll always be extendingfrom. And we can make a class now called ProjectCreate.And it's going to extend from BaseModel, and we're going to say there's a namethat's a string and a description that's also stringSo that kind of looksa little bit like a data class, but now we want to definenot just kind of type hints for these two fields,but actually what that data needs to look like to be valid in our projectAnd to do that, we'regoing to add another import here called Field.So we're going to say our name is a string, but it's actually a field,a Pydantic field, and we want to define a minimum length of 2 and a maximum lengthof 120. And this max length of 120that might be something that'slike a database constraint, right? And that's why you have to have it in place.So anything that doesn't match this is going to be resulting in error.And for the description, we can use that union operator to say,hey, it could be a string, or it could be none.And we want this field to have a default that's none anda max length of 1000.So that field is going to add constraints on top of,is this the right type.So let's check this out in our REPLAnd now we can justinstantiate this like we did our data class.And we'll see that's a new object there.It also prints nicely. Oops.Let me assign that to a variable.Now, where we kind of see the meat of Pydantic is if we try to create a new project andthere's some validation error. So we know that the minimum length ofname is 2. So if we just try to pass in a one-lengthstring here, we're going to get a nice Pydantic validation error.And this is why Pydantic is kind of such a powerful tool in Python and why it's souseful at that API boundary, because as we saw with regular type hints,there's suggestions, we can actually do whatever we want.But when we start using Pydantic at runtime, Pydantic is going to say,hey, you have a validation error, and not only that,it's going to give you exactly what that validation error was.You don't have to dig into the code to be like, well, why didn't I get it right?You don't necessarily have to read a bunch of complicated rules.Later on in the course, we're going to talk aboutsome kind of further customvalidations with Pydantic, for example.I have this minimum length equals 2.Do you think that it is going to give me an error if I just pass in a string withthree spaces? No, right? Because that doesn't, thatstill passes this validation. So as you work on production code,your needs for validation are going to get more complicated.So you probably are going to want to, you know, strip that string and maybe clear off the empty whitespace in the beginning and end, and then check that.And Pydantic has a lot of nice built-in tools that you can use to build upon.
