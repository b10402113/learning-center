> > Nina Zakharenko: Creating a project with UV, I'm going to kind of quickly show you how to do this.We're actually going to beworking from a kind of a fresh repository from the get-go,so you don't necessarily need tohave your own kind of generatedproject here, but I'm going to quickly show you how to do it.All right, here we go. So letme do kind of a new demo directory here,and I'm going to do UV init and Pythonand here I'm going touse, I'm going to say greater than or equals 3.14,and we'll do that.It's going to initialize a new, using the UV tool,a new project. And if I look in here,we're going to see a few generated files. First off, running UV init this way createsthis folder as a git repository.It's not connected to GitHub or anything like that, but it's just there set up for you.If you take a look at Python version here, you'll see that it's set at 3.14because I specified it with that flag.There's a main.py file, which has a little bit of sample code in it.There's a file called pyproject.toml, which I'll show you very quickly.It's got a name, a version, a description that you can modify,the readme, the Python version requirement again, and then this kind of list of empty dependenciesIn the course setup stepswhich we'll talk about ina sec, you really want to make sure you have a modern version of Python because,and you're specifying it, because chances are your system versionof Python is probably very out of date, especially if you've not maintained it,could be 3.9 or like much older, which doesn't have support for a lot of the modern featuresthat we'll be using. I had a quick question about the usage of a Python version file like at theroot repo. My understanding is that it, you know,will enforce like any commands run within the shell, make sure that it's using that version,but like, do you know how strong of an imperative it is when it comes to like looking at itthrough a certain IDE? Like I've had, you know,like PyCharm, for example, try to impose like, oh, I have this.I should be using this version or this compiler, and it's like, you know, stop and try to specify itin the project. Yeah, so that is usually done with a,that's a great question. It's usually each Python project that you work on is probably going to be tiedto a specific version of Python. It's going to have packages added that you'll probably want specificversions on. Every project you work on is going to have different dependencies,and so best practice is to have something called a virtual environment for each Pythonproject, and this UV tool actually does that for you.So if we take a look here, let's do ls -la again.We'll see kind of these hidden files.If I run uv sync, which is going to look in this pyproject.toml file at the dependencies andinstall them, right now there's none, but it is still going to create kind of an empty virtual environmentSo if I look hereagain, we'll see that UV has created this like .venv file that has all of thatinformation for the project. And soyou could manually activate the virtual environment.You might be used to doing that for kind of older Python projects by sourcing the.venv, bin, I forget what it is.I've been using UV for so long that this is no longer muscle memory,but if you source the .venv bin activate here,you'll see in my terminal now says helloworld.py.What that means is if I say which Python,when I'm in this virtual environment, it's pointing at the version of Python that's in that hidden .venvfolder. If I type deactivate, I'mout of that virtual environment, and if I say which Python now,it's not even found because there's no Python linked.If I do which python3, that's like my system Python.So it's a great question.UV wraps a lot of this under the hood, which is why it's become fairly popular.So if I do a uv run main.py,that will, that's telling UV use that version of Python we specified,and you should see kind of a hello.So just a quick recap, UV init, that created that project,made metadata. There's a few arguments that you can pass in there if you don't wanta git repo, for example, you can run it with --bare.Yes, UV is the new hotness.Yeah, it's much more widely adopted, widely used than poetry.
