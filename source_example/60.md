> > Nina Zakharenko: So that's it, you're done.You have gotten a fairly good understanding today of some modern Python bestpractices and tooling and type annotations and a lot of the goodstuff that has gotten added into Python over the past few years to make your development experiencedelightful. And for our final project, I have gone ahead andput that up on GitHub. So if you want to take a look at the code,you can clone it. So I'm just going to quickly do a very fast codebase walkthrough.I'm going to talk about how the demo connects the Frontend and why you should not do thatand what you should doinstead, where you might want to go from here, like some additional features that you might want to addand lastly, a few linksto additional resources. So I have grabbed a copy of this codebaseThere are a few differenceshere. Mostly the same app, handful of additions.So let's take a look at our task model,and we'll see here that I've added two new fields to task.One is created_at, the other is updated_at, so that we have a record of when that taskwas made and when the last update happened.For something like a task that's probably going to have a lot of updates,this is going to be a really useful field.So we have a timestamp column herespecified. I've also added thisupdated_at field. The thing that you really want tonotice here is that there's a default factory here of ourUTC datetime now method that we created earlier that returns now as adatetime. And you'll notice thaton update, we're going to go ahead and call that function.So the default is UTC now and every time an update happens is also UTC now.And I also added something that would be fairlyimportant to a project, which is what happens to tasksunder a project when you delete that project?And here we want to make sure that we set the on delete cascade equalstrue. So when a project is deleted, that is goingto make the relationship explicit and saywhen the project is deletedgo ahead and delete allthe tasks under it as well.For task read, I'm going ahead and exposingthat created and updated at as well so that clients can sort the task list by recency,for example. The migrations thatintroduced these columns are also going to recreate the foreign key with on delete cascadeAnd because both of thosechanges go together, the existing database is just going to pick up the cascade behavior at the sametime as it picks up those timestamps.Here in main.py I have called a configure logging in somethingcalled a lifespan. And thisis a context manager. It's not called at module import like our previous logging configuration wasLifespan is FastAPI's startup andshutdown hook. So code that is before the yield runs once when the app boots andcode after the yield, right now there's nothing here,runs once on shutdown. So that's a more idiomatic way ofsetting up logging. And the advantages of doing it this way is that the loggingis going to configure after the app is fully constructed.Any startup lines from FastAPI itself can also flow through that configured handler.It's a lot easier for test code to override the settings object before that lifespan runsbecause we aren't setting any settings at import time.There's a test fixture in the authentication chapter that relies on this.And it's also just the standard FastAPI pattern.So matches every example that you might see out there.For as we've been building this, it's just another layer of interaction,so I didn't think it was worth adding to our app.We've also added a little bit of metadata here.So if I look in release tracker, I've gone ahead and set a version,and I've created also morethorough routes that has a little debugger statement here,has the app name, the version docs, our health endpoints, so everything that we might want to see.Dependency injection stays the same. I did add a few more tests that you might want to look at,and there's also some instructions that I have provided here forrunning the demo, if you'd like to go ahead and do that.That project is at Anjana slash release tracker,and because I had GitHub Actions configured, you can also go ahead and go to the actionstab since it's public and take a look at the workflow that was run onmy commit. As we saw at the beginning of the course,this is my demo page here for projects.I can filter and have a nice link here to the API docs,filter by priority, etc. So this is just a single static HTML pagedone with Lit, and it's read-only.You can't authenticate, you can't change anything about the projects or tasksjust uses fetch calls andthen renders them. In production right now, this is bundledwith my FastAPI app. You don't want to do that in production really everbut you can take alook through the sample codebase to see how it's done in this example,how those static files are bundled, and there's also more information on the course website about howthat is done as well.I did it this way so that I wouldn't have to deal with a CORS origin setupbut if you do needto worry about that, which you probably will, there's a section here at the bottomSo my Frontend and theAPI share an origin. I don't have to worry about this.You're probably going to make a separate Frontend, and your browser is going to block that API trafficunless there's a configuration with CORS headers.FastAPI does have a CORS middleware for that case, and you can go ahead and configure it.
