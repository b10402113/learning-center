> > Nina Zakharenko: We're going to talk about a few deployment best practices before we wrap up the course and takea little tour of the final release tracker application that is posted on GitHub just so you cantake a look at all the code in one place, with a few additions as well.Right now, our app works on localhost.Which is great, but how do we get from localhost to a deployed service?We're going to need a few things. We might need some command line tooling so that our code base staysclean as it grows. We might need a container so that the artifacts that run in production are the sameones that ran in development.We need some sort of workflow around secrets.We would like to have a continuous integration workflow to prevent broken main branches.We want to run a few quality checks. We want to run Ruff,MyPy, pytest, coverage, and include those dev dependenciesso that we can have the quality checks as part of the deployment process.I'm going to talk a little bit about dockerizing a FastAPI app,managing those secrets, GitHub Actions, and then for our exercise,we're going to build a Dockerfile. We're going to run the stack,and we're going to confirm that our app comes up with that bundled Postgres container.So a little bit about Ruff, MyPy, pytest,coverage. Right now, Ruff has just been running through the VS Code extension.Pytest, we've just been running it on demand as we make changes.For production, you need a way to specify what checks you want to have availableand use them in the terminal because you don't know who's using VS Code or where Ruff is installed orany of that. You really want to focus on reproducibility so that any contributors can runthe same commands fairly easily. So, we're going to use Ruff for linting and formatting,MyPy for those static types, pytest for tests,and coverage for some test coverage reporting.So, I'm going to go ahead and add a few additional devdependencies. So to do thatI'm going to do auv add dev, and we're going todo coverage, MyPy, pytest, andRuff. And now we can do, nowwe can run Ruff on the command line and see where it is that we've introducedformatting issues, so. There's probably lots of places.And I'm just using no cache.Oh, all my checks actually passed, but let's go ahead and format anyway.And we'll see that 18 files were already formatted.So Ruff check is going to run the lint rules that are configured in that ruff.toml filethat we saw earlier. So here I'm saying,hey, Ruff, don't look at the auto-generated Alembic versions.I didn't make that, you know, it's totally fine if it doesn't pass the standards,and then pass in a few flags as well for some things that I won't care about.So just a few flags and things to know.If you run Ruff check with --fix, it's going todo some auto fixes of things that are safe,reorganizing imports, you know, if you're running it, then you're ready to remove unused imports andyou know, kind of othersimple fixes. We don't necessarilywant to run that in CI because that could change the code that wascommitted. So that's something that you probably want to run locally but not check in that configurationor pass along that configuration.--no-cache, that's going to skip any caches that Ruff has andmake sure that we don't have any sort of stale environments.Now, for MyPy.We want to have a tool.mypy section to configure it just like we have tool.pytesttool.uv, tool.fastapi. We are goingto take a look at what a config like that might look like.So here we have a Python version that we specify 3.14.Some warnings that we want to turn on and excluding a few directoriesas well. For this project, I'm not doing type hinting in my testsand again we're ignoring AlembicSo let's save this, andI have a feeling MyPy is going to complain quite a bit.But maybe not. Let's see what happens.Cool. So I have some incompatible types here.I have an error where a function is missing a return type annotation.So, just a few things I missed, and it's nice having all these checks in one place,so you don't have to rely on like mousing over things in your editor to figure out what went wrongSo just quickly coming backto these flags. This one meansdo reject functions that have some type annotations, but not all.If you're working on a legacy project and you're slowly adding typing,this might be a setting that you might want to set differently.A few other flags, I don't necessarily think it's worth discussing each one just becausethey differ so much project by project, but the excludelist is useful here. And lastly, we know that pytest already runs on its own.Coverage is kind of data that answers like which lines of the code base are covered by tests,which lines of the code base do tests actually exercise, and which ones they didn't.Coverage is going to run the test suite and record every line that was executed.And so I'm going to add a few more configuration options here to my pyproject.tomlfor coverage to say, here's the source, and in my report show what's missingbranch = true is goingto track both line and branch coverage.So that means that if there's a function whose if statement is always taken on the truthy side,that's going to count the falsy side of that as missed.So let's take a look at our coverage.We're going to do a uv run coverage and do a Python-m with pytest. And -m is the way that we specify the moduleLet's take a look atthe report. And hey, it's pretty good, not bad,right? I'm missing some coverage here in our database,and it tells me, kind of per report exactly what is missingwhat line numbers weren't coveredSomething that's really nice aboutcoverage and it is really impressive to managers who haven'tseen something like this before. Uv run coverage HTML and that's goingto create an HTML file that you can then viewA little afraid about what'sgoing to happen when I try this, but let's check it out.All right, it opened right here.So that's the coverage HTML report that is automatically generated.I can have a check mark here that hides everything withcovered that is fully covered, so.Yeah, this is fairly nice.If you really are, if your team's really into test-driven development,this is one great way to generate some reports that check for that.We do want to have a recipe for checks and keep those commands in the same order asour CI runs to minimize any issues with our local versus CI workflows notmatching. I do want to point something out here.Which is the use of pre-commit hooks.So for larger projects that do want to ensure this or want to ensure certain checkshappen before they're even committed to theremote repository, they might use something like a pre-commit hook,which makes sure that the checks happen automatically before each commit.Ruff has a good tool for this.It's a fast Rust implementation of a tool called pre-commit.You can install it this way and configure what hooks that you want to run.Tooling for this is out of scope, but there's good documentation that covers this setup.
