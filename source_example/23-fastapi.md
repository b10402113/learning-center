> > Nina Zakharenko: So now it's time to get to talking about FastAPI and exactly whatFastAPI is and why it's kind of taken Python web development by storm.I think fairly recently, within the past few months,FastAPI has overtaken Flask as the number two web framework being used in PythonSo it really kind ofmakes the web development a delight and feel like magic, and a lot of the issues that you might havebumped your head against using other Python web frameworks,they just kind of don't really exist in this web framework becauseof the layers of validation and its reliance on using type hintsand modern tooling. So just kind of a few things Iwant to point out, FastAPI is fast, but it is what itsays on the tin. It's built on top of another web framework called Starlette andPydantic. It's one of the fastest Python frameworks available.It matches a lot of other frameworks in terms of benchmarks,and it uses those native Python type hints that we talked about.So there's really great editor support, we get nice auto-completion.There's that validation out of the box.You just write standard Python, and we'll look at the automatic OpenAPI integrationand how we basically get for free a Swagger UI and a ReDoc page for our APIreally without having to do anything, which is just delightful.You don't have to use any tools.You don't have to use curl if you don't want to test your API,and it's a way of documenting exactly what your API does to anybody who uses it.So you don't have to manually write out that spec.And lastly, the developer experience is just delightful.There's a lot of boilerplate that is just significantly reduced.You're not parsing JSON. You're not validating query parameters,it just happens for you under the hood, and you can kind of focus on the task at hand.So just a quick note, Flask and Django versus FastAPI.Django is really a batteries included framework.It does everything. It comes with its own ORM.It comes with its own templating layer. It lets you create an admin panel.If you need kind of a really big monolithic web application or you have a support team that needs accessto the database in a protected way, that's a really good application for it.It can feel heavy and opinionated, but it's still,I think, the number one web framework being used in Python,so. Flask is more of a micro framework.It's very unopinionated, but it's older.It doesn't support type hints as well.It doesn't really use Python's native async capabilities.Flask has added async support recently.FastAPI was built from the ground up to support modern Python,and it's kind of, it's a little lightweight like Flask,but it's fairly opinionated and it does provide really massive productivity boosts,not just for writing code, but also for maintaining it and debugging it.So, you all should have therepository already cloned, and we're gonna doone quick thing, which is we're gonna add anew remote. So you already cloned the repo on your local machine.So if you do a git remote -v, you should see an origin set up here for both fetch andpush to this repository. We're going to add a new origin.So it's a separate repo that has all the solutions for theexercises that we're doing today. If you get stuck, you can take a look at that instead of having forme to inline all that code in the web page.So we're gonna do a git remote add, and the new name of the origin is gonna be solutions,and this is available for you to copy and paste on the website too.And then the repo is nnja slash python-for-pros-solutions.gitand we're going to fetchthat as well. And now if you dogit remote -v here, you should see that there are two origins.If you haven't had a chance to do this, there'll be further instructions in the exercise as well.So moving on to FastAPI basics, we kind of looked at what it'slike to create a new project with uv init.But for our release tracker application, we're gonna want somethinga little bit more complex.We need to install some tooling andset up some configurations. So, you'll have deleted your pyproject.toml,and then we're gonna run uv init, packagePython, but we're also gonna provide a name ofrelease tracker. You don't generally need to do this,only if the name of your project differs from the name of your source folder.So now if we look at source, we'll see that there's a folder here called release tracker.So, same thing that uv init is going to add a readme Python version,and git repository, but add some new configuration into the pyproject.toml and createsa source release tracker folder with an empty **init**.pySo when you use thisit does do a littlebit of scaffolding. Because wealready had a main.py here, it didn't generate one for us.But we are going to open up pyproject.toml,and we are going to delete this project script section here.We don't need that just yet, but we'll come back to it.And then when we open source release tracker and open up this **init**let's empty that out aswell. We don't need that for now.We are going to add a file called ruff.toml to set some linting rulesfor our project, but I'm going to cover that during the exercise,and that's going to be a way to tell, not just VS Code, run Ruff when I'm saving files,but to have a way to run Ruff on the project from the command line programmatically.So since we emptied out our pyproject.toml, we do want toadd now our FastAPI as a dependency.So we're going to do uv add, and we're going to provide fastapi and standardThis uses a version ofFastAPI that bundles a lot of the dependencies for us,and we're going to specify a version here, and we're going to say greater than or equal to 0.115.0And it's going to addthat to your project, put it in the pyproject.toml and install all of the stuff that FastAPI dependson. Now we're gonna opena new file and we're going to make it in source release tracker,and we're gonna call it main.py.And we're going to write our first FastAPI app.So we're going to say from fastapi import FastAPI.And then we're gonna say our app is equal to FastAPI,and we're going to give it a title and we're going to say it's called the Release Tracker API.And then we're gonna write a method called list_projects,and list_projects is going to return a list of dictionaries.And we're gonna return just like some sample data here.We're gonna return a list, and it's gonna have a dictionary in it,and the ID of one and a nameof example. And then to turn this into a route in FastAPI,what you need to do is add a decorator. We're gonna say @app.get,which is the HTTP format that we want to support.We're gonna provide a path here of /projects.So app equals FastAPI, that creates our core application instance.We're going to use that app object quite a bit later on to register more routes,set up middleware, add event handlers.App.get, that's our decorator, and we tell it what route we want to support with this function.We have type hints here, sowe are defining our type hints to let FastAPI know whatto expect. And lastly, FastAPI is going to convertthat Python dictionary to a JSON response.So now we're gonna go ahead and run our server,and we're gonna need to edit pyproject.toml one more time.So make sure that you have the latest copy open,and we're gonna add a configuration.And we're going to call it tool.fastapi, and we're going to set a valuefor our entry point of release_tracker.main and then a colon and app.And that will let us, that lets the FastAPI dev server know how to find the code.You can leave this out and just provide a path to the file where your app lives.So let's try this out. If weare in our python-for-pros directory here, we should be able to now say uv run,fastapi, and dev. And let's see what happens.Should bring up a console here that says server started,and we can go to this URL and take a look.We don't have anything going on here, but we can take a look at the documentation by going tolocalhost 8000/docs, and it'll actually say like, hey,here are what the methods are that are supported on this API.We have a GET for /projects, takes no parameters,takes a successful response, and we can actually even try it out in here by pressing this try it outbutton, saying execute. And we're just going to see that stub in theresponse body. But this hit the web server live, which is pretty cool.So, just a quick note, running FastAPI with fastapi dev,it just makes a really thin wrapper that runs the underlying web server under the hood,but it gives you all of this nice terminal output that's colorized.It's a much better developer experience, and it also supports live reloading.So as you code and add stuff, unless you introduce some bad syntax,your web server should just recognize that.When we switch to production instead of fastapi dev, we're going to use fastapi run.So, this is for your environment only.So a little bit about using that source layout, we want to create our code under source/release_trackerto keep our source separate from config, test,scripts, anything else. So that's kind of the main place that the code for our API is going to liveAnd because of setting uprelease tracker as a project with that uv init,it's installed in our virtual environment, which means that our test can import the same package thatthe REST of the code does.So that should help with some of the errors that you were experiencing earlier.So that is our first API endpoint anddare to say I think it was pretty easy,right? So, a quick noteabout async versus sync endpoints, you might be wondering why arewe not using async here, and FastAPI does support both,and you can mix and match depending on what your endpoint needs to do.You'll probably want to use async def when your endpoint needs to await some other operation,database call, network request, third party API.Let's take a quick look at what that might look like.So it might look something like that.If you just define your route with def, FastAPI recognizes it.It's not going to block the main event loop. It's not going to block the entire server,freeze up. It does run that function in an external thread pool.For this class, we're just going to use def throughout until we get to a point where we need tointroduce async because the ORM layer that we're goingto use SQLModel automatically supports async operations, so we don't necessarily have to defineit in every place and writing async layers does addcomplexity. You have to manage sessions, async database engines.It's harder to rely on lazy loading relationships between two different models.Tracebacks are harder to read. You have to modify your tests.So, those synchronous database callsand the standard def endpoints, it does make code a little bit easier to read.The FastAPI thread pool will handle that concurrency for us.But know that depending on what your API does,you might need to use more async versus sync.
