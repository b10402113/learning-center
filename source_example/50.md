> > Nina Zakharenko: Quick note about password hashing.Never store plain text passwords.And I'll say it again, never store plain text passwords.There's some issues here. Your database could be compromised,SQL injection. Leaked backup, maybe a LLM decides to post allyour data on the public internet, maybe you misconfigured a Cloud bucket,and every user's credentials could possibly be exposed,right? So a lot of users will reuse passwords from site to site.A leak can turn into account takeovers, bad news.So we're going to store the hash and not the password.Hashing is a cryptographic function, takes an input of any size,and it's going to return kind of a fixed size string.It's one way, it's cheap to do it.But I'm going to future proof this with modernalgorithms right now. It's, I wouldn't say impossible,but infeasible to crack some of these.That is not a guarantee for the future. Also, some of the hashing algorithms thatused to be used are no longer considered secure because of this.So just be mindful that technology changes and what's safe today may no longer be safe tomorrowBut kind of what's importantabout this is that when you use a salt,it is deterministic. So if you provide the same password and it's hashed twice,it's not necessarily going to produce the same output because of that salt,but you're still able to verify it.So what we're going to do is when the user is going to log in,the server is going to go ahead and hash that password and submit it,and it's going to compare it to that stored hash, and if it matches,then the password is right. The server doesn't necessarily even have to know what the password wasSo a quick note aboutthe libraries that we're going to use.We're going to use a library called pwdliband one of its kindof associated features called Argon2id and I'mgoing to show you how to go ahead and add that to your project.So we're going to do a uv add pwdlib,and then we're going to pass in argon2 here.And it's going to install some stuff.Pwdlib is kind of a modern Python library to do some of this hashingwork for us. It has a fairly small interface,I'd say. It defaults to Argon2id.Argon2 is, ID is analgorithm. It won the password hashing competition, but that was in 2015,and they have not had another one since, but it is currently kind of therecommended choice for new applications, and if you ask FastAPI,hey, what's your recommended algorithm, it's going to point to Argon2.So kind of a quick note about Argon2id is that it'sdeliberately slow and kind of deliberately memory hungry to try to put up some roadblocks to anybodywho might be trying to hash it.So it does make brute force attacks a little bit less effective than they would be for some of thoseolder algorithms, but again, not necessarily bulletproof in thefuture. So passing this intoour pwdlib install, adding that module just kind oftells us to, or tells pwdlib, go ahead and install that Backend.So we're going to add a new module here.And we're going to call it security.py just kind of at the top level.And I'm going to go ahead and import from pwdlib the PasswordHashfunction. And I'm going to say that password_hash is equal to PasswordHash.recommendedwhich again in this caseis going to return Argon2id.And a few helper methods here, aget_password_hash where we are goingto return password_hash.hash of a password.And then also a verify_password where we havea plain password here as a string and our hashedpassword here as a string.And that's going to return a boolean.And we're going to return our password_hash is going to have a verify function where wepass it our plain password and our hashed password.It's going to return true or false if that was verified or not.So get_password_hash is going to create that hash that we're going to want to store in that databaseand then again, verify happens
