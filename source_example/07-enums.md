> > Nina Zakharenko: So, let's talk a little bit about one of the features in the standard library enums and Python.Those are really useful, and I don't remember what version of Python they were added in,but they're kind of really awesome.So let's say that I had a status here that I wanted to use all through my code,and I named it in progress, and it's like, well,what was it? Was it status equals in underscore progress?Is it two words? Is it all capital? And then you have to go dig through the code to figureit out. So enum is something that helps with that,specifically string enum. For enumsthat have strings, there's also an int enum and a kind of a few other types,but string enums are probably the ones you'll see the most.So I can have a class task status here,and I can say like planned is planned and in progressis in progress, etc. Etc.So something that's useful here is now I havehints. I can auto-complete in my code.I don't have to remember what task status in progress actually was.I can just select that in my editor here.So it kind of solves the class of problems around typos.Invalid states are a lot easier to reject as well.And let me go aheadand put this in the REPLand we can see thathere as well when I look at this in the REPL.We'll see that the type is of task status.planned,and the name is planned.And by printing that outyou also just get thenice keyword thing right there.So much nicer if you're kind of working with that fixed set of values.This is the approach that I recommend.If you need numerical statuses, usethe type of int, you know.So you might be looking at this and saying, well, I just already typed all of these things out.Why do I need to, you know, this is duplicative work.I just typed the same thing in there.And you know, maybe you have a bunch of these and you don't want to do that.You can also import auto from enum.Oh, let me go ahead anduse that here. So if I do that, I can just say auto.And if I paste that inhere as well, oh, I need to, so I got a name error,name auto is not defined. That's because I didn't import it.So let me go ahead and do that in the REPL here as we're typing.And if I look at my task status.planned,we'll see that it is the same thing, yes.Why do we need to inherit from string enum?These are just normal class fields, right?This is an enum, a string enum type.So if I look at, let me go ahead and paste this in really quick.So if I look at the type of task status, we'll see that it's an enum,enum type. If you just defined a classwithout this, if you wanted a string enum and you didn't pass this in,none of basically none of the auto-completion would work.This auto method wouldn't work. So we're kind of specifying that it's a string enum type.You know, this could probably be a little bit nicer syntactically,but because it was added later on in Python,that's kind of the syntax at the moment.If I could extend on that question, yeah, what I think they might be asking about is like what aresome of the other advantages of using this typed enum type,like what benefits does that class extension give you that you wouldn't have if you just made class taskstatus and manually put them in?Yeah, so this auto thing is kind of one of the nicest ones.It's just a really quick way to deduplicate code and again like prevent typos.Using int enum is useful as well, but this is kind of a foundation forsome of the other kind of work we're going to be doing where we're checking stuff.So when we, for example, have set list of values that we wantto store for a database column, that's like a great use case fora string enum, because we can do very quick validation and say,is this one of the accepted types that's accepted in this column?Yes or no. Does it match the correct format? Yes or no.If you made your own class that kind of did all of this,it would just be a little bit more overhead.Does it protect us from the kind of mutable type coercion that Python doesimplicitly? Like in that use case, for example,if we were going to be adding new values to the class to,you know, new fields, but we knew what type of database is expecting this,I'm guessing this is just a nice guard rail for that type safety to protect us from like accidentallystring casting it or int casting it or yeah, and there's other ways to kind of check and validate forthat which we'll cover, but here we're also being explicit that the members in this enum mustbe strings. So if I wanted to do like something elseequals one, you're going to get a nasty errorIt's going to say, heythat's not a string, andyou kind of get that for free even without any sort of type hinting,
