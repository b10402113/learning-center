> > Nina Zakharenko: So let's go ahead and do a quick exercise with some ofthe concepts that we've learned.We're going to do an exercise on comprehensions, exceptions,and data classes. So for part one, rewrite this for loop asa comprehension. Next you're going to takethis list of titles and build a list of titles for tasks that aren't done.You're going to write a function that raises and catches an exception,and then you're going to define a simple data class called project with 3 fields.You're going to print it and confirm that archive defaults to false.If you don't remember the syntax for this,you can refer back to the previous lesson.Yes. Do we have to worry about decorator functions not being apure function and creating side effects in our code?Yeah, decorators do create side effects.Do you have to worry about them?Not necessarily, especially if you're using decorators provided by a librarybut yeah. Do decorators addadditional functionality to an already existing function?Why not create another function and call that?Yes, decorators do add existing functionality.They also, you'll frequently see decorators stacked, so you can have multiple decoratorsfor one function that do different things.The reason that you want to use them instead of another function is because herewe're just adding this log call decorator.We don't care what arguments this function takes.We don't even care about what this function's name is, because of this syntaxwe're kind of doing stuffwith the metadata of the function itself.We can also, decorators can do a lot.They can change values. They can log stuff.But they're definitely kind of thecleaner syntax for kind of this flavor of reusable code.In this exercise, you looked at transforming for loops into comprehensionsYou looked at throwing exceptionsand catching them and then going back to our previous lesson on data classes,you talked about, you learned how to write your own data class or practiced how to writeyour own data class in this example.So let's take a quick look at the solution here.I'm not gonna go through all of this because this is allkind of code that you've already seen me demonstrate,and I do want to make sure that we have plenty of time toreview types and also really dive into our FastAPI project.So let's check, take alook at main.py here. In thisexercise, we had a list of project names,we had the list comprehension that we wrotefor slugs, where we looked at every single name in those project names.And what we returned was a new list that made each value lowercase and replacedevery space with a dash.Looking at a tasks list of dictionaries, you created a comprehension where yougrabbed the title for every task in this list of tasks,where the status of that task, done, whether it was done or not,was false. So this comprehension givesyou all of the tasks that are still open.You validated a project name and we had our type hints here that name was a string and it returnsa string. If this through a value error,you raised an exception and provided a detailed message saying that that project name can't be blankAnd lastly, you wrote adata class project, name is a string, slug is a string,archived as a boolean with a default value of false.If you look at the project archive status on a project where you did not pass that variable in,you verified that it was set to false.
