> > Nina Zakharenko: All right. So we just went ahead and added some logging and some migrationsto our app. And now we can talk about kind of a meatiertopic, which is, now that we have tasks in our database,we have a task model that we can use in our Python code.What are we going to do with it?The task model is a little bit more complex than the project.It has a few status fields. It has a due date that we have to keep track of.But we haven't built that into our API just yet.So in this section, we're going to build on the task.We're going to build the task API.We're going to introduce relational queries.We're going to introduce query parameter filters and talk a little bit about eager loadingand how that's going to affect our relationships.So we're going to have a new router for tasks, and it's going to look a lot like the projectrouter, but with that relationship to project in play.And we're going to talk about doing some queries and filtering that looks a little bit more like theSQL that you might be used to. We're going to use where join and talk a little bit about selectin load and how, if we don't use that and N+1queries, could be an issue when a response includes related data.So we want to make sure that we're using our database and those relationships as efficiently as possibleWe're going to talk aboutusing database fixtures within pytest so that we can easeour testing process a little bit.So, talk a little bit about using in-memory SQLite,as well as the static pool pattern, and a fixture chain that's going to build up a little bit ofsample data every time we test, so we don't have to do that manually in each testAnd lastly, for our exerciseyou're going to implement somefiltering in that list tasks method and write some tests to make sure that yournew code works. So it's time to build our task endpoints and the task router is going to look fairlysimilar to the project router from that previous chapter with one key addition.We're going to have another dependency called task dep that is going to load a task by ID and raise404 if it doesn't. Solet's take a look in our dependencies here. We already have a get project or 404.What I'm going to do here is, 100% a code smell.I'm going to take this and I'm going to copy it.And before I do that, I'm going to import my task here from models.One thing I want to point out about this is if you are copying and pasting code wholesale,that tends to be a good signal that there is an opportunity to refactor there or generalizeUnfortunately, refactoring this would involvetalking about some typing concepts that I think are a little bit above and beyond thegoal of the course. And so I'm not going to go ahead and do that.We're just going to duplicate for now. But at the end of the course,I'm going to talk about some features that you might want to add to your application or some refactoringthat you might want to do. And this here is a prime target forrefactoring to have the code base be a little bit cleaner and not quite so tied toour models. All right. The setting isMac option is meta. I don't know if any of you ever use Emacs keyboard bindings.If you try to use them within VS Code,the default Mac behaviors, it just sends weird symbols,and I don't want that option on.So, I'm going to make sure that my Mac option isthe meta key, and I will stop hopefully typing weird symbols as I try to use the keyboardshortcuts that aren't present. All right, so this is going to take a task ID andwe're going to change project to task here.And kind of update all the references hereSo for task, we're goingto do crud.get task. And yeah.So then we check if task is none, raise HTTP exception,and we're going to manually say task not found here, return task,and here we're also going to have a task dependency and say annotated on task and depends on gettask or 404. So, notgoing to go too far into what this does, but now if we need,now if we have routes where a task is required,you know, if it has to be looked up by ID to have some operation done on it,to delete it, to update it, we can use this new task dependency to ensurethat it's present in that route and that we don't have to do any sort of additionalerror checking. And then that crud.get task is going to pick upthat extra detail because we need to eagerly loadthe related project. When we're going to add that filtering logic.For now we're just going to consider it. It's going to work the same way that crud.get project worksIt's going to return eithertask or none. Once westart doing this and ending up with a lot of duplication,something you might want to look up is fast API's class-baseddependencies. You can also move some logic into repository orservice layers, and then using generic helpers isprobably the path that you would want to take for something that looks like this.So that's a single function and it's parameterized by model typing using afeature in Python called TypeVar, or if you'veseen a lot of type Python code, you might have seen thatspecified with this capital T type annotation.But yeah, all of those are kind of out of the scope for this course.It would be a chapter kind of on its own.We're just going to keep this for now.So, we added a project router.Now it's time to add a task router.And I'm going to go ahead and make a new file,and it's going to be in our source release tracker routers,and that's going to be a task.py.And let me just show you all what is in that directory right now.
