> > Nina Zakharenko: And one more thing I quickly want to talk about is how we might want to setup fixtures for our tests to make this a little bit easier.And I think the best way to kind of talk about this is going to be just to show youSo let me quickly changemy environment, and we can talk about what that would look like.So if we take a look at test slash comp test,we're going to see thatevery test is going to end up with its ownSQLite instance that's going to be isolated.And our test client here is wired tooverride that production session. So we want to have a few pre-populatedrows. Now just a quick noteabout why we're going to be using SQLite for tests.First off is speed. So SQLite, super fast in-memory SQLite database for testscreated in seconds. If you have a test suite that spins upPostgres for every single run, it's going to take a really long time.So depending on your code base and how thorough you want to be,if you are using Postgres, there is a good chance that you want to be runningthose tests maybe in some kind of integration testing step,but as a developer, that's not necessarily something that you want to be running on your local machineas you're regularly making changes.It's also nice in this configuration that every test gets its own database,so the tests can't interfere with each other.But do you know that there are some limits to this.SQLite doesn't enforce some of those Postgres specific behaviors.There's some weird stuff around native enum types.There are a few constraint checks that don't work in the same way.So you kind of want to be mindful that,you know, even if you have 100% test coverage,if your production database is Postgres and your test database SQLite,you are going to end up with some gaps.And that could also hide some subtle bugs that you might get with interacting with Postgresbecause that underlying infrastructure doesn'texactly match. So it's kind of up to you to decide when and where wouldbe appropriate for your specific project.So very quickly here, the session fixture, it's going to build a fresh in-memory database.It's going to create every table from SQLModel metadata,and it's going to yield a session that's bound to it.So just want to point out two non-obvious bits that make this fixture workthis SQLite path here.That means that there's no path pointing to your file system.It's just an in-memory database.You have to worry about cleaning stuff up after every time the test runs.Each new engine starts with an empty schema,so there's no leftover state between every time that you run your tests.Static pool here with a check same thread isfalse, that's going to keep that in-memory database alive across multiple connections withSQL Alchemy and that FastAPI test client open.If we don't have that pool override, the database could vanish between connections and the tests couldget some weird cryptic errors.Lastly, that fixture uses this yield syntax, so the with session block opens before the test runsand closes when the test finishes, and that's going to trigger a rollback on any sort of uncommittedtransactions and free up the connection.Now, the client fixture. So our test client that'skind of synchronous HTTP client from FastAPI's test utilitiesdoes a little bit of kind of wrapping magic to give us some of the samemethods accessible in our tests that we would have otherwise, but the client fixture,that's going to kind of bundle this up so that every request inside a test is going to use aper-test session, not the real database.So specifying those overrides here andthis app.dependency_overrides is kind of FastAPI's, you can think of it like anescape hatch for tests. So anywhere that the app would normally call get_sessionto get a database session, FastAPI is going to call this override instead.And then inside that test session depth resolves to that same in-memory only SQLitebacked session. So any writes that are performed by this test or anykind of previous fixtures, those are going to be visible to subsequent requests.And we also want tomake sure that we clear get engines cache as wellbecause if we don't resetthat cache, a second test could get that same cached engine for the first test buildcould be pointed to aclosed in-memory database. So you don't necessarily have to understand whatall of this is doing, but just the general concepts of this is how we configure a SQLite databasefor a database for testing.So a lot of tests, they need, you know,at least one project to work on. A lot of tasks are going to need at least one task.Using pytest fixtures, let's just kind of bundle insome of that code so that we're not having toconstantly be creating it in every single test.So here we're going to get a sample project ID and we're going to post something toprojects to create a new project. And now we can use this fixture all over our tests and sayyou know, give me thatproject ID for the project that was just created.Same thing for a sample task. So this is just some boilerplate code that we're abstracting away.Just a quick note that posting this through the HTTP layer rather than inserting it into the databaseis a deliberate choice because it lets us use some of that built-in validation andthe serialization code paths that the real API uses.So if there are any bugs in that in our routes,this would also surface them.And lastly, if we look at test tasksI'm going to show youwhat a test that is written to use those fixtures looks like.So let's take a look atthis test. Here, we're using that sample project ID that we'regetting, and that kind of lets us focus onjust the behavior of the tests themselves.So when we use this, it's going to be passed in and we can just focus on actually testingwhat we want to test.Not a ton of setup boilerplate, not a ton of teardown of schema creation.The fixture kind of nicely handles that for us.And then the test kind of reads more as like an API call sequence for whatwe are actually wanting to test for.Quick note that the fixtures in this, they use ascope, which means a fresh database per test, which is a nice default.
