> > Nina Zakharenko: Logging, middleware and also evolving your schema.So we talked about shared dependencies, centralized exception handlers,per feature router. So we took that project's router,pulled it out. We're going to talk about adding some logging.So using standard library loggingto kind of get some more information about our application as wego, just to help us debugwhen we're developing locally all the way to having structured logging in production that can then beprocessed and used with something like, you know,Data Dog. We're going to configure that logging.We're going to talk a little bit about FastAPI middleware, and middleware wraps every request.In this example, we're going to use that to time a request and emit a log line and then talkabout how middleware compares to exception handlers from previouschapters. And we're also going to talk a little bit about schema evolution.So we're going to add a task model and then run an Alembic migration toupdate that database without losing our existing data.Before we add a ton more to our app, we really want to see what's happening inside ofit. And you might have used print statements a few times here and there to be like,hey, what's going on? Print is fine for quick scripts,but logging is what you want to use in production.A lot of times if your production code base has any kind of CI/CDgoing on, then having a check for print statements is usually part of it to make sure thatno print statements sneak into production, because that could be a way to expose client data orsecrets or anything else that you might not want showing up in your logs.So the quickest way to do thiswould be to import logging.And then you can say logging.basicConfig.And you would set a default log level here.And that's kind of all that you need to do.But so that tells Python, send any messages that are info orabove, and we're going to talk about that in a minute, to the console.And this like one line setup is going to show you how to.There's some issues with thisone. There's no name for the logger here,so you might not be sure where this logging message came from exactly,especially in a complex app.We're not sure what the logger levels mean here or how to set, you know,if we hard code it like this.That one sucks. We might not be sure how to,you know, we might want to set it programmatically a different log level for developing locally versuswhat's running in production, and we also want to make sure that we format those logsSo instead of calling thoseglobal log functions, we want to create a logger object for each module,and the convention is to use it, name it using a special **name** variableand that is going toautomatically be set to the module's path.So if we imported logging here, instead of doing logging.basicConfig,we can instead make a new logger.Sorry, that should be logging.getLoggerSo that naming convention createsa hierarchy. If it's the root release tracker,it could be configured to only show errors,and that configuration could flow down to different modules as well.So just to quickly cover log levels.Let me show you a little snippet here.There are several layers of logging andloggers are going to use that severity to filter messages,and here they are in order of severity.So debug is usually for detailed diagnostic info,stuff that you want while you're working in development, tons of information to tryto, you know, figure out a bug or what's going on in a specific route.Info is for general kind of operational events,you know, stuff you might want to know about, but maybe not.Warning is something unexpected happened, but hey, we didn't crashError is something really serioushappened and, you know, maybe a specific operation failed,can't connect to a database, something unexpected that really affects how theapplication might work. And that configured levelis going to act as a floor. So any messages that are at that level or above it are goingto be emitted, and anything else that's below it is going to be dropped.So typically on development, you probably want to run on debug.If something goes wrong, you want as much information as possible.In production, you're probably running on info.So any of those debug messages aren't going to clog your logs.And there's a few ways to flip to debug in alogging level in development. One easy way is to have an environment variable or a configurationfile. So let's say wehad a payload with the detail like.And if we wanted tolog that, we could call logger.debugfor example, to specify thatlog level and say, you know, use an f-string maybeand say we got an error.And this was the payload.The issue with using an f-string here in the logger is this message going tobe emitted if the logger level is set to info?No, but that string interpolation is going to happen anyway.And so specifically for logging, we want to use a different syntax called percent syntaxor lazy loading syntax. And the way that it works is weadd a percent S where we want to add our string,and then provide the payload that way.There's a lot of different formatting options here.Percent S specifically means string.There's other, if you want to get fancy with logging, this is an infinite black hole,an infinite rabbit hole. So all I want to point out here is that this variable is goingto get evaluated lazily. So if the logger level is info,we don't need to interpolate the string.And there's a lot of linters, you know, Ruff,etc. That enforce this, and a lot of production databases also turn that on.Something else to note is that production teams, they might often ship to a structured logging tool,Data Dog, Splunk, and that indexes each value.For example, the status code, the path has its own searchable fields,and that's a lot more useful than trying to search for a needle in a haystack in a long lineand a string. So with percent S using some of these libraries,those values are going to stay as separate arguments.Now, something to point out, we've been using pytest so far and not thestandard library testing module because of a lot of niceties.I would say logging and logger is similar.It's part of the standard library. You don't need to rely on another package or import anything elseBut it's not very niceIt's not very easy touse. There's kind of a lot that you have to keep in mind.There are modern libraries for logging in Python,like structlog, for example, that are probably something that you want to switch to as your project
