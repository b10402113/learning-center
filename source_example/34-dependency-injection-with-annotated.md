> > Nina Zakharenko: Now that we have done a little bit of refactoring, we stood up the V1 version of our application,we're going to move on to dependencies, exceptions,and routing. Because right now the whole file just lives in main.py.That's where all our routing happens.Our database access has moved to red, and we have a little bit of boilerplate that doesa database lookup or returns a 404.So that's kind of another repeated pattern that we've been seeing in our codebase.In this section we're going to talk about a little bit of a deeper look into usingthat annotated syntax that we saw yesterday.So how FastAPI's dependency system actually works under session depth,which we already defined, and we're going to talk about that principle of DRY,don't repeat yourself with using project app.It's going to be a custom dependency that handles that lookup or 404 pattern for any route thattakes a project ID so we can pull out that logic into one central place,and we're going to use something called project dep for that to kind ofsignify, you know, session depth was, hey, this needs a session.Project dep is, hey, this needs a project.And we're going to talk a little bit about some centralized exception handling.So somebody earlier on had asked, are thereother types of exceptions? Yes, there are many, many,many in the standard library, many that usually come custom bundled with packagesand modules you might use HTTP error or exception is kind of one of those onthe FastAPI side. We're going to talk about catching integrity error because in our database,projects and slugs need to be project names and slugs need to be unique.So right now, if you try to create another project with the same exact name,it's going to throw a 500 server error, and that's not very descriptive.So we're going to catch an integrity error just once in main.py so thatwe can kind of keep that logic in one place and not have to implement it in every single methodAnd that kind of helpsour routes read a little bit cleaner as well.Lastly, we're going to talk about API router.So right now we only have one table in the database.What happens to our app as our models grow and our database growsWe're going to have todo a little bit of reorganization.So all of the endpoints for project are going to move into their own modules.So that main.py kind of just becomes this thin configuration layer.So injecting dependencies with that annotated term, we sawthis syntax earlier session dep annotated session dependson get session, which lives in database.py.And this depends function, ittells the framework, before you run this endpoint,run this other function first and give me its result.In our database, we have this very simple generator function andit closes right after the request is finished.So just a little bit about what's actually happening here.When FastAPI resolves this dependency, it executes the code up until this yieldstatement here. And the with session, get a session,that's a context manager like we saw on the first day,as we saw pretty early on in this course.And that means that the context manager is gonna,it opens the database connection, creates a session, and then yields to the route handlers.So when the route is finished and it returns that response to the user,FastAPI is going to resume the execution of the generator after the yield.So at that point, the context manager just exits,and that automatically closes the database session in the same way that we saw file handles automaticallybeing closed when we used similar syntax to open files.Now if there's some sort of exception that happens like that integrity error thatbubbles up out of the route handler, the session context manager is gonna intercept itand clean it up. So SQLModel's contextmanager style here is designed to automatically roll back those active uncommittedtransactions when an exception occurs inside the with block.So that means that our route handler doesn't have to call session rollback manuallyYeah. Does this also meanthat we're opening a new database connection on each request?No, so the database connection happens hereand I will talk aboutthis a little bit later, but this decorator,the LRU cache, that is, yeah, that's kind of how thatgets handled under the hood.So we've already done a little bit of refactoring here because we are alreadyusing this session dependency, but just a reminder,if we didn't have this, we would be typing that session is asession, and it depends on get session in everyroute handler here, which is not very useful andrepeats code. So using this session depth here,defining it just once lets usclean that up a little bit and just say that every session relies on having that session dependencySo that's a little bitabout how we clean up our dependencies with annotated in Python 3.9That's when this feature wasintroduced, and it allows us to attach some extra metadata to a type hint.And FastAPI uses this all over the place.I would say it's one of FastAPI's really nice modern featuresSo we're going to createa new file to handle some of these reusable dependencies,because as our project grows, we don't want to just keep jamming them into main.py.So in here I'm just going to make a new file called dependencies.py.And let's open that up.So first things first, we need to import annotated.And then from FastAPI, we're gonna import thatdepends statement. We need our session.And then from our database package, we're going to import get session.So because there were some questions around this yesterday, I just want to point out for every importfrom typing import annotated, that's from the standard library,these two imports come from packages that we have installed via uv add.And then this last one from dot database import get session,that import points to a module in our own project.So I can in VS Code command click on that, and that will take me right to the method definitionAnd then we're going topull this out and putthat in our new module.So now any endpoint that needs to use the session dep can just importit and use it as a type hint and FastAPI is just going to see that depends get session metadataand it'll automatically inject thatsession. So since we removed this here, we do now want tofrom the dependencies import session depAnd we can clean upour imports a little bit because we're not using this anymore.So this is just another way to kind of continuously keep looking at the code base and refactoring andtrying to figure out what features are present in the packages that we're using thatcould help us achieve those goals.And we're going to see this pattern again later on when we introduce users as well,
