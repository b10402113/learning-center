> > Nina Zakharenko: This slugify method that we had.If we wanted to be really explicit about what the arguments that it takes and the arguments that it returnsthe way to do thatis with type hints. So you can put a colon here and say name is going to be a stringAnd we're going to sayseparator here is also expected to be a string.So you put that colon after the name of the variable with the expected typeAs we saw earlier, wedon't need to import string here, that's just available in the default namespace.And then when we want to specify what the return type is,we use an arrow to do that.So this says name should be a string, separator should be a string,and the return type should also be a string.But note that this is not enforced at runtime.So I can still with these type hints,I can still call, you know, I can still do this.And the error that I get is not like, hey, don't pass an integer in here.It's just that int has no attribute strip.So there is a layer of tooling that needs to live on top of these type hints to make surethat they get checked and enforced.Python doesn't just do that automatically.For comment syntax, unfortunately, no nice multi-line comments.It's just one hash before a line to comment it out.And there's some weird kind of syntaxthat you might see in Python functions that tends to be a little confusing.And that's passing in starsand argument. I want tosay. And I'm using thatF string there. So Ihave that archive project method in the REPL now.And I can do, let's say archive project.I can do notify equals true, force equals true.And what this star does is it says that every argumentin the parameter list after that is going to follow as keyword only.So you need to explicitly name those arguments when youcall this function. So if I did now archiveproject and I said payments API, and then I said true and true.I'm going to get an error because it says archive project takes one positional argument,but three were given, soAnd name here is thatpositional argument. That means that there's no default value set for itand Python is going touse, kind of when you call this function,the position that this argument is in is the one that it's going to use.So I could also do like, you know, if I didn't want defaults here,and I wanted to force having to pass inthat. Then this would workI could also, even thoughthey're not defined with default, it can also,as long as I kind of set them in,can also do that. Even though, you know,these arguments are just positional.So the other format is a lot better.You really kind of want to, when you can,make your code as explicit as possible.So this is kind of a really nice way of doing that and enforcing,kind of solidifying what arguments you're going to get.There's a kind of a keyword called kwargs or keyword argumentsand those collect any extrakeyword arguments into a dictionary.So that can be useful when the set of valid keys is notactually fixed. So let's do a method like this,and we have, we'll do a build project.And we'll have a name.And we'll have a set of keyword arguments.We can say that our name is name and our slug is a slugifyof name. And this makes a dictionaryand we can just updateour dictionary with the kwargs, or sorry, keyword args and return our project.One sec, Mark. So if I did build a project here and I said,payments API and I gave it a description,does checkout, and I said archived, that'sfalse. That's just going to stuff all of those keyword args into that dictionaryIt's also useful for sometimesyou get really long methods and you don't necessarily want to specify each and every one.You might see the syntax here.What is the point of the syntax? So this double star syntax says that this buildproject method can take any amount of keyword argumentsunlike here where we specifiedthem. So if I havethis archive project, I have these defaults here.I can just say I don't need to provide them.If I try to pass in a description here,I'm going to get an error. It says like, hey, I got an unexpected keyword argument description.You never specified that in the function signature.But here I can do that. I can pass in any number of keyword arguments,and they will be collected in the dictionary.Yes. When would you use a regular string versus the F string?Almost all the time, yeah, I think modern Python.The only time you don't necessarily need an F string is, you know,if it's just a string that doesn't do any sort of interpolation with arguments.But if you're doing like, you know, hey,you know, my name is Nina, and I want to print that out,you'd almost always use an F string. There's like a lot of,I'm not going to dive into it because it's not really relevantfor the REST of the course. There's a lot of very fancy stuff that you can do with F stringsYou can do inline formattingYou can call code inthem. There's a lot of kind of niceties.Yes. With the keyword only argument, the single star.Would all the parameters following the star, are they optional,and the positional ones, are they required?Yeah, the positional arguments are always required.And yeah, that star says any argument after this point is going to be a keyword argument.Yeah, it's not necessarily like we don't need to deep dive on this,but this is a good overview of every kind of function signature that you'll generally see in Python
