> > Nina Zakharenko: We're going to move on to section 2 of Python fundamentals.And our goals here are we're going to work with some of those collections that you saw,a little preview of the lists, tuples for whoever in the stream was asking about immutable datastructures, sets, dictionaries, when you might want to use each one,a few kind of common methods. We're going to use for loops to iterate and thentalk about comprehensions and rewriting for loops as comprehensions.We're going to talk about exceptions and when to fail and when to recover when it makes senseWe're going to talk abouta feature in Python called context managers.You don't necessarily need to know how to write your own, but you will see them a tonall over standard Python code, starting from how you read files to a lot of placesin Fast API and other frameworks.And you'll know that it's a context manager when yousee that keyword called with.And then we're going very, very briefly touch on decorators.We're going to wrap some functions with them, just to kind of understand what the decorator syntax isbecause if you're not familiarwith it, it looks like magic, and it's not quite magic.So, going back to our main.py, thereare a few types of data structures that we're going to use all throughout.You might get back a list of projects if you call your projects API.Dictionaries are going to represent incoming JSON.Python dictionaries match up with the JSON data structure,the structure of JSON quite well.We want sets when we are interested in preserving unique values.So duplicate values in a set will automatically be rejected,and then tuple to define a set of fixed valuesthat need to be bundled together.So the syntax for someof these is a little bit confusing, and we'll dive into that in a little bit,but just kind of for reference.Lists are, use square bracketsAnd if we ask Pythonwhat is the type ofthis, it's going to say list.You might be tempted as you're kind of coding along to name your variable list.Don't do that. It's, pretty sure it is possible.I'm not going to try it because I don't want to break my code.But funky stuff will happen.So you might want to be careful with that.Tuples are defined by parentheses.So if we go back and we look at the type of that, we'll see it's a type of tupleSets and dictionaries use thesame kind of outer wrapper. So if I wanted to make a set,I would use curly braces and just to show you that if I added4 here twice, it's not going to save.So looking at the type of that, that's a set.Dictionaries also use curly braces.But they, you set yourkey here and then you put,what is this symbol calledSorry, I'm blanking.Colon, thank you. Put a colon after the key and then define the value.And if we check the type of that, we'll see that that is a dictionary.So it's kind of the main collection data structures that you're going to see.For lists, let's say we had our like a payments APIand we had our portal and we had a ops consoleWe can call the appendmethod here. And something interestingabout append is it doesn't return anything.So it doesn't return the list with that value appended,but instead it modifies it in placeSo we'll see that ourproject name there was appended.You can also add lists with plus.It's kind of a funky feature of Python.It's not particularly explicit. I don't recommend doing this.If you do need to add one list to anotheryou can use this extendOops. I missed a bracketthere. Oh, sorry.We need to define this, and then we'll call food.extendwith that, and then if we look at food,we'll see that these are added.Another kind of funky Python thing like, this is also valid syntaxAgain, recommend not putting thosekinds of Easter eggs in your code because they can be hard to debug.So just use the built-in methods. So lists are mutable.They're probably the most used data structure that you'll see in Python.Something funny about lists is they don't have to contain the same typeSo, again, this is notrecommended. Whoever's consuming this list is probably going to have a hard time with it.So, well, you can theoretically do it, don't do it in your code.So tuples are also kind of this ordered type of collection,but they are immutable. So once you create this tuple,it can't be modified. So, say we have our alpha version andit's version 0.0.2. If wetry to call append on version, Python'sgoing to be like, can't do that. Sorry, tuple object has no attribute append.So you can make new tuples, but you cannot edit existing ones.So those are useful data structures for things like coordinates, pairs, other small records like RGBvalues, you know, tend to be a set of 3 values from 0 to 255and like using a tuplefor that would be the perfect use case.So, sets, as we looked at earlier, they store only uniquevalues. So let's take alook at those and say that we have like a list of unique priorities here.And we're going to use those curly braces.So if we look at unique priorities now, something to note is that these values are unorderedSo unlike a list ortuple where the shape stays the way that you defined it,that's not how sets work.So don't expect a set to look exactly the same as when you defined it.And if I try to make a set,like I showed you earlier, with two duplicate values,there's no error there. Python just does not include it.So, outside of the scope of this class, but you can do all sorts of operations on these valuesYou can do, yes, MarkWhat would a type definitionfor a tuple or mixed list look like?We'll talk about that soon.Yeah, there's, we'll cover typing kind of all together,yeah. Can we reassign tuples?Reassign them. So if I had my like version tuple,can I say version toa different variable, if you change the value of any position of the tuple,it would return a different tuple.You would just be creating a new tuple. There's no way to change a value in a tuple.Yeah, sets are super useful.You'll see them all the time. They're fast.Just don't expect things to go out in the same order they came in.Order is not guaranteed at all.Dictionaries will map keys to values, so.I had my list of projects here and I'm going to say, youcan, so I'm breaking up my dictionary definition here by putting a new line after that initialopening bracket. This is totally valid syntax as long as the lines underneath are indentedI can also optionally havea comma here or not.It doesn't affect the code in any way.Sometimes for multi-line function calls or multi-line definitions like this,developer teams will always add that trailing comma just to make diffs cleanerbut check your project styleguide for how that should be handled.So I have my projects dictionary here.You'll see a lot of dictionaries used in Python,especially in Python APIs to represent JSON, andif you need just thekeys, you can call project.keys.If you need just the values, you can call projects.valuesfor a projects dictionary here.If we want to loop over both the key and the value, which we'll talk about in a little bitwe use projects.items, and thatreturns key value pairs. Sowe talked about lists, tuples, andsets and dictionaries. Just want to show you a little bitof lists, how lists and strings interact with each other.So if I had a string name here,and I had a list of names, ifI wanted to get the length of my list here,I can pass that into this built-in length method.That also works the same for name.I can pass name into length to get the length of that string.You'll often see strings and lists kind of converting one back to the other.If you need to split up a string by a value,like maybe you got a CSV, like a string that's comma separated,and you wanted to break that up into a list that you can work with in Python,you would use the dot split method.So let's say, sorry, I had letters here, A, B, C, D,E. If I called letters dot split, that would split them up.You can also be explicit and specify.I think the default argument here takes either space or a new line or some other stuff.Generally, you want to be pretty explicit about what to split on so that you don't get unexpected resultsYou'll probably see this againlike with CSVs, and ifyou want to take a list and convert itback into a string, you would use the dot join method.And here I'm calling dot join on a string that just contains a space,which is a little bit funky, but you'll see that now I got back a string.I can also join on comma and string,and I'll get a comma delimited string that joins that list of names.And notice here that there is no extra comma at the end.So it's kind of a nicety in Python.We've been talking about the different kind of collection data types in Python,when you might want to use each one.And kind of a little bit of coverageon the kinds of methods that you might call on those.So you took a look at how to take some strings andsplit them to turn them into lists or how to take a list and use the join function toturn that into a string.That's something you'll be doing fairly often.Something that I kind of shouted out already a little bit,but want to reiterate is if you have a list of names andyou call this sort operation on them like we saw before with,I think it was reverse, this doesn't return anythingbut if we look atour names now, we'll see that they are in fact sorted.So that dot sort operation modified the underlying datastructure and same thing with a reverse.Doesn't return anything, but you'll see that the list is reversed now.There's a different kind of class ofmethods that you can call that perform operations without modifying the underlying datastructure. So, let's say I had a list of letters here that were kind of alljumbled. If I call, ifI use the built-in method sorted on these letters,it will actually return a list of sorted letters.And if I take a look at this list, you'll see that the underlying data structure is not modified.So, this is something I really want to reiterate because a lot of timeslearners coming to Python willcall, you know, list.reverse, try to assign it to a variable and use it later,and it's not going to work kind of how you expected, or somebody might call this not expecting it tomodify the underlying data structure and ending up with some weird results.A shortcut kind of way of being like, is this going to affect the data structure or not?You'll see that I called the sort method on the names variableor instance. So that kind of tells me that it's going to do something to the data in that instanceThis sorted is, built inPython type, and I passed letters into it.I didn't call it on letters. And that kind of gave me this different behavior wherethe sorted list was returned.My underlying data structure was not changed.Something interesting about dictionaries in PythonIf we take a lookat this, we'll see that the values don't necessarily have to be the sametype, but keys in a dictionary have to be immutable.So you can't use a list as a dictionary key because thatlist could be modified in memory as the program executes.What can be used as a dictionary key?A tuple, yeah, that's right. Because tuples are immutable, they can be used as dictionary keys and you might come across that.
