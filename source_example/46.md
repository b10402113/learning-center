> > Nina Zakharenko: We've done the routes. Let's talk about what CRUD is going to look like,because we're going to see a few new ways of representing those SQL queries with SQLModel.We're going to talk about where, and that is a predicatethat specifies a predicate that is conditionally applied.We're going to talk about the join function to filter by a related table's columns,and we're going to talk about select and load to make sure that our load relationships are managed efficientlySo, let's go ahead andtake a look at crud.py here.We're going to do a little bit of setting up our imports first.So, here from our models, we're also going to need to addtask priority. Let me seehere. Sorry, this is an old version of the file.Let me see, models. Do we have models?No, sorry, we're good. Just making sure I'm on the right version so I don't confuse you all.We're going to add a few more imports here from our models,so we're going to grab our task, and then we're also going to need that task priority and taskstatus as well. And then from SQLModel, we're going to import session and selectWe're also going to haveto do a little bit of date handling, somake sure those are present, and we're also going to import directly from theSQLAlchemy ORM here.This select and load function.SQLModel is, because it's based on SQLAlchemy, it's bundled with it,so we don't have to worry about installing another package or anything like that.Now, for list tasks. Thisis going to be a little bit of a beefier method.And what this list tasks expects, andlet me see if I can pull this up side by side, so we can kind of take a lookhere at the expected method signature.So the first thing that we need is going to be a session.And then we're going to use that star pattern that we saw earlier to say anyarguments after the session are expected to be keyword arguments.So we're going to specify our project ID.It's going to be an int or none with a default of none.We're going to specify our project slug, and that's going to be a string or nonewith a default of none.And our task status. Which is going to be a task statusor none with a default of none.And same with task priority.Overdue only is going to be a boolean,and if it's not passed in, it's going to be false.And this is going to return a list of tasks.And now what we want to do is create a statement, which is basically a SQLquery that we are building programmatically and run once at the end of the method.So that makes it a little bit easier to read, a little bit less error prone.We're not typing, you know, raw SQL anywhere in here or anything like that.So the way that we build a statement is bystarting off with a select.So we want to select on the task table,and then we're going to pass in some options.And we're going to say select and load task.project.And because of some type checker funkiness, we also have to add,you don't have to, but your type checker might throw an error or a warning here.We're going to say ignore the argument type.Try not to do these too often.This is basically just a hint to the type checker.There's some weirdness with SQLAlchemy, and it's just kind of required so we don't getcomplaints. So the first thing that we're going to check for,we're going to say if a project ID is not none and notice we're not usingtruthiness here, we're being very explicit and just saying like,you know, it's not anything falsy. We're explicitly checking to see that it's not none.We're going to build this statement by sayinguse kind of this existing statement that we defined here as a base.And then also add a where clause and say task.project ID is equal to the passed inproject ID. And if a project slug is provided,we can say if the project slug is not none, then build the statement again andnow join on project wherethat project.slug is equal to project slug.If the task status is not none, then we're going tobuild that statement again. And we're going to say statement wheretask.status is equal to task status.And same thing with the task priority.And notice here that we're not doing an if else if if if,just having a lot of ifs is a little bit easier to read and alsoby using an if else if, you're not kind of catching that case where someof these are provided and you don't know what set it's going to be every time.And lastly, our check for is overdue only,which is going to be a little bit more complicated.So if we only want overdue only, we need to build a statementwhere we're checking that task due dateand making sure that it's not none.If there's no date set, then the task can't be overdue.We're going to check that the task due date is less than now in UTCAnd we're also going tocheck that the task status is not equal to done.And specifically task status done.No. Let's go ahead and fix that.So mypy now is saying we're missing the return statement.So what we want to return, we want to take that session and we want to call execon it. And that says go ahead and executethe statement that we've built up and give us all of the results.But then I also want this to be a list,so I'm going to pass that into the list constructor.There is a little bit of kind of funkiness here.You might see mypy complaining.While this is not recommended syntax for general Python code,we have this nice, you know, is not none.This is specifically part of how a statement is built with SQLAlchemySo we can add afew more flags that basically say like, hey, you know,type checker, let's chill out a little bit,and this one specifically is ignore this code E711,and for this one it's a type ignore,we're going to see the operator.I'm not going to dive too deep into this, also a rabbit hole,but it at least kind of fixes our typing errors for now.So this is how we are able to build a query with different parameters thatmay or may not be present.We build the query and then SQLAlchemy is going to turn all of those multiple statements into onesingle SQL statement.For me, this kind of syntax is a lot nicer than writing,you know, just SQL, not having any of that nice type checking,not having the type checker here also try to give us some hints.So one thing to point out here is we talked aboutproject.slug. So that's a column on projects, not on tasks.If we did want to filter on a project slug,we do need a join statement there.So that's just going to go ahead and add it to the query.SQLAlchemy knows that the join condition is on a foreign key on task.project IDso we don't have tomanually spell it out here.After this join happens, project.slug is going to be in scope in that whereclause. And then the result of that join,that's going to change which rows the database actually walks,but kind of this top level select task is going to keep the result shape.So it's going to return a list of task rows and not tuplesnot projects. Now, a quicknote about that N+1 problem and why we are using select and loadhere. So task read exposesa project name and a project slug.Those come from properties on thetask itself. And when thatproperty reads self.project, it's going to lazy load that related project row fromthe database the first time that it's accessed.And if we don't have any other kind of setup here,calling crud.list tasks and then trying to serialize 50 tasks is going to run51 queries, one for the tasks plus 1 per task to fetch itsrelated project. So that's called the N+1 query problem.Using select and load explicitly fixes that.So adding it to the select is going to tell SQLAlchemy to fetch all of the projectsfor the loaded tasks in one extra query eagerly ahead of time.So, that's 2 queries in total, regardless of how many tasks are going to comeback. That first query is going to load the tasks, and then the second one does an internal lookup withthose project IDs from that first result, and it's going to combine them.So, easy thing to miss, but an important concept to note.And then when the response serializer tries to access task.project name,that data is already going to be in memory, and get task is also going to use thatsame pattern. So let meshow you that really quickly. And we canagain kind of look inour task.py here to see.We're going to want to call crud.get task here.We're going to need a session, and we're alsogoing to take a task ID.So if we wanted to use similar logic,we would do something like this.Let's take a task ID here.And we're going to do a task or none,and we would build out our statement something like this.So we already know that we want to return the session exec on that statement.Here, we'd only want to be getting the first task.And we'd want to select on task.And for our options, do that select and load task.projectand also add a where statement here where the task.ID is equal to task ID.So, looks something like that.And there's again some addedyou might want to ignoresome type errors here. Oh, sorry, I did not miss a comma.This is not how that syntax works.So just to show you very quickly, thisis how you would kind of break up a long statement in Python into multiple linesSo this would work kindof just like this, if we went ahead and made this one long linebut that syntax just makesit a little bit easier to read.All right, so SQLAlchemy does ship a few kind of eager loadingoptions. Each one is slightly differently implemented.So just a quick note on when we might want to use each loading strategy.So for select and load.That's going to run a second query with the where ID in.That's best for one to many or many to many relationships.That first query is going to stay a little bit more compact.There's also a joined load, which uses a left outer join to pull the related rows inthe same query. Might be better for 1 to 1 relationships or kindof smaller one to many sets where that row duplication maybe doesn't matter so much.And then the default lazy loading, that's fine when the relationship isn't necessarily accessed duringserialization. So the response doesn't return that related fields,you don't necessarily have to worry about that N+1.So kind of quick rule of thumb, if that response touches a relationship,eager load it. You don't necessarily want to wait for traffic to be impacted or having towait for that lazy loading. Do note that when we create a create task in CRUDit's going to have toaccept a project ID separately, because that route handler that's going toget it from the URL path, it's not going to be from the request body necessarily.And lastly, update task is going to reuse some of that payload model dumpexclude unset true logic, and that's going to make sure that only fields that the client has explicitlyset are going to become part of that update. So fields that aren't provided are going to need to stay
