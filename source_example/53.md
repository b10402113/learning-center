> > Nina Zakharenko: We've done the work of generating tokens.Now we need to talk about protecting our endpoints.So generating that token, half the work, the other half is verifying that token on the authenticatedrequests and also getting that right user out of the database so that we have accessto that information. We're going to use Fast API's OAuth 2 password bearer anda small dependency function plus use a current user dep as an alias to letus add just one extra parameter on that route handler.Now OAuth password bearer, that's a Fast API utility,tells the framework, hey, expect a token in this format in the header.Few side effects, Fast API is going to pull the token out of the header on those protected routes.So if there's no token, the request is going to get rejected with a 401.Which is nice. And lastly, even nicer if you've been enjoying the docs UI,that interactive docs page supportscreating an authorized button with no extra work from you,and it's going to prompt for that user's email and password,and then post that to the token URL and it's going to store that token for subsequentrequests. It's going to be fairly easy to test our API from that interface as an authenticated userwhich, if you are familiarwith writing complex curl statements, it's not always easy.All right, let's take a look at security.py and make sure that we save it because we've now implementedthe job secret key in our settings.So we're going to do a new import here and say from Fast APIsecurity, let's import the OAuth 2 passwordbearer. And then we're going to say that our security schemeis OAuth 2 password bearer.And we're also going to pass in a token URL here,and that's going to be /auth/token.So that's going to be the endpoint that the docs UI posts its credentialsto. We're going to build that endpoint a little bit later when we talk about the auth router,but one thing to keep in mind iswhen we verify a token, we need to do three things.We need to decode it with that key, the signing key.We need to pull the sub, the subject claim out,and look up that user in the database. So any of those steps failing,that's just a 401 unauthorized error.And we could use that annotated and depends featureof Fast API to create this method.And I'm going to just start with the method signature here and talk about itWe're going to need atoken, which is going to depend on that security scheme that we set up here earlier.We're going to need a session, and it's going to return a user model,which we haven't quite set up yet.We're going to get our settings. We're going to makesure that our potential exceptionthat's being thrown here is 401 unauthorized.And let's kind of talk a little bit through about what we're going to do.So let's make sure that we have all of our imports set up.We're adding a few new concepts here. So we're going to say from typing import annotated,we have our jwts, and we are going to need to use a little bit of Fast API logic hereas well. So we're going to import that depends,our HTTP exception and status as well.And we're also going to need to import an exception from JWT,the JWT framework that we're using here.So that's going to be imported from jwt.exceptions,and the specific exception that we want to catch is the invalid tokenerror. And lastly, we're alsogoing to need our settings.We're going to need our session as well.And we're going to need our models.All right, so coming back to this, what we want to do is wrap this ina try-except block, and we're going to say our payload is going tobe jwt.decode, and we're going to pass in the token,we're going to pass in our secret, andwe're going to pass in the algorithm.So if this fails, we want tocatch these specific exceptions. We want to except on that JWT invalid token errorbut we also want tocatch value error, meaning something about this didn't pass validation,types or anything like that.So we're going to name this exception, and we're going to raise a credentialsexception here that we defined above.So that's kind of going to bubble this more detailed customexception here up. Now forthe happy path, we want to grab a user ID,which could be a string or none, from the payload.If that user ID is still none, we'regoing to raise a credentials exception.Something went wrong. Something was not properly providedor set. Otherwise, we do have a user ID,so we're just going to say that the user IDis, we want it in the integer form, so we're going to go ahead and pass that user ID stringand grab that as an integer.So if we've gotten to this point in the function,things are kind of going as expected. So now it's a safe place to try to get that user fromthe session and say session.get user, which we're going to introduce in a sec,and use the user ID.Now at this point, if the user is none, or the user is not activewe're going to raise acredentials exception again, just one more check, otherwise all good,we're going to return the user. So just a few things to mention here.We're using that same credentials exception here that we created at every failurepoint to keep things consistent.If we return successfully, that means, hey, the token was valid,the user exists, the account is active. Great.This exception is not particularly specific, which is kind of a good thing.Maybe if somebody is trying to mess with your database,you don't want to give them the exact reason why their credentials were not able to be validated.So kind of up to you what path you want to take here,but the headers that we return are kind of a part of a larger OAuth 2 spec,and any client that's kind of using that is going to use that to decide,hey, do we want to prompt again for a login or not?And this here is going to preserve that underlying exception in thechain, so you can go back and kind of internally look back at it later.When you're working in production, a traceback like that might be useful.So you're not just swallowing this error and not showing it again.You're going to have that full stack and decide, hey, was the token malformed,expired, you know, maybe the subject was formatted poorly.The client doesn't have to know about that though. That's server side stuff.All right, now in our dependencies, we're going to make a newdependency and we're going to import a user here.I'll talk about the user model in just a moment, and we're also going to import our get currentuser here as well. And we're going to make a new current userdep that's kind of that same shape as our annotated and depends,and we're going to say we're going to annotate the user with getcurrent user. So that way we can just use get current user dep when we want a user andknow that if this worked successfully, thatauthentication passed. And lastly, any handler that adds thatcurrent user dep is going to be a handler that requires that authenticationAnd again, we're talking aboutauthentication, not authorization. So, for example, on projects,if we look at the router post, we can say that the payloadis project create, the session is session dep, but then we might also want to say,hey, we're expecting a current user here, and we're going to use a current user depfor that, and make sure that we import it.So at this point, if we're creating a project, we know that current user gave us a validuser back, and if it didn't, that Fast API would have taken care of that with a 401 responsebefore the handler even started. So we're going to put that same current user dep in every routethat should require auth for this particular project,that's project create, update, delete, and task create,update and delete, and our reads are going to stay public.As part of the exercise, you're going to be trying this authorization flow in the docs UII'm going to, you mightmiss it, but there's going to be a little authorized button on the right hand side,and you're going to use that to create a user and try out authenticatedrequests once you've implemented the REST of the logic.So just an FYI, the button's a little small.And just to know, with Ruff, you can have Ruff configured to doa little bit extra security linting. It's not bulletproof,but adding, it's called the S rule groupcan catch things like weak hashes, hard coded passwords,and kind of a few other things that are classes of common errors and issues in Python code that couldmake it more insecure. So that is something that you might want to set up for yourself.And just very quickly, because we've already spent a lot of time building modelsthat's not the focus ofwhat we're doing here. I just want to go through our user models here that you're going to have availableOur user has an emailby using email string hereimported from Pydantic, we geta little bit of extra validation for making sure that the field is actually an emailwithout having to write our own regular expressions.Hooray. I haven't met anyone who likes writing regular expressions yet,but if they're out there, let me know.Yeah, so we have our user base here. We have an email and is active for creation,we need an email and a password table here is true,so we're going to have a users table with an ID and a hashed password,and then when we're getting a user back, we just want an integer.We also need to have access token defined as a model,so that access token is going to be a string, and the token type here is a new type hintthat you haven't seen yet. Literal means it has to be bearer, and it can't be anything else.
