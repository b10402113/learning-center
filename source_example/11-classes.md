> > Nina Zakharenko: --- Python has two kinds of shapes for grouping data and behavior together.If you're using typed data, there's something called the data class that's also not super new,but kind of fairly new. You might not see it all over the place yet.If data classes do a lot for you,they're really nice. You don't have to kind of manually define a lot of boilerplate.If you need a class that has a lot of logic,you need full control, then write a full class.So if we wanted touse data classes, we would say from data classes,import data class. Rough, Idon't want you to clean up my imports that aggressively just yet.Let me turn off one of the Ruff settings for now for me,and that is gonna be this source fix all Ruff explicitI'm gonna turn that offfor now. If that's annoying you too, this is where you would turn that off.So to define a data class, you would just say data class,the name of the class.I'm gonna say name and tell it what type to expect.We're gonna have a slug and that's also going to be a string, and then we're going to have archivedand that's going to be a booleanwith a default value of false. So now I can just say project andspecify name. And that givesme that data class. I can assignthat to a value. I can print it out,and I can also easily accessthose underlying values. If your class mostly just stores data andyou want to validate it in a particular shape, this is a really easy way to do that.One of the niceties is that when I print payments,it prints it in this nice way. If I have a regular class and I don't implement these like underlyingmethods, print—sorry, **repr**. It's not goingto know how to print out that class, and it's gonna print out something kind of yuckyAnother thing that you getfor free with a data class is checking for equality.So I have this payments variable.Let's say I have payments2 and I don't change anything.I can check is payment equal to payments2, andthat'll tell me true. Not using data class, you have to build in this logic yourself.So the data class decorator, it's gonna read that class body.It sees the three typed attributes. It fills in**init**, which is the creation method for theclass, implemented equals, implemented repr.Yes. Is there a way to make a collection immutable orlike freeze? Yeah, we'll talk about that in the next section.Yeah. For data classes, you canalso define methods.Self here means like theinstance basically of this, so it's a special keyword.Here, we're going to say that this method returns none, and we just set,when we call this, we're just going to set self.archived equals true.So if I copy this in andI have my payments here, I print payments,we can see archived as false, but then now I should be able to call payments.archiveNotice I'm not passing anythingin when I call it, and if I print my payments again,we'll see it's now archived, so that's kind of how data classes work.I'm not going to show you, I'm not going to demonstrate regular classesbut I do want towalk through them on the course website, because it's something that you'll probably see.So regular classes are defined like this, no data classdecorator here, and no type hints necessary.They're not kind of like that integral part of a data class, but you always need to define this**init**. You might also hear this called dunder in Python,which means it's kind of like a hidden or a secret method thatis not necessarily exposed to a user.The way that you call project here is just by saying that the class name open parentheses,you know, pass in what this init expects.But here, unlike in the data class, we have to set all these fields manually.If we wanted to print out nicely, let me show that to you very quickly.So let's say we had our project here, andI try to print this out.I just get that, that's not very nice, and that's because that class does not implement**repr**. So that's kind of the use case there.Those attributes assigned in init, they belong to that instance.So if you need class attributes that are shared,you just define them under the class definition.So that default separator is set for the same value for every single instance ofthat instantiated class, but self.name is going to be different for each one.So you also want to be mindful here not to use mutable class attributes like thoseempty lists as a default.We already talked about how those can be problematic.You don't want every instance to share the same,every instance of this project class to share the same instance of that list and stomp on it and overwriteit. Mutable state should be defined in **init** instead.And classes can also inherit from each other.You would put the class that you're inheriting from in parentheses between the class name and the delimiterhere. Inheritance is way out of scope for this course.
