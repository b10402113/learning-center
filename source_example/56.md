> > Nina Zakharenko: Lastly, I want to talk about how we might want to dockerize aFastAPI app. So let's take a look at a Dockerfile that we might set up for ourapplication here. And this Docker image is going to freeze the appwhich Python interpreter, all thedependencies, and create a single artifact from that that's going to run the same way on a dev laptopor in CI or production. And I'm just going to show you a very small example ofone that is built around Python 3.14-slim, which is an officialimage. So here we're going touse that, make sure that we're using that slim package unless we don't need to because the full Python3.14 image is a lot bigger, can have issues at runtime.We are using UV's official image here forpulling in UV and UVX binaries and drop them into the runtime,and we're going to pin that UV version.So sometimes, you know, new versions do weird stuff,so we'll know that we're always using this version and we would need to manually set it.A few flags here that help with a faster startup.We're telling UV go ahead and compile to bytecode when you install it,that way there's no hanging at the first request.It's going to already be precompiled.There are some issues with Python buffering that could come up and this just tells Pythondon't buffer to standard outor standard error. And lastly, here's our environment path where our virtual environment is going tobe. UV creates that automatically, so we don't need to do that.This is our app directory that we're going to use,and here you could just copy everything in this project if you're very confident there's some issueswith that. You might accidentally copy an .env file.So here I'm just being really, really explicit about what I want to bring over.Lastly, I'm going to run uv sync and pass in that --no-dev flag tosay we are not interested in bringing in those dev dependencies,expose this on port 8000, andthat's going to tell what port the container is going to listen on,Compose, Kubernetes, Docker -P use it, but it's not actually opening thatport, so. And lastly, this is the commands that we're going to use to run our FastAPI serverSo instead of dev, we'regoing to do run because we're not interested in that kind of interactive,you know, dev only live reload experience.If you wanted to specify more workers, you can do that in the commands by doingsomething like passing in the workers flag and having 2 or 4 or whatever to kind of match themachine that you're running on.If you want more configurability here, you're going to have to insteaduse the underlying web server that FastAPI uses,which you might see in logs or other places.It's called Uvicorn. We're also going to wanta Docker ignore file as an added layeron top of, as anadded layer of protection on top of making sure that like caches don't end up in our containeror anything like that, and that would look kind of likethis, so here we're sayingignore all this stuff, you know, our virtual environment because we want to build that on the containerGit any compiled Python filesany caches, coverage, and our.env here. So a little bit of an extra layer of protection.This is the most important line.We don't want to have our virtual environment copied.It's not going to be built for the same platform. It's going to cause a lot of issues.So we've kind of had our compose.yamlfile that we've been looking at quite a bit.That has just taken care of bringing up the database.We're going to want a new compose file for the app.And it's going to look a little bit like this.Compose.app.yaml. So we're going to specify our services.We have a web service. The environment needs a database URL.Here we're setting debug to true, just kind of for testing purposesYou know, this is runningon our machine. Might not want to do that depending on your environment,what ports are being exposed, and it also depends on the database connection being healthy,and I'm going to show you how you can verify that in your app.And this is the command to bring it up,make sure Alembic is up to date and then run FastAPI.And again, specifying our Docker DB image here,and specifically the volumes as well.So this is kind of a way for us to layerour containers, so that the web and the database serviceare bundled at the same time and kind of can talk to each other and understand each other's dependenciesSo you're going to havean exercise where you run the full stack andwe're not going to cover how to pass in secrets outside of the .env file.So just kind of for the test example, you're going to create anenvironment variable for that JWT secret and just pass it right into Docker.
