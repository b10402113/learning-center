> > Nina Zakharenko: So we saw variables, those give a name to a value,and if we have something like this, wecan set like a priority.If you're coming from a typed language like Java, you might be like, why are you not telling the compilerwhat type this thing is?And that's because Python is a dynamically typed language.So the type of variables inferred type hints helpa little bit with this. It used to be kind of a very common source of bugs,but the type hints are just hints.They're not necessarily checked at runtime unless you use specific tools,and what that means is thatI can reassign this to, youknow, I can say like, I can modify this,and now that's 13, but I can also do something like this.And that is totally valid Python.So that's kind of a big gotcha.One of the really useful things in Python is that you will seeyou can convert one type to another.Sometimes that happens implicitly, but you can also set it explicitly.If I wanted to kind of explicitly call the int constructor,which you should never do this. This is just an example.I can go ahead and do that. You can see that I didn't need to import int or anything atall. That's because it is, it's already in the namespace.It's a built-in value. So you might see like floats.You could do that for bools, so bool of zero is false,bool of 1 is true.And you'll also see this later on with some of those collection types,so you could do a list, a dict, a set,all of those are built-in keywords.Python also has a kind of standard set of comparisonsSo double equals is, doesthis thing equal the other thing on the other side.So we can use that for comparisonExclamation point and equal isnot equal to. So if we do that, that should return true.Less than, greater than, less than or equals,greater than or equals. That's kind of the syntax that you'll see.And you know that these comparisons return booleans.So that's kind of the standard shape of the simple types that you'll see so far.One thing that we'll be doing a fair amount of is using tools from the standard library.So we could use modules to add functionality to our code,and for those we explicitly have to import them.So there are a few standardtools in the standard library that you'll use fairly often.DateTime for dates and times, pathlib for file paths, logging forstructured log messages, re for regular expressions, enum for enumscollections for advanced collection typesand operators, and one of thecollections in this advanced collection type that I use frequently is a default dict.So that lets you create a dictionary and specify ifthere's no value in that dictionary, whatfunction do I call toinstantiate that. So a default dict where you passed in list as the constructor meansthat every value for that dictionary is guaranteed to be a list.So if you're doing some sort of list operation, you don't have to do any sort of extra error checkingor say like, this key is empty, make a list.So lots of really good stuff in there.And to import from the standard library, we don't need to uv install DateTimehere or anything like that. It's just part of Python,and that's kind of the syntax there.So let me show you very quickly, if I do a from datetime import date.I could do a print, datetime today.And I can also do these imports right here in the REPL.So just a quick thing that I wanted to show you,this underscore yellow squiggly line in my VS Code,that's Ruff complaining. It's saying this block is unsorted,it's unformatted, your code doesn't look very nice.But when I saved it, it went ahead and formatted it for me.So if I do a print date dot today.Oops. We'll see what date it is.Import syntax, you might see stuff like import datetime that importseverything in this package. You generally very much don't want to do that.It pollutes your namespace and can cause other issues.So try to be as specific as possible.Is there a Cree equivalent for Python?Yes, type hints are definitely part of a modern Python workflow these days.There was a lot of resistance to adding type hint and type hint tools in the Pythoncommunity for a long time, mostly because, you know, when you have an old code base andyou know, maybe there's sometechnical debt and things just kind of work,it's a hard sell to introduce type hinting, because you have to add it incrementally.There's a lot of errors. It could be a fairly long process.Modern Python, almost all modern Python uses type hints.It just solves whole classes of problems and subtle bugs.So depending on what kind of code base you're working on, you might see it,you might not. If you are working on a Python codebase without type hints and you slowly start addingthem, you might be somebody's hero.So yeah. So yeah, like I was saying,just be specific about your imports.
