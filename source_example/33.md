> > Nina Zakharenko: If you're interested in not necessarily using the docs UI,I do have some reference for how to call the API with curl inthe course documentation, but not necessarily something that's important.You can use curl, you can use the docs browser, whatever way you want to interact with your API indevelopment works. So let's start our final project wherewe're going to refactor our database logic to crud.pySo, we're going to havea new crud.py. It's going to hold those database operations,and then you're going to take a look at how calling into crud.pyfrom our existing main.py file is going to look.And you'll see the difference. Main.py is going to handle the HTTP routing,parameter injection. Crud.py is going to handle those database operations.So we are going to check out thatfresh branch for chapter 6, and we're gonna do that with git checkout -b.You're gonna name yours my-chapter-6.Don't forget that no-track flag, so that your stuff doesn't get overwritten if you accidentallytry to pull, and we're creating that new branch from origin,chapter 6. Let me goahead and delete that branchand run that command again.Cool. So, we need to make sure that we run uv sync,so that our dependencies are up to date with the dependencies in the exercise andwe're gonna do a little bit of refactoring here.We've gone ahead and moved from main.py that slugify method,and now we want to go ahead and implement our databaselogic in this new crud module so that we can then call it from main.pyand a few guidelines here, that these crud functions they're going to call session.add,session.commit, session.refresh. The routes are not going to touch those transaction boundarieson the success path. Route's own HTTP concerns,so status codes, HTTP exception, but we want to make sure that we don't actually change any API behaviorwhile we are doing this refactor.So to list our projects, we need tocreate a new statement. And we're gonna select our project,order by project.name. And ifyour refactoring involved just copying and pasting stuff from main.py,that totally works too. No need to duplicate typing.So, we're gonna return a listwhere we use our session to execute ourstatement and get all of the matching projectsFor get project, all weneed to do is return session.getproject and we're going to pass in that project ID.It's up to the route to handle the 404 if there's no matching project,and for create project, we are going to make sure that weare validating. So we're gonna say project.model_validateand we are going to pass in our payload,and as well as updatingthat slug. So we're gonna say update the slug with slugifyand we need to pass in the name in the payload.So now that we have a project, we can add it to the session.We can commit it, we can refresh it andwe can return it. We should get rid of this red squiggly line that was complainingabout a missing return type.And let me do delete and I'm gonna do a little bit of copying and pasting aswell so that you don't need to see me type out all the code that already existed in main.So, here we're going to do a session.delete on project,and here a session.commit. So as a reminder,to update our project, we needed to get theupdated fields and explicitly exclude unset variablesAnd then we're gonna checkif the name is in the updated fields,and it's not none. Let's go ahead and slugify that field, add it to add our new project to,or sorry, add our updated project to the session, commit, refresh it,return it. And same thing with create project.So here in create project.Did I miss one? Great.No, it looks like it looks like I already got rate project, get,create, update, delete. Cool. So now we have this crud module that is goingto make our route handlers look a lot cleaner and easier to use.So now that we have implemented that crud, all the crud functions,we're gonna update our main.py here.And we're just gonna say crud.list_projects, crud.get_projectsWe're gonna keep that HTTPexception here. And same for POST crud.create_project, crud.get_projectand this makes our codea lot cleaner. Our route is focused on HTTP concerns.Our crud is focused on interfacing with the database layer.We are going to continue refactoring this later on, so this isn't quite the final evolution ofwhat our project is going to look like.Now there were two tests that were added as well that you might want to take a look at.One is conftest, which tests our configuration andcreates a pytest fixture for our session that uses an in-memorySQLite database for our tests so that we're not running our tests against Postgres.There are pros and cons to this.But it is faster. It's very easy to create,tear down a SQLite database, and it'sreally great for tests. It might miss some edge cases where SQLite and Postgresdon't behave in similar ways. So it's kind of the one thing that you need to be mindful of.So let's go ahead and do the run pytest.And all of our tests passed, meaning that our crud refactor was successful and did notchange any behavior in our app.
