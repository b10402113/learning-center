> > Nina Zakharenko: So in order to take this class and turn it into something that SQLModel can use to communicate with thedatabase, what we need to do is inherit from SQLModel here andjust set this flag, table equals true.Isn't that nice? That's all you kind of need to add to say that,hey, SQLModel, this isn't just like a class for data validation,even though it kind of looks like Pydantic, you should also use this to translate this to a table inthe database. So we've already gone ahead and installed SQLModel,and I'm gonna add a new file in release trackerand we're going to callthat models.py. So we're going to touch source release trackermodels.py. And again, all of this code will be in the exercise,so don't worry about it too much.So first we're going to import a few things.I'm gonna do import from datetime, UTC and datetime.We're going to use that annotated flag we saw earlier, string constraint,and then column and datetime from SQLAlchemy, and fieldand SQLModel from SQLModel. Notice here that we're using SQLModel field and notPydantic field. So as wesaw earlier, we want to use annotated to make sure that we have a reusable namethat we can use forvalidation that might be used in multiple places,and that kind of, I'm just gonna copy this over cause it looked just like that,that looked like project name.And we're gonna create aproject base for SQLModel. And what that does isno, my imports.I thought I turned that off. I'm sorry y'all, too aggressive with the rough type checkingWe're gonna have a namewhich is a project namewhich is a field, andunique equals true means that's a database constraint and a description as well.And I'm gonna have a new class that defines the actual tableSo to do that, we'regoing to say class project, and we're going to inherit from project base,kind of in that same way that we used model base for Pydantic.And say table is equal to true. And let's think about what values for our project we're gonna want tostore in our database. So first we can specify a table name here,if we wanted to. I think SQLModel will do likea nice default, but it's nice to be explicit,and our ID is going to be an int or none, and again,we're just gonna copy what we did earlier.Default is none, primary key is true.So that's the ID for our project. And we're gonna have a slug,and it's going to be a string that's a field, and we want to make sure that our slug isunique, so that we don't have two projects with one slug,because if we have a route where we access that slug by URL that could cause a conflict.And lastly, we want to have a created at fieldfor our database so that we know when this particular rowin the database was created.So we're gonna use a datetime here.And for the field, we're gonna do something a little interesting.We're gonna set, we're gonna create a default factory.And I'm going to create a new method called utc_now,and I'll show you what that does in a sec. And then I'm going to specify herethis is going to be a column of type column,and we want to make sure that we are using a datetime,and we're going to set that timezone to true, and we're going to say nullable equals false.Now, let's go ahead and define that utc_now function.And all that's gonna do is return to datetimeAnd it's going to returndatetime.now, but in UTC. Sowe are inheriting from this project base.There might be, we don'talways need to include table equals true because this project base that's gonna haveall of kind of the common attributes that represent a project.So we might not always need to take an IDor we might not always need to take a slug and breaking down our definition this way gives us alittle bit more flexibility. We also saw this annotatedagain for our string and string constraints.And if we see where this is used, it's for our name.And that means that every time a project name is saved to the database,we're going to strip the whitespace, we're gonna confirm that it has a minimum length of 2.And we're also using this flag unique equals trueto specify that a name and a slug for each project needs to be uniquein the database. We're gonna talk about creatingread and write schemas as well, but for now,let me just show you what those might look like.So for our project create, we'rejust gonna inherit from our project base.That gives us kind of everything we want, and that's becausewhen we create a project, we're not the ones setting the primary key or the ID.The database gives us that back. We're not the ones manually setting the slug.That's created with a method that reads the name after it's been strippedof whitespace, etc. Etc. So our create just inherits from project base for an updateWe're going to inherit fromSQLModel, and we're going to say we need a project name when we updateor it could be none.And we need a description or it can be none.We're going to set those to none as a default.And lastly, for reading a project, we're also going to inherit fromproject base here. And we're gonna need an IDand a slug, because again, for when we're reading those fields are already populated at the databaselevel and also that created at we're going to get that for free with our new factory.So this is kind of a way to think about what data needs to be accessedat, for every type of action that we're taking a create,update, delete. You might have also heard of this as CRUD.And if we wanted to very quickly testand verify that our new models work, wecould do that with uv run Python -c,and that's just going to say run this next line of code in the virtual environment.We're going to say from release tracker import models.And then print models.project. So we have our class there.
