> > Nina Zakharenko: So just a quick note on conditionals.Those are done with the if-else and syntaxso we can do thatAny other else-ifs, I'll justas many of them as you have, I'll get in line after this.No, my keyboard shortcuts are not set up in this instance of VS Code that I'm using towork along with you. Soeverything other than if is optional, but if you're using if-else if-else,remember white space is important. So like, if I had this else herenot tied with anything thatwould be or kind of indentedfurther, I would get an error saying there's no matching if blockYou can do if-else, alsoif optional, as many else-ifs as you wanted.So that's kind of the syntax there.And really we're just using that, you'll see that all over the code baseYou'll use that for likechecking conditions, and as you build up methods, depending on what arguments you get,you might go for a different path.So if we had a method, and let memake sure that I have a DateTime imported, sowe can have a method that's called like is_overdue,and we have a due date and a status.I want to say, if the due date is none,then no, it's not overdue.Otherwise, if the status is equal to done,we're going to return false.Otherwise we're going to return is due date less than datetoday. And notice here these two statements, they're not kindof associated with each other. They each run independently,and we can try this out in our REPL.And we can say, let's say january_1st is equal to,sorry, we're going to do a date here.And we're going to say is_overdue, and we're going to pass in that january_1st date,and the status is going to be not done.Oh yeah, totally overdue. Andquick note about booleans and truthiness.Values in Python can be truthy or falsy,meaning they're kind of adjacent to true or false.They evaluate to true or false, but that's not necessarily always explicit.So for false, false is false.None is none type, but under the hood,Python treats that as false. And we can verify that by using thatbuilt-in boolean type and passing a none.And now we can see, oh, hey, OK, so none by itself is false.We can check 0 is false.Empty string evaluates to false.And so do empty lists, dictionariesetc. So kind of avery important note because of these conditional statements,you might be like, you know, here we're checking is none,but we can also have a shortcut that says like, let's say we have,do we still have our january_1st variable here?So we have this variable.We don't necessarily need to check here explicitly if due date is none.We can just say because of that truthiness, if due date,and that does the same thing.So this is kind of the more the cleaner format,you'll see this all over the place.So none, zero, empty string, empty list all evaluate tofalse, but any number greater than 0 evaluatesto true, any string evaluatesto true, any list with something in it that's also going to be true.So that's kind of how Python looks at truthiness and falsiness under the hood,kind of a fun fact aboutPython is that booleans are ints under the hood,and so you can do like really weird stuff.You can say that, and what do people think this isgoing to evaluate to? Yeah.Don't do this. Your coworkers will hate you, butyeah, fun fact. And yeah, you also want to be mindful about the distinction between none,empty string, false. Sometimes that can change how data is evaluated.So sometimes you want to be explicit, sometimes it's not that important.
