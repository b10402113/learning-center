> > Nina Zakharenko: So we're going to set up our database.And in this exercise, you're going to make sure that you have Alembic configured,you're going to generate that project's table, and you're going to populate it with some seed data.So just a reminder, checking out this exercise branch is going towipe any changes locally that you have made and make sure that you have kind of a working copy ofthe project before this exercise.So make sure that you have either stashed any uncommitted changes or justcommitted them with a commit message so that you know where to go back and look later.And then run this command or run these commands to set up the starting date for thisexercise. You're going to check out your my chapter 5 branch.You're going to run uv sync to make sure that all of the newdependencies and Pipproject.tml are mirrored in your virtual environment,and you're going to run Docker Compose up to bring up your database.And there are a few new files that have gotten added or modified here.We've discussed them over the course of this section.So I did provide a list here so that you kind of are able to takea look when you have a chance.We were working on the exercise for this chapter to create a database andkind of manually seed it with some data.And I think I have some cache filesthat might make this not work, but I'm going to try to walk you through it as best as possibleI know somebody in thelive course had an issue where they had Postgres running on their Mac withinstalled with homebrew. And so there is a port that was conflicting there.So if you have any issues, this has worked for folks who havetried it in class, but that was kind of one issue that came up.So if you already have Postgres running or use a dev container with Postgres or something like that,you might need to do a little bit of a cleanup on your port mapping before this works.So I've gone ahead andopened up, picked up this chapter 5 branch,and let me just close this a little bit. So we have a fresh, I'm going to do uv syncto make sure that I have synced all those packages.And then you should have a compose.yaml here,and I'm going to bring up that container by saying, Docker Compose up-d. And yeah, I'm having an issue with the portalready being allocated, so let medo a little bit of troubleshooting here and see if I can figure outwhat is connected here. Let me grab the name for this containerand see if I can just manually stop itand make sure nothing is on that port anymore.I'm just going to give it a second to spin down.And this should also give you some guidance on how to troubleshootif you yourself already have something running on that port.So let's try that again. Bring this container up.And we're going to use Alembic to generate and run the initial migration.I'm going to say uv run Alembic, revision and use that --auto-generateand that -m flag just like a git commit to give it a label.And when you're working with other people, try to be a little bit more descriptive than initial,they might appreciate that. Let me make sure I deleted that volume and tried Docker Composeup again and see if that works.Yay, Docker. So what I did there, just if folks are havingthe same issue, I doubt you will. I've been running this quite a few times on my own machine,is I passed the -v flag to Docker Compose down to make sure that that volume was deletedwhen, and that data was removed because I probably had some stale stuff in thereGreat. So now I canrun, I can take a look at the versions here, and I have this migration.It's auto-generated in production, you do generally want to scroll through this and just take a quicklook and think about like, is this doing the right thing?Is my downgrade command, you know, doing the right thing.Here it's the initial revision. The downgrade command just drops the whole project table.So, you know, be careful not to downgrade if you don't mean it.You might end up in a state you didn't want to end up in.But now we can do uv run Alembic and upgrade head,and that's going to create that table in our database.And that also means that we can run DockerCompose exec DB with our username release tracker,or, database release tracker, and then we can run \dtto take a look and see that our projects table was in fact created.And we're going to quit.So we had a seed script where wehad to create some projects. I only have one at the moment,but let me go ahead and add another one in.So let's say we have a Frontend and we have an API.Projects name equals, let's say payments API.And our slug is goingto be payments API. We'll talk about how to hook that in to be auto-generatedlater, and we're also going to have a dashboard.So dashboard is going to be a project, we're going to say name is internal dashboard.And we're going to say the slug is internal dashboard.And lastly, we want to make sure that we add those two to the session.Otherwise, we're only going to be adding thatone row in, which is not what we expect.And then we commit the whole transaction.So we can then run that with uv run,Python, and we're going to do a script seed.py and that loaded up some sample data.And instead of connecting directly to PostgreSQL likewe did before, I'm going to pass the -c flag and then just a little bit of SQL here thatsays select the id name slug from all the projects and project that runs that one SQL command againstour Docker Postgres and returns all of the rows that we put in by our seedscript. What's the dot doing in the from .models import for the projectread? I'm just curious what that syntax is and why it doesn't work without it.I'll cover that more a little bit later, but it's basically another format for importingso defining a kind ofa relative import from that models package.
