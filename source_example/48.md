> > Nina Zakharenko: You're going to build a task API as well as some tests.So you're going to start with this exercise branch and then implement someconditional filter logic and CRUD operations for tasks, and then you're going to write two endpointsthat are going to use those filters.So I provided some working tests for you, and there are some to-dos that are going to guide you onyour way. Great.So in this exercise, your task was to build up the taskAPI and add some database tests.So you wanted to implement that conditional filter logic for yourself and create operations for tasksand then write two endpointtests that use those filters to kind of verify that they worked.So let's go ahead and run through this.You should have gotten that fresh branch and applied any pending migrations,if you did have any, and we already implemented that filter logicAnd let's take a lookfor that. We're going to go to list tasks and just take another quick peek at this tokind of see how we build up a statement when our SQL is a little bitmore complicated. And especially here we have this overdueonly flag that we're using that doesn't map one to one with any particular databasetable. To determine if a task is overdue,we have to build a slightly more complex statement where we're checking to see,hey, the due date can't be none.Is the due date in the past versustoday? And lastly, let's double check and make sure that task status isn't done.So this kind of lets us have a little bit more maybe business logicin here on top of just having that one to one relationship betweenwhat is in our database, what our Python models look like,and kind of start layering on top.So now that we have implemented those two reminders thatstatement.where, where we are reassigning the statement each time,that lets us make it so the statement grows and grows and grows,but only when the particular conditional in that if statement is met.And that overdue syntax that we use is specificto SQLAlchemy and not to Python.So it might look a little bit strange, butthat's kind of what that syntax looks like.So let's go ahead and take a look at our tests, test for tasks,and we wanted to implement two new tests so that we can verify this behavior.For the first one, we wanted to filter our tasksby status, and so we're going to use that sample project ID that we already have.We're going to POST two tasks here given that sample project ID so that we knowthat our project has two tasks in it.And then to test that we're going to say that our response is going to be client.get ontasks, sorry, the tasks route here, and for our parameters wewant to set the status as done.And then we want to assert that our response.status_code was equalto 200, meaning all good.And we want to check what the shape of our data looks like as well.So we're going to get the titles for tasks.We're going to say for task in response.json,which is what we're getting and what we are kind of looping through,we're going to get the task title here.So kind of a quick hack for list comprehensions.I find it convenient to kind of write them in reverse.So I'll write the second part first to just kind of get a handle on what I'm looping over andwhat I'm going to call it, and then figure out, do I want to transform that data in any wayDo I want to add conditionals?So maybe that's just a personal quirk, but it's a way of working that I find a little bit easierSo now we want toassert that our titles are equalto done task, and make sure that you use the sameexact same string as we have up here.And lastly, to test that our tasks are going to filter byproject slug as well, we're going to do a little bit of setup here and POST the projects withother project and a description and then add some tasks here as well.And so what we want to be checking for here is we want to kind of do the same thingas we did earlier and say that the response is the client.post on,or sorry, the response is the client.get on that tasksendpoint that we have, and we're going to add in a few params as well.So here we're going to say that the project slug is goingto be. Let's take alook at that response. Assert that we have a status code of 200.And take a look at those titles and make sure that it's the second task that passesSo kind of that samething here for task in response.json is what we are loopingover, and what we want is the task title.And we're going to assign that to a variable here as well.And we're going to assert that our titles are equal to the second task.And just be mindful of that as well to make sure that your spelling matches.So let's try this out and make sure that we have all our tests passinghere. Now we can also try thisout in that docs Swagger UI interface.Don't run this again if you have already run it, but I'm going to try to seed my databasewith a few tasks so that we have something to look at here.And in the past we've done uv run Python scripts/seed.py.I just wanted to introduce one other format that you might see,which is the -m flag, which says Python, run this as a module,and then instead of providing the file name or the path to the file,we're going to provide the module name, which in this case is cris.seed.All right. Let me take a look hereat the docs page. Solet's try it out. Let's get some tasks and see what we have in the database here.So I'm not going to set any of these, and I'm going to go ahead and click on execute,and looks like we have a few tasks here.We need to do the dashboard and stabilize the V2 endpoints and backfill some data.Cool. So now I can use this docs interface to do a little bit of additionalfiltering fairly easily. So let's say that I wanted to check on a status of inprogress. I can go ahead and do that here and click execute again.And just make sure that this only returns tasks with the status of in progress.Same with that overdue only flag.I can set that and see quickly which tasks are overdue,and you can kind of experiment with this and set multiple different query parameters here and justverify that things work how you expect them to.So if your pytest is green and your docs UI returns tasks that are filtered in the way that youexpect, you've gone ahead and done this exercise.
