> > Nina Zakharenko: Let me go ahead and cover that flavor of validation because you are going to see itlater on in the code.And there are other typesof fields, secret fields, that one is anespecially useful one because it treats the value of that field as a secret,make sure that it doesn't end up in any logs or stack traces.So that's another very useful example.So let's talk about string constraints.String constraints is a type of constraint that you import from Pydantic.And if we did want to strip white space before validation,that is just a flag that you pass in.And then you can say a minimum length of 2 and a maximum length of 120Now the way that youwould want to use this is with something from typing called Annotated.And with using Annotated, we create a kind of a new type hint for something thatwe're probably going to be using all the time.And here we're going to call it StrippedName.And we're going to use this Annotated type hintAnd the format for thisis, you add two square brackets, and the first thing that we pass in here is theexpected type hint like we had before, you know, here we just had a string.And then the class, the constraint that we want to use everytime this StrippedName is used.So I'm going to just copy this so you can see the difference, comment that out.So every time we annotate with StrippedName or use StrippedName as a typehint, we're validating that it's a string and using this kind of additionalenforcement. So here, we wouldno longer need to use this field because that's going to be enforced by string constraints,and we just need to define the type hint as StrippedNameSo that's kind of makingour own little custom annotation, because this is something that's probably kind of come up a fairamount, and you don't always want to be typingall of this, so it's kind of a way to make that concept reusable.And by using string constraints, Pydantic is going to strip that value first,and a string that's all spaces is going tobecome an empty string, which is then going to fail validation.Yeah, so just a few kind of things to cover between these two approaches.Constraints like length, strip, pattern, case, use string constraintsor equivalents for number orlist. Any custom logic that doesn't necessarily use or mapto a built-in constraint, you can use Pydantic's field validator function that we'regoing to look at later.And when your validator isdoing, you know, kind of dot strip and rechecking the length,using that built-in constraint just makes it nice and tidy.You don't have to worry about, did I do that or not in every single method.And you can also, since you've declared this in a variable, reuse it.So if you had like, you know, class TaskCreate herethat also had a name.You can also make sure that that is validated with that same StrippedName without having toretype this whole thing. So it makes it reusable.We're going to be using Pydantic quite a bit later on in the course.It is a core component of not just FastAPI but also the ORM and databaselayer that FastAPI works best with andit really kind of feels like magic because so much of this type of validation that used to be sucha chore just happens automatically, and you really don't even have to think about it.
