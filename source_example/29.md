> > Nina Zakharenko: Once we're ready to wire up Project Read into the API,we're going to go ahead and replace our mock database.And instead of using pedantic based model, we're going tojust import-based model and that inline project read class.So before we move on to the exercise,I'm going to quickly cover what Alembic is, but you will be running Alembicin your own environment with the exercise to make sure that your models matchmy models. So to set up Alembic, you'll need to add it as a dependencyand this specifies which binarywe want. In this case, it's the PostgreSQL server, and we're going to initialize Alembic in our projectAnd we do need toadd another dependency that we're using here called pedantic settings because it's goingto read some values from our environment.We don't need to do that just yet, because I'm providing in our config a default databaseURL that should match your default database URL given the Postgres config file that we're usingAnd there's a few thingsthat Alembic needs to know, needs to know how we're going to connect to the database and how to findour SQL model definitions. And so Alembic will create a new INI file,and you want to use some boilerplate to tell Alembic aboutthe shape of our project.Diving into this is not necessarily useful, but this is kind of allof the features and flags that you could set.We do need to configure env.py.That's the Python script that's going to run every time Alembic executes a command,and we need to make sure that it knows where our database URL is set in our config,that it imports our SQLModel metadata, and we need to make sure that thecontents of env.py import SQL model, which is not done by defaultLastly, I have provided afile in the exercise called script.py.mako, and that's a template file that Alembic usesSo that way you don'thave to remember to type import SQL model every time.So generally, how migrations work, migrations work by creating revisionstelling you how to movethe database forward and move the database back, generally just forward.Rolling the database back tends to be a fairly complicated operation,especially if you have data that's already populated, but this lets you have kind of a snapshot of yourdatabase at each version that you can easily review and modifyyour database schema so that it matches your Python models without having to do a lot of workSo, I might have generatedmy schema and created my projects table and thought,oh wait, I want to add an updated time, and I don't have that.So you would run a second, create a second revision and then run that revisionto upgrade your database so that it matches your Python models.And we're going to do that. This command Alembic revision auto generate will inspect your databaseschema, see what changed and generate that migration revision for you.Running Alembic upgrade head is going to take any pending migration scripts and actually apply
