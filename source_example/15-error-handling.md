> > Nina Zakharenko: Exceptions are raised when there's something that happens that interrupts a normal program flowAnd sometimes you want tobe the one raising an exception. Maybe the code can't complete safely.Maybe there's a validation error. That's when you would throw an exception.It's useful to catch exceptions when they add context.Let's take a look at this piece of code.We're doing a little bit of validation here, andwe're raising a value error if we try to pass in a blank project.So if I call validate project name here on payments API,all good. If I try to pass in an empty string,this value error with the name that I specified right here is called.If you want to catch an exception, you do that with a try block.So I can say try valid project name equals,and let's do that again.We're going to validate our empty project name, and to catch thatexception, we can go to the next line,do that one and say except, and then pass in the specificname of the exception, andhere like we might want to set the valid project name todefault. So if we look at valid project name nowwe'll see that it's setto default. We tried doing this, we caught this specific exception,and this code ran. If no exception came up,this would not get called.So just to show you that quickly, ifwe look at valid project name here, we'll see that is in fact a Project APISo just a general noteyou want to catch exceptionsas specifically as possible so that you can do the right thingwhen that specific exception comes up.You never want to just accept and notspecify anything because you'll end up with some weird issues.There's also, I forget exactly what the class name is, but a class of exception where if you catch ityou even catch exceptions fromPython catching like control C if you're trying to exit out a script.So yeah, being specific is useful.You can create custom exceptions.You just need to subclass the exception class which is available in Python,and you might want to define a custom exception when something generic like value errordoesn't cut it. You can also raiseexceptions when you catch them, if you want to modify the shape of them or rely on some other exceptionhandling code down the line.You can additionally catch multiple exceptions by just saying trythe piece of code that's running, except value error, except your second exception,etc. Etc. So that's a way to do kind of specific things when specific exceptions
> > Track current time
