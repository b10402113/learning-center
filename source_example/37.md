> > Nina Zakharenko: Now, putting all of our endpoints into main.py, that might work fine for kind of a small projectOur application is probably goingto grow. We're going to have dozens or possibly hundreds of endpoints and main.py is going to becomefairly hard to navigate. So we want to break the structure up with a featurea fast API called APIrouter, and that helps us split our app up into different routes or paths,smaller focused modules, and thenmerge or kind of refer back to them in our main.py so thatwe have more of a top level understanding of whatour API contains. So we're going to make a new package here.And we're going to call it routersAnd we're going to create2 new files in this.First we're going to do **init**.py.And then we're going to create a new file in routers called projects.pyAnd I'm going to startjust by copying over ourprojects route here into our newprojects file. And of coursethere's a few things thatwe need to import here.And we're going to be using the dot dot for the local import within ourproject here, because it's now 2 directory levels up.So we want to make sure that we have Project read.We need to import our session up.And then of course from fast API.So we're going to be swapping this out. We're not going to be using an app anymore.Instead, we're going to define a new router, and we're going to say that the router is equal to APIrouter, which is going to be something that we import from fast API.And there's a few arguments we can provide here.We're going to provide a prefix.And that's the route, path that is needed for this API router.And then optionally, we can also provide some tags.And these tags are used in that docs view togroup similarly tagged objects together, so, kind of helps for display.And then instead of saying app.get, we'regoing to say router.get.I'll move over one more endpoint from here.Actually, let's move them all over.So we're going to need to import our session dep,our project up, and fix any other import errors that we might have.So, no more app, we're going to switch over to using our router in everyinstance here. And lastly, make sure thatall of our imports are present there.Missed that one. Project create project update.And I'm going to keep this as the unrefactored version for now.So once we want touse this router in main.py, it's a fairly straightforward fix.And just a quick note about the differences.We create an API router instead of app.We use router.get instead of app.get, and then adding in this prefix lets the routerknow what path it's created on, so we don't have to repeat it in every single pathhere. So that's the next step is going to be to clean up all of these routes.So, we don't need this projects prefix anymore,so every time we see that we can go ahead and remove that.Now, in main.py now, we're going to want to import from routersSo, we're going to sayfrom routers, import projects. And we have our fast APIinstance here, and now we'regoing to want to include the new router.And there is a method on an app called include router,and we're going to say projects.router.So, those project endpoints now live in that separate module and main.pyjust includes them. But we still have our app.exception handler that's notspecific to that one route, and we're still also serving a root here.There's a nice tutorial on fast API called Bigger Applications.So as your app grows, that's something that you might want to check out.Larger apps might have nested routers or per router dependencies,and that tutorial walks through some of those scalability patterns.
