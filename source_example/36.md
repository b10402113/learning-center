> > Nina Zakharenko: We talked a little bit about exception handling here.So here we're raising a 404 if we're trying to get or delete a,or update a project that doesn't exist.But we talked about how if we currently try to post a project with a duplicate name,we're just going to get that 500 server error.Right now our API has no idea how to handle that.So I'm going to add a new test to our test APIhere. And what we want to do istest that creating a duplicate projectis going to fail. And we're gonna takethat test client and a sample project ID.And that's gonna be an int.And it's gonna look something like that.We want a response. We're gonna hit a post to projects and this isthat same name as sample project ID and what we wantto do is we want to assert that the response.status_code is equalto 409. And we want toassert that our response.json isgoing to be equal to, we're going to give it a detail.And it's gonna say, data conflictoccurred. So, right now, ifwe try to run this test, and let me take a quick look at main.py here andmake sure I haven't broken anything.We'll do a little bit more refactoring here later.And if I run pytest, Ishould, sorry, I'm in the wrong directory.So making sure that I amin the repo root. Let me run pytest.And I get an expected error here.So I was looking fora nice description here and a 409 response.I didn't get that. Instead, the database barfed and,hey, SQLAlchemy, integrity error. I have a unique constraint here.What are you trying to do? And so that's not very user friendly.And 409 is specifically the status code that says,hey, this request can't be completed because it conflicts with something else in the database,so we don't necessarily want to do a try andaccept block in every single router just to catch a new class of error,because there might be several errors that your application might want to handle.And we don't want to duplicate the code.We don't want to have that import everywhere.And thankfully there's a feature in FastAPI that lets us handle these types of errors fairlynicely. And that is a decorator called @app.exception_handlerWhat it does is it'sgonna register a function that runs whenever a specific kind of exception bubbles up,and it could be in a route, a dependency somewhere else,and the routes are gonna kind of stay focused on that happy path,and we're going to make sure that our crud.py continues to stay free of any HTTPknowledge. And that also means less error codes getting passed back between crud.py and main.pySo we want to makesure that we are handling the exact error hereand we actually get thatright here in this error message.So we can copy this and move that up to our importAnd say from sqlalchemy.exc importIntegrityError. And then we can go ahead and use that.So we've initialized our app and the syntax for this exception handler isapp.exception_handler, and then we pass in the IntegrityError,which is that exception that we just imported.And this is a decorator, so we still need to define the function,what's going to happen when this is triggered, and we're going to say a handle_integrity_error here.And what we take as a requestand the exception. Now hereis where we would return that nice JSON response.And we haven't done that yet. So let me show you the syntax,and we're gonna have to import that fromfastapi.responses. We're going to importthat JSONResponse. And what we're going to be returning here is status_codeAnd we're gonna use thatnice status package that gives us these named status codes,conflict, and we're going to say that the content is gonna be,we're gonna have a detail here.And we're gonna copy theexact message here that we used in our test.It looks like there's one more thing that we need to import, which is the Request fromFastAPI as well. So nowevery time our app spits up an integrity error, we should get this response and let us run our newlyadded test to verify that.Hey, it's all green, tests pass.So just to demonstrate, there's no session.rollback here in that handler.And that's because that session lives inside that get_session with blockand that block is going to automatically exit when that route raises,so that's going to roll back that uncommitted transaction.A few exception handling patterns as your app grows is we want to use thisapp.exception_handler for shared exception types.You know, for example, integrity error, one handler means just one place to update the response logicWe're not putting, hey, dataconflict with project occurred here becausewe're calling the project endpoint. We already know which model that's associated with.That could change if your database models are very complex or interwoven,but kind of for now this handles the case, and we want to keep those route local errorsin that HTTP exception space.And if you're trying to import HTTP exception in your crud.pythat's kind of the wrongpath because we want that data layer to focus specifically on model specific database specificexceptions, and then have that web layer translate that to an HTTP response.And lastly, we want to, if you wanted to doany sort of logging here in that exception handler, just note thatyour route handler is already exited, and so you only have access to what's inthese two variables. What was in the request andanything additional in the exception here.
