> > Nina Zakharenko: In section 3, we're going to talk about types, Pydantic,and intro to testing. So you're going to read and write some more Python type hintsincluding a little overview ofgenerics and the union operator and when you might want to use that.You're going to run mypy to catch type errors before they hit runtime,because as we saw earlier, I can run typed code even if it's wrong,because there's nothing at Python runtime that's validating that.And we're going to use a very popular Python package called Pydantic and create models inthat that do validate data at runtime without us having to runmypy. And then we're going to create our first kind of small little unit test,and we're going to run that with another package called pytestSo if we have afunction with type hints, we kind of talked about how that's a lot easierto understand. So if we had that slug function that takes a nameit's a string, we knowthat it returns a string, and we're going to return,you know, we're going to strip it, we're going to lowercase it,and we're going to replace any spaces with dashes.So reading the signature alone, you know, slug is not necessarily a great name.What does slug do when I'm going back to look at my code?Well, knowing that it takes the name that's a string and returns a string already gives me a little bitmore information than a Python function might have otherwise.As we add parameters, each one of those parameters gets a type.So if we have a function to add two numbers,A and B, and we return A plus B,the way that we would add annotations to that, A is an int,B is an int, and we are returning an int as well.One interesting bit if we have a function that,for example, takes the name and we want to print out a greetingfor that name, and I'll use an f-string for thatWhen we call greet, itdoesn't return anything, even though here it kind of looks like it does.That's because this print statement did something and outputted something,but greet doesn't actually return anything.And so to signify that with a type check, we would set the return type as none.We can also annotate variables when we declare them,and we kind of saw that syntax a little bit in our data classes.So we can say project name is a stringright here when we declare it.This is useful when you are defining a data type,like a collection that you might use later, just to give whoever's reading your code alittle bit more context. So let's say we wanted to count,and this dictionary is going to have a set of keys that are stringsand a set of values that are integers.This doesn't really tell us much and wecould type annotate that by saying that our counts dictionary is going to be a dict.And again, that's a built-in type, we don't need to import that.And we add some brackets to specify what the types in this dictionary are going to beThis is particularly useful forall types of containers, the syntax lists, tuples,and you can kind of nest those in a way that's ascomplicated as the data structure you're representing.There are type checks can get fairly complicated,especially for production level code. There are some operators that you might see in the code base.One that I especially want to cover is the union operatorAnd that specifies that thetype could be one of these things, which is specifically usefulif you want to support having an empty string explicitly,because that empty string, remember, is kind of a falsy valueAnd you would do thatby having, let's see, usingthat union operator to say this name can be a string or it could also be none,and those are both valid values.So you can kind of pass as many, you can use as many type hints as you want aftera union operator, and that means that it could be one of any of the types that you provided.We already saw that our type hints don't validate.So I have that add function up here, and we cancall it like this. Like, notparticularly great. No checks going on at runtime.We already saw that we can use the plus operator on two strings.So type checkers like mypy canverify that and also, more importantly, provide hints in VS Code if thecode that you're writing doesn't adhere to the type hints.There are some other Python libraries used for typing that are up and comingthat probably very shortly are going to be recommended replacement for mypyOne is uv's type checkerlike somebody mentioned in thechat. The other one is from Meta. It's called Pyre.They're both, mypy is legacy code, it's fairly slow.Both of those new generations of type checkers run much, much faster.They're based on Rust. They're very configurable.They're made for very large, complicated code bases,but neither one is currently in version one.So depending on your organization's risk tolerance, you might want to use it anyway,but just wanted to make sure that you all knew how to use mypy as well.So to use mypy, it's not part of thestandard library, so it's something that you're going to have to install.And in the directory where you cloned that initialgit repo, you're going to want to run uv add --devDev means we want mypyas a development only dependency.So when you're running this code in production, having this flagged as a dev dependency means itdoesn't necessarily have to get bundled with your running app.Now that added the mypy to the virtual environment as well as any other packagesthat it relies on, which is quite a bit.Once it's installed, we can do uv run mypy and then specify a fileor a path. And here in our examplecode, mypy says, no issues.I found one source file.If we change one of our type hints here toreturn an int and we run mypy again,it's going to say, hey, I found one error in one file.It's going to tell me the line and the file as well.I can command click on this in VS Code if I'm running the terminal in VS Code,and it'll take me right to the issue.If I mouse over this, it'll say, hey, here's an incompatible return type.You told me the return type was going to be an int, but I got a string.What's going on? And that same incompatiblereturn type message is present here as well.I got a string. I expected an int.Mypy reads its configuration from pyproject.tomlwhich is one of thosefiles that got automatically created when we used uv to create aproject, and let's take a look at that.So here we can see the newly added dependency groups of dev with mypy that happened when we ranuv add --dev mypy, so we don't have to keep track of this manually.If you wanted to add some mypy config, you could do that herein a section like this tool.mypy strict equals true,which we don't want for this project right now because that means always show errorsfor typed code, and then here we're going to specify like what directorywe want included in mypy checks. So I'm not going to include this right now.I just kind of wanted to show you that as an example.We'll do some more configuration later on.When you use that strict flag, complete function annotations need to berequired. So if you're adding typing to an existing codebase,that's probably not something that you want to set.And it also checks for kind of somebad typing habits as well.So I'm not going to talk about any of like the individual flags to mypy or the individual settingsthat you can set. The mypy documentation lists every single one in thorough detail.Lastly, if you want to mark a package as typed,like you're publishing your own package on PyPI,you can create a file in the root of your repositorycalled py.typed. It's just an empty file, and all that does is flag to consumersof your code that, hey, expect this code base to be typed.Before we move on to validation with Pydantic,anybody have questions? How do we find all the decorator functions and keep track of the evaluatedresults of each decorator function within a large codebase?That's a great question. I would say generallyunless you have like abug you're trying to track down or weird unexpected behavior orsomething's working, something's not working with the decorator like you expected to,it's really the only time when you generally dig into the decorator code of any moduleor library that you're using.It's kind of an implicit contract.You just kind of assume that they're going to work how they're defined.And there's really not too much reason to dive into the code,unless you're the kind of person who reads all the code for every library that you add,or you're working on something that's highly secure. You might want to do a little bit more investigation.
