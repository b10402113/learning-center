> > Nina Zakharenko: So I want to give you all a little bit of an intro to pytest.Python does have testing built in in the standard library.It is challenging to work with.There's a lot of boilerplate that you have to take care of.This library called pytest has came up a few years ago.It's become, I think, the most widely used test framework for Python.It abstracts away a lot of the unpleasantness of usingPython's testing framework in the standard library, andit's really a delight. And so the first thing that we need to do is installpytest as a dev dependency, andwe're going to do that with uv add devand pass in pytest as the package name.Pytest automatically finds tests just by naming convention.So you can drop your tests into a directory at your project root,and any tests that are in this directory are going to get run automatically.So you do have to make sure that the test in this directory starts with test underscorethat file name, and yourtest functions get defined inside of that.So let's say that we had a Python file here calledslug. We would then have a filein our test directory called test_slug.pyAnd let's write kind ofa first test in this test_slug.pyLet's open our slug.py aswell and see what that would look like.So we're going to have this slugify function here that we've seen a few times.It takes a name that's a string, it returns a string,and we're going to return name.strip.lower.replace, and we're going to replace all thespaces with dashes there. And if we wanted to testthis function now, we would need to import itso that it's available in the test.And let's say we want a test.Let me close this up here. We wanted to test our slugify,and we wanted to say test slugify lowercasesand dashes spaces. And all we're going to do here isassert that slugify release trackeris going to return release-tracker all lowercase.So now, if we're in our kind of top level directory here,not in the test directory, we can say uv run pytestNo module named slug.Did I not save this?Let me see. Oh, right, it's not a module.I'm not going to explain modules at the moment,so let's just quickly, instead of importing it,we're going to use, we're just going to define slug at the top.Don't do this, but I want to show you an example of a successful pytestrun. So it's going to, we're going to get one dot for every test that ranand hopefully they're all greenThat means your test passLet's start a second testto make sure that slugify strips outer white space,and we're going to assert that slugifyis going to return payments-api, no weird spaces.So if we save that and run it now, we see two tests that pass.When something does fail, the output from pytest is quite nice.So let's say we change our slugify function to make a star instead and not the dashand we run our testsagain. It's going to tell us exactly what failed.It's an assertion error, what the expected result was,and what we got instead.So very nice syntax, runs great on the command line,and this diff is really like one of the main features for using pytestSo there are ways touse pytest to ensure that an exception is called again,or sorry, that an exception is raised as part of your code.We're not going to dive into the syntax too much. We'll see more of that throughout this course,but I do want to cover just two more features of pytest before we move on.One is fixtures. So if you have a lot of shared setup between tests like atest database connection or an HTTP client, they let you define that once and then reuse it.And then parameterize, which lets you run the same test against a list of differentinputs so you can make sure that different scenarios are tested.Now, we are going to add features when we wire up the database,and we're going to need a fresh session for every test.For now, just know that like assert, and then if you need a sneak peek of whatpytest raises look like, you can look in the course websiteAnd on extension, what exactlyis BaseModel doing as Field is doing the validation and constraint that letsus instantiate an instance of that class.
