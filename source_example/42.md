> > Nina Zakharenko: We have our projects, but our projects don't have any tasks, and we're gonna have to add that.And those tasks are gonna have some of their own state.Is the task planned? Is it in progress? Is it done?What the priority is, and, optional due date as well.And one way to do that is to use,a string enum for our status like we saw,much, much earlier, and that's gonna have a lot of benefitsfor us in terms of enforcement andhaving our code be less error prone.So let's go to our models.py.And while enums are in the standard library,we do need to import them.And we want to have two enums, one for a task status,we're gonna use string enum for that.And I'm just gonna be, explicit here, so it's a little bit easier to read.We're gonna have an in progress.We're gonna have a blocked.And we're gonna have a done.And then one more for the task priority as well.And we're gonna have a low, we're gonna have a medium,and a high. And an urgentwhich means maybe your bossis calling you after hours.So when SQL model sees that string enum annotated on a column,it's going to store that value as a string, and it also adds a database level constraint that isgoing to reject anything outside of the values that we explicitly listed.So we're gonna go back to kind of that same threeschema split that we used for project.Where we had project base, we had project,we had also project create, update, and project read because not all fields in the databasemight be coming through the API vice versa.That's a way for us to be very specific. So the shared fields are going to go in a taskbase and the table model is going to add the database only fields.And let's quickly take a look at what that looks like.I am going to copy our project name strength constraint here,and just make one for task name.Again, this is an opportunity for you to refactor later on.But we're gonna make a task base.We're going to base that on SQL model.And it's gonna have, you know, our title.Let's call that task title.And a few other fields such as details, status, priority,and due date. So for our task base,we're going to have title, details, status, and priority,and we'll see that those are calling back to that string enum that we created and also havingsome default values. We're also gonna have a due date for the task anda relationship back to project.Notice that our details are using that union typing operator,so that we can see, hey, the details could be a string,but they could also be none. And again, for the table,we're sticking with those table only fields in there,and that's going to be those IDs that are the primary keys,etc. Now, we'll see that projects.id here, that carries aforeign key constraint back to the project.So that means that one project can have many tasks,and using again index equals true on our project ID means that the database is going to buildan index on that column, which makes lookup a lot faster because we're probably going to be usingthis a fair amount. Now, we do need to import some new things here.We need to import relationship from SQL model and also make sure that we havedate imported here as well.That foreign key constraint that connects rows in the database.That is represented by the SQL model relationship that connects to Python objects inmemory. And when both sides are wired up correctly,task.project and project.tasks, because it's a one-to-many relationshipcan now be navigated froma loaded record in your Python code without having to write any joined queriesby hand, which is great.Nobody likes joining queries. So task declares itshalf of the relationship above, and this back populates tasksthing that tells SQL model that this is one side of therelationship, but we do need to define it on the other side as well.I'm gonna move these tasks down to the bottom of our models.pyAnd let's go ahead andchange our project as well.So for a project, we have an ID, we have a slug,but now we also need some tasks.And that's going to be a list of type tasks.And we're going to have a relationship here as well, and the back populates is going to be on projectYou might have noticed herehey, we moved task downto the bottom. Why does this type annotation work here?In Python 3.14, those annotations are evaluated lazily.So we get that forward reference here automatically, which is nice.I'm not going to talk about the schema variants for test,create, update, read yet. You'll see that code in the exercise in just a moment.When you have that in place, you're going to go ahead and create a newAlembic revision. And I'm not gonna run it yet.But it's gonna look something like that.We're going to run an Alembic revision. We want to auto generate it,so we're going to use that flag, and we're going to name it add the tasks table.And when it's run, Alembic is going to load env.py,which imports from releasetracker.models, and that import is going to bring in bothtask and project classes into that SQL model metadata.Then Alembic is going to connect to the database and say, hey,what does the current schema look like right now?Just projects, and then it's gonna diff it.So there's going to be a task table, task status, task priority,enum types, the foreign key, the index, that's gonna become a new Alembic version.And I do recommend kind of maybe not in class today,cause it takes a while. If you're generating this for production,just take a quick look at the auto generate script and see what's in there.Sometimes Alembic doesn't do the right thing, and you really want to double check and make sure thatnothing funky is happening, you know, that foreign keys are set up how you expect them to,and the downgrade and upgrade code kind of does roughly what you think it should.There are a few things that auto generate can sometimes get wrong if you rename a table or a columnthat could show up as a drop and create, which might not be your intent.Sometimes server default values aren't passed through.Sometimes constraint checks or custom indexes are not represented,and sometimes backfilling data changes also doesn't work,or is not present. Those aren't auto generated in general,you're gonna want to write that backfill yourself.Also kind of a gotcha with Alembic migrations specifically for Postgres enum typesPostgres will create like thattask status as an enum type, butAlembic doesn't, if you do need to roll back,Alembic doesn't always pick up those table types.Not very important, rolling back an Alembic migration doesn't happen very often practically because therisk of destructing data is so high.But I just wanted to point that out. So if you get into a funky state where something'snot working or migration ends up in a weird state, we're just going to blow away our Docker volume andstart again so that you always have good kind of fresh seed data in the database.And then you'd apply that migration and make sure that you're on that current migration version.So yeah, downgrades very, very rarely happen, you knowif you have a tonof users, a lot of traffic, there's probably already stuff in whatever new table came out.So you do want to be mindful of that,and it is useful to add those migrations to version control so that you have a history of the database that you can review if you need that.
