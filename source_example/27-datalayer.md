> > Nina Zakharenko: So yeah, our app has amnesia.Every time we restart the server, if we add in anything,all our data is just going to disappear.We don't even have endpoints for adding data, sonot quite even amnesia. It's kind of like GroundhogDay, you know, every day is the same. We have no database, so weneed to connect our FastAPI app to a real Postgres database,and we're going to cover how to spin up a local Postgres SQL database using Docker.We're going to talk about the ORM called SQLModel,which is created by the same author as the FastAPI package,so they work really well together.We'll define our data schemas and we'll use those as blueprints for both our databaseand our Pydantic validation. We're going to set up a tool called Alembic to track and apply databasemigrations to our schema over time because, you know,projects grow, and we're going to make a little script to populate our new database with a few samplerecords just so you have something to look at.So first things first, we need a database and we're going to use Postgres,which is kind of industry standard for production web apps.Python specifically works very well with Postgres, and you don't want to install Postgres onyour computer. It is a challenge.I think I did it once before, many, many years ago,and I remember spending a long time debugging it.So instead, we want to run it in a Docker container.And why Docker? Because we can use a native Postgres installation.We can pin it. So one version per machine can be challenging when you have multipleprojects that run multiple versions of Postgres.Sometimes that does happen in production.We can easily just wipe state between runs.If we have stuff in our database and development, we don't really care about, it's really easy to justblow up the container or just the data volume and clean that up.And containers can give each project a pinned version of Postgres anda named volume specifically for its data.Teardown is one command. It's going to wipe the container and the volume fairly easily,and you can also use Docker for Redis,Kafka, any other kind of backing services that you do not want to install directly onto your host machineSo we are going tomake a compose.yaml file, andwe're just going to put it in the root of our project directory.So I'm going to touch compose.yamlhere and open that up in VS CodeWe get a nice littlesymbol here to show us that VS Code has recognized this as a Docker file.So we're going to set up a few things. We're going to set up some services, and we're going toset one up called db.And don't worry about trying to type along with this.I'm going to provide this compose file for you before the next exercise,or you can go ahead and grab it from the course website.So I'm going to kind of cover the important parts, butwe want to make sure that we have a service and a volume,and we're going to have a volume called postgres_data.And there's kind of a few important things we want to capture here.So first is the image. Right now we're going to use postgres:17-alpineFor ports, we're going toneed to do some port forwarding so that the localhost app that we're running on our machines canactually talk to the database. So we're going to use port 5405432,both on the host machine and in the Docker container.And we're going to set up some environmentvariables here and I'm just going to copy and paste these in.The name of our Postgres db: release_tracker, Postgres user: release_tracker,Postgres password: very_secure_release_tracker. So not a container that you want to be publishingto anything but your own local machine.And we're also going to have a health check command here,which will let Postgres knowhow do we know that this container is running successfully?And for that we're going to use shell and it's going to run pg_isreadyand it's going to provide the username and the database as well.And we can do a little bit more config here and say, you know,the interval is 10 seconds, and the timeout is 5 seconds,and we want to do 5 retries just to be very sure.And lastly, volumes here. It's going to bepostgres_data, and we're going to do a/var/lib, which is just kind of the standard path for Postgres data.So let's check this out and see if it works.I might already have a named volume working on my machine, so let's try thisout and see what happens.So the way that we would run this is with Docker compose-d. And it looks like it went ahead and started this Python for pros container on my machine.So I have a nice check here.So previously we used Python dictionaries, westored our data in memory, not great.So let's use, figure out how to hook into this new Postgres databasefrom our app, and to do that, we're going to use SQLModel as an ORM which is similar to Hibernateif you've done Java or Entity Framework if you've done .NET,Active Record for Rails, or Sequelize for Node.js,etc. Etc. If you've used any one of those, the concepts are familiar.So just a quick note about SQLModel versus SQLAlchemy.If you've done database connection in Python before,you might have used SQLAlchemy 2.0.It's been around forever. It's the industry standard, undisputed,powerful, flexible. It is what SQLModel uses under the hood,and that means you don't have to do an extra step of wiring in Pydantic validation.You kind of get the best of both worlds there.So you get that Pydantic data validation built in, and you also get the power of SQLAlchemybecause that's what SQLModel usesunder the hood. So it really prevents a lot of code duplicationAnd I'll show you kindof how to do that in just a moment. Soif we were to—and this is not part of a repo.I'm not necessarily going to save this file,but I am just going to tell VS Code that this is a Python file so we get some nicesyntax highlighting here. So if I say from sqlmodel import—andlet me quickly add this as a dependency, just so the nice code complete works.So there I just ran uv add sqlmodel.If you didn't catch that, don't worry.That step will be repeated in the exercise.So let's say I had a class of Book,and I had an id for that book,and I had a title for that book, and an author, and pages.So I'll go back to id last, but for pages, it's going to be an int or a None,and for that field, we're going to set the default as being None.Maybe we don't know that value for all the books.Author is just a string. Our title is a string,but we also want our database to consider this an index because it's a frequentlysearched value. And lastly, our id is going to be an int or it couldalso be None, and the case when it might be None is when we have a book and it's notsaved yet, right? So there's no id set.So we need to make sure that we catch that case.And that would be a Field and the default is None,and we also want to set primary_key equals true here.And this is our primary key for this table.
