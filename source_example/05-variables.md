> > Nina Zakharenko: So we're going to learn about kind of the fundamentals that you need for readingand writing Python code. We're going to write a few small functions and some conditionals.We're going to learn about classes and objects.But like I said earlier, I'm not going to be spending too much time drilling in how do youexactly write this thing. Chances are you're working in production,you have code hints on, you have an LLM kind of suggesting the next line.So it's more about like, the goalis more about getting you familiar with the type of code that you're going to be seeing.So you all can kind of pull this upand type along with me if you'd like, but I'm mostly going to be working in VSCode here and we're going to be looking at kindof both how you run Python from files and the REPL as well.So I did want to show y'all something a little bitinteresting, if we do uv run Python here.In the REPL, if I try to type exit,which is a feature of modern Python REPLs, I'm going to see here,use exit, parentheses to exit.And I'm like, oh, that's not quite right. Took me a very,very long time of debugging and troubleshooting to figure out exactly what was going on.Turns out that the Python extension for VS Code does a lot offancy things, and it doesn't really support using this new style of terminal unless you change somesettings. Since I do kind of a lot of work in the REPLwhen I'm just kind of quickly typing stuff out, you also get things like syntax highlighting.So I'm going to set this up for kind of demo purposes today.You all don't need to do that unless you plan on spending a lot of time in the REPLThere are a lot ofkind of nice features that you get with that Python shell integration.I'm going to turn them off for now. So if you're like, why does my REPL look a little bitdifferent than yours, and let me just make sure thatwe can get that going.So let's see. I do the wrong Python.No, it doesn't work. I will need to debug and troubleshoot that,but just, it's not that important.It's just going to mean not that much syntax highlighting.Maybe let's try it one more time in a newterminal window. Oh, there it goes. OK, so some things were just stale,but you'll see that when I did uv run Python,I have a little bit of a coloring here and I can also just type exit,which is nice because that is like the number one thing that beginners used to get stuck on.So you have the repo cloned.We have a main.py here.You might be thinking why main.py.So this is kind of useful for like modules orany kind of production level code to be like, hey,like if we're running this and we actually mean to execute it, we're not just importing it,then go ahead and run the main method.But for demo purposes only, we saw that uvrun main.py worked. I can also just delete all this anddo a print in here, that'll work as well.It's not best practice, but while we're learning, it might be a little bit easier for you to do thatLet's do a don't showagain there as well. Sowe can make like a string here.We're going to call that payments API and let's make a little slug for itIf we just ran thisright now, we're not going to see anything.If we did this in the REPL, like I mentioned earlier,we would see that result.Sorry, if we did like a payments API just the string would show that to us.Even if I add this here, it's not going to do anything.So when you're running Python code, if you want to see the result of something, you need to use printSo let's say that Ihad this name and it was like had a bunch of white space in it.I could print out stripping that string.And let me try using a run selection and terminal,and see if that will work.It's another thing that VS Code is kind ofdone something funky with, but let's see if we can get that working.This is one of my favorite VS Code features, and I kind of wish that they weren't breaking stuff allthe time these days, but that's totally OK.Let's just run this manually.So if I print that in there, we'll see now that likethere's no white space in the beginning here.And there's a lot of stuff that kind of comes built in with strings.We can do a lowercase, we can do an uppercase.We can tab complete like if we did name dot upper here,it'll also show us like a little type hint in VS Code,sorry, not a type hint, a little hint in VS Code to show us exactly what that does.And we could do like a, you know, name.replace if we wanted to replacethose strings with like dashes, you would do something like that.Strings are kind of a fun thing in Python because,we'll talk about this a little bit later, but they're a type of collection you can loop over a stringand that will give you every character in it.And there's kind of some other fun stuff in Python.Like if I did a string, I could multiply that.But a few other things that are handy about print, you can just pass in like a fewthings to print that are comma separated.And that will show you kind of that statement separated by spaces.If you're just quickly debugging stuff, that's super useful.One thing about strings that you might see is somethingcalled an F string. So an F string is a special kind of string that extrapolatesvalues, and you can actually just put whole code in an F string interpolationthat will run. We're not going to dive into that too much,but I do want to show you an example of what an F string looks like.So if I say like name Nina, I can have an F string that says the name isAnd defining that with theF at the beginning, I put curly braces here and the nameof the variable, and that will interpolate it for you.I definitely don't recommend if you're like joining two strings in production,I don't recommend using plus to do that.There's some weird performance sketches with that.So use F strings wherever possible.Python also supports arithmetic and numbersso you might see likean integer, you know, 5.Floating point numbers as well.If we multiply an int and a float here, we'll get a float back.There is no double in Python like some other featureslike some other languages might have, so that's kind of something that you want to be mindful of.We can do standard arithmetic as well, so I showed multiplication,you know, addition, division. Those are all kind ofthe standard things that you might see.So let's say total tasks I candivide that by 2. There's also kind of two things that you might see is integer divisionand remainder as well or modulo.So booleans, they're either true or false.And that capitalization is important.So kind of different case than JavaScript that you want to be mindful of.And then lastly, none is a special keyword,means null or no value.And sometimes you might want to explicitly set that to a variable kind of as a flag and be likeif this variable is setdo this thing, if it'snot, do something else. So you want to make sure that you're using that like to indicate that a valueis missing on purpose, not necessarily when it's an empty string or zero,we're going to come back to the concept of truthiness a little bit later.
