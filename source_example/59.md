> > Nina Zakharenko: For our final exercise, we're going to implement a health endpoint and DockerSo, you're going to createa new GET health endpoint that checks if our database is connected successfully.We're going to build the Docker image. I've given you the Docker file because this is not a pop quizYou're going to run thefull stack. You're going to have Docker use your health endpoint to monitor the running container.Just one thing that I want to point out here as you are working on this exercisehere we are just goingto export our JWT secret key, you know,just to be like, hey, does this work?And just because it's going to be in our environment here, it's going to get passed into Docker ComposeThis is going to kindof show us some logging and we'll probably freeze up,you know, your terminal. So before you run this command,just figure out what your working directory is and open another terminal in thatworking directory, and that way you have two tabs or Windows, so you can kind of see both side byside. I personally I'm notgoing to be running this stuff locally on my machine because I have way too much running,but I'm going to walk quickly, kind of walk through the exercise with you so that you can make surethat you ran the right steps. So again,we're going to be starting here on a fresh chapter 11 branch.You're going to go ahead and run quality checks.These ones should all pass.These are going to report some errors in health.py specifically,if you haven't implemented yet because it's just a stub function signature,but no body yet. For the health endpointUsing this path, we wantedto select one from the database just as kind of a quick way to verifythat we're able to connect successfully.So, let's take a look at what that would look like.This is kind of one scenario where you might just want to go ahead and pass in some textto execute. We're going to return a status of healthy,and that is a command that we can then hook back into Docker so that Dockerknows, hey, how am I sure that this container is healthy.So, not necessary, but kind of a little bit of infrastructurenice to have.If anything happens trying to run this, we return a JSON response that says unhealthy status codeservice is unavailable. And then, you know, whoever is in charge of that container running successfullycan actually do something about it.So just a quick note here, we've been using session.exec.You might see session.execute here and wonder, you know, why,what's the difference? Very quickly, session.exec is SQLModel'snice wrapper that does nice things with our Python models,but its type stubs only accepta few different specific types, not raw SQL by text,which is probably for the best, you know, it's one way to kind of prevent folks from usingthat framework to do something that, you know, it doesn't necessarily shine at or want to doIt's also going to causesome mypy errors because ofhow the type hints are set up. So, session.execute here is the underlying SQLAlchemymethod. And one of its type stubs does accept a text clausespecifically, so this isn't going to cause any sort of mypy errors.If you just run a bare session.execute without specifying this is text,you're also going to get a type error back,because SQLAlchemy does require literal SQL to be wrapped in that text blockto make that intent extremely explicit and be like,hey, I meant to do this, right?So frequently you might not want to do this for this health check endpoint.This is just a very quick way.So you went, you went ahead, youbuilt the image, you confirmed that it exists, and then you went ahead and youran it. So, you built it, you confirmed it exists,and then you ran the stack with compose compose.yaml specifically to bring up both the web containerand a Postgres container. And we're just passing in JWT secret key from the host shell there to passit into the container. Yeah, not a great practice,but good enough for a quick demo.And your compose logs should show DB container starts,runs through health check, web container builds, or rebuilds if there's already a cache,and then web is going to run Alembic upgrade head against the new database,FastAPI run well with the server, okayTo verify that health endpointyou could just curl againstit and you should see a status of healthy and you can alsocheck the container health status by running this command.When you're done, Control+C to stop compose, and then you can shut down the stack this wayLastly, Postgres data, named volumeit's going to persist betweenruns. If you want to delete that, use that -v flag.Okay. So if you can curl to that endpoint, show status healthy,then you're done. And just kind of going through a few troubleshooting tips here.Did you remember to set the JWT secret key?You know, could be exported or not set, maybe can't reach the container.Does Alembic upgrade head fail?Well, maybe the web container started before the DB was ready.Could be some issues with Docker build, or if your health endpoint doesn't return successfully,there are a few things that you can check here to make sure that your health endpoint is doing the
