> > Nina Zakharenko: Now it's time for you to try out logging and migrations for yourself.So what you're going to do in this exercise is fill in the task schema variants.We're going to generate and apply an Olympic migration that's going to create that tasks table,and you're going to run the server and take a look and see that our log request middleware is actuallyemitting timing logs. So go ahead, saveyour changes, grab the new branch, make sure that Postgres is running in Docker,because if it's not, you're going to either have Alembic hang or end up with some weird errorsso that's what you needto do before you start, and then go ahead and start by filling in the task models.I'm going to go ahead and grab the same branch so that we can take a look athow to complete this exercise. And of course, I forgot to stash my changes here,and I'm going to switch to a new branch, my chapter eight.And do a uv sync just to make sure that I have all of the latest dependencies.And I'm going to confirm that my Postgres is running as well.So we're going to go ahead and fill in those task modelsSo we're going to startwith creating, let me makesure I have the freshest version here.We're going to take a look up here and see that our task status and our task priority is alreadydefined and we want to go ahead and fill in our taskbase here. So we knowthat a task has a title.And we're going to use that task title string constraint that we set up earlier.We know that a task has details.So we're going to say details can be string or none,but the default's going to be none. And the task has a status.And we've already defined all the valid values for that,and the default status for task is going to be planned.Same thing with priority, we already defined our task priority as an enum.And we're going to set a default priority of medium.And lastly, we have a due date.And that's going to be a date or none, with a default of none.Now, this class task that inherits from taskbase,that's the one that has that table equal true flag,and that defines some of the database only fields.So that is our ID, which is our primary key,that is our project ID, which specifies that foreign relationshipback to project because one project can have multiple tasks.So that's how that relationship is defined, one to many.And lastly, we had specified that relationship back populates back to tasksand that back populates is also now added to our project as wellto say back populate ourtasks as a list of taskson projects. So now you can call project.tasksif the model has been fetched from the database, and that should get you all the tasks for that projectin a really kind of simple, straightforward way.For our task update here, this is the update schema for patch requests.So for patch, client is only going to send the fields that it wants to change.The fields that are not included in that request body,those are going to stay untouched.So we're going to have some defaults here.So we're going to have our title, and that's going to be our task title or nonewith a default of noneSame for details and allof the other properties on task.We have our task priority that can also be none, default of none.And I did not spell that correctly, so let me make sure I go ahead and fix that.And we also are going to have a due date, which is a date or none,with a default of none.Now, lastly for task read, the read schema is going to add some of thoseserver generated fields that don't exist at creation time,but should appear in API responses.If you're creating a new project and you're providing that project name and the titleor the details or whatever it is, you're going to want a response that tells you how to accessthat newly created project, whether that be an ID or a slug or some other way that you can thencall get project if you needed to use that again and get that straight from the API.So what fields do we have on the task table model that taskbase doesn't? And we just saw them, there's two,there's the ID, which is an int, and there's also the projectID which specifies that relationship, which is also an int.And that's going to give us our updated models.py for tasks.Now this is going to be exciting because the state of my database might not match the state of yoursI've been working on these chaptersa few at a time. So I'm going to try to run Alembic,and we'll see what happens. It might crash out.If it does, I will revert my database, drop that volume,and just make sure that I'm starting from a fresh start.So let's see what happens here.I'm going to make sure that my models here are saved,so that Alembic does the right thing, and then I'm going to do a uv run on Alembic.And I'm going to say I want a revision.And I'm going to say auto, go ahead and auto generate that,and I'm going to give it a name with the dash m flag and say add the tasks table.So that should succeed. Looks like that finished successfully,which is great. So now we're going to go ahead and apply that migration.As we see here, the upgrade ran, and the tasks table was added,and we can verify that with uv run Alembic current.And that will tell us what revision Alembic is currently pointing to.Now I'm going to go ahead and run my developmentserver. And I'm just goingto run that on a different port.Let's try this one. AndI'm going to take a look in docs here. I'm going to get on projects,and my database is currently empty.I never ran the seed script, but let's go ahead and try it out.And as I execute this, we will see our new logging linein the logs. So we know that this is at loglevel info from this nicely formatted terminal output that we getby running by using uv run FastAPI dev.We can see it's info, we see the routes, thestatus, and what the endpoint was and how long it took to completeNow, I did add adebug to my environment, but I'm not seeing it here.So that means that my environment is not currently up to date.And let me go ahead and double check that and see if we can get some debug lines showing upas well. So I created a new.environment file. Unfortunately, my environment file, whenwe pulled down this exercise, it's set in .gitignore because you don'twant your environment file committed, which does make it a little bit challenging with these exercisesSo I'm just going tomanually set it here. And we're going to say,let's see if that, I never rememberthe command. It's not set, I think it's export.Let's try that. Let me just go ahead and addit to this .env file and then we can source it.All right, there we go. Let's set our debug.Set onset, I don't remember it. It gets me every time I have to do this like,you know, a few times a week or a few times a month,and there's too much syntax to remember. So let's try running our server againand taking a look here and see if we get any of those debug messages now.So we haven't added any, but if we did,this is where we would see them.One thing I did want to point out here is that when we ranuv run Alembic current and it pointed us at a revisionfor the head revision for which migration we're currently on,does this remind anyone of any other tool?The implementation is not similar to Git, but the concepts translate.So this is a way of having some versioning and having some source controlfor your database and having a history of,you know, when did this column get added?Oh, this subtle bug started cropping up 10 months ago.Let's take a look through the revisions and see, is there anything that could possibly have gotten addedaround that time? As your app grows over years and years,this is just incredibly useful, incredibly convenient to not have to edityour database by hand. You don't have to necessarily be a DBA anymore in order togain benefit from these tools. And you can source control it,which for people that for a long time, just slap a query out there or especially in,let's say, starting to name names, Google BigQuery where you can just save them there.And it's like, oh, that's not with your repos.So how do I know what you did?We don't know, you can just go grab the definition.Yes, source code's the way. Yes, you can put it in your source code.You can see all the revisions if they're version controlled.And that's also kind of that, you know, anyone on the team can take a look and have access tothat. It's no longer some sort of mystery.
