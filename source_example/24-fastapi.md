> > Nina Zakharenko: So now that we have our first route,let's look at something a little bit more complicated.How do we handle more complex URLs?How do we use Pydantic to validate, and how do we build out some endpoints that will interact withprojects? And since we haven't introduced a database yet,we're just going to use a Python dictionary to simulate somedata. We're going to have a small Pydantic model that defines the shape of our project.So we're going to add kind of at the bottom here.I'm just going to add some mock data, andI'm going to use a Pydantic model called ProjectRead,which is not defined yet. So let me go ahead and import Pydantic.We're going to use that BaseModel again, and we're going tocreate a new ProjectRead class and inheritfrom BaseModel, and our ID is going to be an int,and our name is going to be a string, and our slug is also going to be a string.And we still have our app defined here.So let's make a new route that's actually going to look in this dictionarySo I'm going to commentthis out for now, and we're going to make a route for getting aproject, and it's going to take our project ID,which is an integer, and it's going to return our mock database,and we're going to get one of these values by the project ID.And now we need to hit this with a decorator, and we're going to say app.get,which is the HTTP method, and the route is going to be projects andproject ID. This needs to be a stringThis is kind of, eventhough we're not explicitly using f-strings here, we're familiar with this format,so we know that what we're implementing here is a route where we can call slash project slash 1or 2 or 3 and get a project by primary key.So as a reminder, we're using get here and not that square brackets intact,so that it returns none if we try to get an ID that doesn't exist.And we have our type checking set up here as well.So if somebody tries to get slash project slash,you know, slug, it's going to error out and say, hey,you're passing in a string, this route expects an int.If we do want to support a route that supports queries,the way to do that, let's say we wanted to list projectsand provide a name which is a string or none and provide a default value of none.This would let us filterour mock database query. So we can say projects equals list,mock database dot values, which again gets us all those values.And if name is none, let's just return all the projects.Otherwise, we're going to return a list comprehension, p for p in projects if the name isequal to the name that was passed in.And then lastly, we have to provide that decorator here,and that would be app.get, and that would be slash projectsSo here, name is aquery parameter. Lastly, before we werejust returning a list of Python dictionaries directly from our endpoints,FastAPI lets you return Python types thatwill explicitly be in the shape of the JSON thatwe defined. So for our get projects and project ID,we're now going to return mock database.get project ID,but we also need to tellFastAPI what the expected response model is, which is the step that I forgot justpreviously. So thank you for bearing with me.And the way that we do that is by setting a response model in this decorator here.We're going to use ProjectRead.So by setting that response model, that's when FastAPI is going todo filtering. So like I said, you know,if you have that secret field and your project model explicitly doesn't include it,it's going to strip that out of the response JSON.It's going to do some validation, and then lastly,it's also going to use this response model to generate that OpenAPI documentation.Let's take another look and make sure that this is saved,and we're going to look at our docs endpoint here.And now we do have that nicely generated documentation because we set the responsemodel, and we can even try this out here and say project ID oneAnd when we execute itwe're going to get thatresponse body as JSON. If I use an ID that is not present,we're just going to get an internal server error. We'll talk about how to do a little bit of errorhandling next. So this docs endpoint is just automaticallyavailable when you properly configure your endpoints.It's fully interactive. It's really nice.You can try stuff out. There's also another built-in endpoint called redocwith a little bit more of kind of like an API body spec sort of thing that will tell anybodyusing your API, what are some samples?What is expected? What are possible responses so far?So both of these are very useful. If you don't want this on a public facing endpoint,there are several ways to turn it off or configure it so that it's only available in development,not in production. But for development, super, super useful.
