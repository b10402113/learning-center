> > Nina Zakharenko: Functions in Python are defined with a def keyword followed by a name,open parentheses, list of parameters and then a colon.And Python uses indentation for formatting, not curly braceslike you might have seen in Java or JavaScript.Indentation is kind of what defines like the next indented block of code lives under theone that you specified, and method could also have an optional return statement.So, let's say we have this method, slugify,and normally we'd be using regular expressions for something like this or a library.I didn't want to torture y'all with regular expressions.I also did not want to answer any questions about regular expressions.So we're going to do this kind of a little bit manually with some of the tools that we haveseen before. So if I do a name dot strip, dot lower,and then I do, return cleaned and I'm gonna replacespaces with dashes here. I can then printslugify and I'm gonna do I'd say like payments API here.So if I do a uv run in .py here,we'll see that slugified value.So, in VS Code, hopefully, whatever editor you're using,after you type that colon, it will automatically indent for you.Methods don't have to return anything, so I can also do like,I can just print this in here.And if I run that slugify, it does the same thing.I highly recommend not doing this.You want to keep your methods small, specific,something like this would be incredibly hard to debug and troubleshoot.So having your method actuallyreturn, you know, a string or something else that can be modified.Maybe when you call this, you want the values to be uppercase.Whoever that caller is can then take care of that versus trying to,you know, figure out the print output.So something that you might see here is defaultparameters. So, let's say I wanted a different separator here.I can provide that, and here, I'm not calling it.So if I do, I forgot my print statement here,let's go ahead and add that.If I do that, we have the kind of the default dash hereand let me, sorry, let'sdo the separator as a dash here and then use that value.So here we'll see a dash. I can also now pass in a separator.So if I run that, now we're using this one, it's not the default,but I might want to, you know, have a different use case.You want to be mindful that defaults are evaluated once and they're done when the functionis defined. You don't want to use a mutable object like a list or a dictionary asa default value, or you will end up with a class of bugs that is extremely difficultto debug. So let me show you.Function, we're gonna say a tag, we're gonna say tags equals none.And if tags is none, then we're gonna do.We're gonna create that empty list.We're gonna say tags append name.And then we're gonna return those tags.So, this is kind of a syntax that you'll probablysee fairly often. This is kind of the correct syntax.And let me show you this real quick.In the Python REPL.I'm sorry, that was wrong quote type there.So each time, it's gonna create a new list of tags.And if I instead set this as a list here.And we're not doing this check.And I copy this into my REPL again.That's, it's just adding themto that list in the constructor, which is not what we want.So, very, very rarely would you want a method to do this.
