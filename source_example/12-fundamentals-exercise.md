> > Nina Zakharenko: Cool. So for our first exercise, you're just going to do some basicPython here, and we're going to use some enums,or sorry, enums, we're going to use some enums.We're going to write some logic here and we're goingto add this to the bottom of our main.py to make sure thatwe can call those sample inputs and see the results.So go ahead and add those and then run uv run main.py and there's solutions here if you need tolook at them. But know that if the driver prints those values and thatthe value in the comment here, then your code works as expected.I'm not going to go ahead and do this one with you because I think I've demoedalmost all of this during the course of theworkshop, but we went ahead and we defined a stringy numb.We wrote this is overdue method withwhere we checked is the due date none.As a reminder, we can also say if not due date,but that introduces that concept of truthiness.So you do want to be careful where you're doing that.If the task is done, we're going to say, no, this isn't overdue.Otherwise, we check if the due date is less than today's date,and we return that. Be mindful here.Talking about time zones is like so outside the scope of this course.Time zones exist. People don't all live in the same place.They're complicated. If you do something like this with due date and you're not taking timezones into account, you might end up with some funky math or somebody in an office in another countrysees the task is overdue. But it's not actually.So when you're working in production, you do want to make sure that you standardize on a time zone.A lot of companies just use UTC because that's easy.And then we used this like match case syntaxfor our enum. And I, thisis kind of a nice shortcut for not showing,you know, if this all set, if this all set, it's just a quick way to say,match on this variable, and if the case is the task status of plans,then, you know, say it's in progress.If it's in progress, the next stage is done.If it's done or blocked, then we can't really progress.So let's take a lookat this in VS Code.And run that and makesure that our values are false, false, true,in progress, which is the string for task status in progress,
