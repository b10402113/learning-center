> > Nina Zakharenko: What you're going to do now is you're going tobuild a registration endpoint with a starting point of kind of everything that we discussed,so you're going to implement registering forthe API. You're also going to write two tests, one that registers a user,and then one that registers a user andlogs in. You're going to generate a development JWT secret key and add it to your .env file,and then you're also going to generate and apply the migration that's goingto create that users table so that we can then save users.You're going to register a new user through that docs UI and confirm that everything works.So just three steps for auth.A user is going to register with that email and password.The server is going to hash the password, store the user.User is going to log in via POST to auth token.Server is going to verify their information and return a signed JWT.And then lastly, the client is going to send that JWT in the authorization header on additional requestsand we're going to havethose protected endpoints verify the token to identify the caller.In that login endpoint, we're going to have a few security helpers and we already have the user modelcurrent user dep, etc.So you're going to build the registration route and its tests.Before we dive in, I do want to kind of quickly talk about any of the errors or issuesthat might have come up as you were working on this exercise and what you can do about those.So server won't start if Pydantic is complaining that the JWT secret key is not set,that setting has no default and a minimum length of 32.So either your .env file isn't there or it's not being loaded correctly,or it has a key shorter than 32 characters,so that's one class of error.If /register returns a 500 instead of 201,make sure that crud.create_user was called with the right keyword arguments in the right order,email and password. And if POST to register returns a422, that request body has to be JSON,not form data. So just double check that.And then if your duplicate registration returned a 500 instead of 409the integrity error handler inmain.py should be in place and confirm that it's there and that it catches the integrity error.Lastly, this might be an issue for some of you as we've been playing with the database and running thatseed script. If your seed script fails with a unique violation or undefined tablethat means there's some sortof data conflict with the seed script.So just go ahead and reset to a clean database and rerun it.Again, docker-compose down -v will delete that volume.So blows away all the data in the database.Don't do this if you want to keep your data.Here, we're just kind of working locally and using that seed script to make sure that we have some gooddata to look at at every exercise, so no issue here.Take down that volume, restart with Docker Compose.Make sure that you run those Alembic migrations all the way to head,meaning all the way to the latest one, and then go ahead and run that seed script again,and that should give you some nice fresh data.Let's go ahead and get started.Let me just run the uv sync to make sure that my environment is up to date.Great. Now I've already generated a JWT secret key and I've set that herein my .env file. And Iconfirmed that .env is git ignored there.So now what we want to do is generate and apply the migration.So to do that, we're going to do a uv run alembicrevision, and we're going to say auto generate andwe're going to say add theusers table. Cool.So I've generated the migration and I've gone ahead and applied it,and if I did a uv run alembic current,we should see that we are currently on this migration.All right, so before I go ahead and implement the register endpoint,I want to take a look at the registration test first and alsotake a look at crud.py here at the create user methodSo again, create user, wetake a session. Everything after session needs to be a keyword argument.We get an email, a password, and a boolean of is_active, which is going to be true by defaultBut this gives us alittle bit of flexibility. We might want to have inactive users where we keep their informationin the database. Maybe something can possibly make them active again.Maybe they need to verify their email or something else.So this is a little bit of flexibility where we can have users that aren't active but are still inthe database. We're going to create a new user with email address,a hashed password. Again, we're going to use that get_password_hash for password,not password itself. And if that is_active flag was provided,we go ahead and set it. Add the user to our session,commit the session, refresh the session so that we get the user ID.And then we're going to go ahead and return the user.So let's take a look at test_auth here.And we want to test registration.So a few things to note here.We are going to again just be using thatroute to do our registration versus trying to create a user directly from thedatabase or calling it from crud just to make sure that it workskind of in that web layer.So what we want to do here first is register a new user via POST.And we kind of already know how to do that.That's going to be a client.post, and the route is going to be /auth/register.And we're going to pass in some JSON here andI'm going to just make this a little bit easier for y'all to read.So we're going to have a dictionary here, and we're going to have an email.I'm going to say new@example.com for that, and we're going to have a verysecure password here as well.And I'm just going to go ahead and keyboard cat it.This is not how you should create your very secure passwords, but here we go.So we have the user info here and then I'm just going to pass that in as our JSON.So now that we have a response, we want to do a few assertions here.So we want to assert that the response.status_code is equalto 201. And we want to make sure thatour body has a few different fields. So we're going to look at the body or grab the body fromresponse.json. And first things firstdoes the body have thecorrect email? So let's assert that the body email isequal to exactly what we put up here.It's going to be new@example.com.And then we also want to assert that our response body has is_activeset to true, and in this case we're going to use the is true assertion hereAnd lastly, we want toassert that hashed_password is notin the body. So I wantto run these tests. I have some to-dos here still.So instead of just making it look like the test is passing when there's nothing there,I'm going to assert 1 equals 2 here, which should blow up for us,and run my tests. Okayso I'm getting a notimplemented error here because I haven't created user registration yet,so that's good, right? My tests are kind of failing in an expected way.So let me go ahead and implement that and then rerun my test and make sure that it's coming upgreen. So now in auth.py, we want tobe able to create a new user account.So we know that we're going to get a payload.It's going to be models.UserCreate, and we know that we need a session.The return type is just kind of flagged as empty right now.So what we need to do is use crud.create_user.And we're going to pass in the session.We're going to grab the email from the payload.And we're going to get the password from the payload.Just a quick note here, as you saw when I was typing payload and I typed dot,it did have email and password in there in my IDE,and that's because our payload is specified to be models.UserCreateSo we get some ofthat nice functionality for free instead of having to go ahead and guess.So we're not worrying about duplicate emails here because we know that the integrityerror handler from main.py is going to catch that and return a 409 andthat's really it. So I'm going to go ahead andreturn this raised not implemented error and try to run my test again and see how it goes.All right, so I'm getting a validation here, you know,1 is not equal to 2, that's good, but I'm also having a different error here,so let me take a quick check. I actually figured out what went wrong,and this is a great call out to whyusing this return type of any is not necessarily a great example.So I missed doing something here when I called crud.create_user,which is that register needs to return the user.Oh, of course, and if a method doesn't return,it technically does return none.So that passed the type check and now my test should pass.So that's where typing is sometimes so unhelpful because it's like it's great if youcan be very specific and if you chuck in any at it,forget about it. That's true, yeah, so I type hint,you know, more specific type hint would have probably saved me a little bit of pain there.But so now we know that this test passes.I'm going to go ahead and copy this over because we also want to test registerthen log in. So we're going to go ahead andregister another user here. Andlet's make sure it has a different email address.Oh, maybe that's why mine's duplicated.That's a good point. You could be getting errors with your in-memorydatabase. That's what I'm thinking, and I'm not worried about it.Cool. So we've gone ahead and registered. Now we want toattempt to log in. We have our user info here, our fresh@example,and we have our secret password here.We don't necessarily care about the response here.We kind of already know from the test above that that goes ahead and passes.So we're just going to register that user.And then the response that we do care about is calling client.post,and we're going to use /auth/tokenas the path and for the data, I'm just going to reuse this dictionarythat I created up here.So a few things to assert. We want to assert that the response for the login,that status code is equal to 200, and we also want to assert thatthe access_token is in response.json.So let's go ahead and run these tests.Unprocessable entity. Let me see here if our data are shaped in a different way.And in fact it is.Just a quick note of what I missed here, when I create the user,it expects an email and password, when we are trying to login, we're not looking to use an email and password,we're using username at this route, sobecause we're kind of translating that email as the username that the user logs in with.So just very quickly, I will set that to login_user_infoand make sure that I'm using username here.And sorry, wrong spot.Make sure that I'm using the login_user_info here and see if that fixes my test.Cool. All good. So now we can say that we have passing testsWere you able to tryit out in that docs endpoint? Let's try it out.So we're going to run that development server with uvrun fastapi dev. I'm going to use a different port here because I have a lot of stuff runningon my machine, so I'm going to pick one that doesn't conflict.You probably don't need to provide that.So let's take a look at docs here.Let's make sure that I can still getwith no authorization. So here are all the projects,but now if I try to, let's say delete aproject, this should fail. Sonot authorized here. I got, as expected, errorcode 401 error unauthorized. I'm not authenticated, can't do that.Sorry, too bad. Let's try a POST to our auth registration endpoint hereand try it out. So I'm goingto go with me@example.com. And I'm going to setmy password to super_secure_pass. I know I've given you all a lot of examples for what youshouldn't do. This is one that you should do. Why not?And I'm going to try that out. Cool. There's me.I'm active, ID of one, very first user here.So if I go here and I authorize and I set my email as me@example.comand I paste in mysuper secret password, let's try that out.Looks like I'm authorized now. So let me go ahead and try to randomlydelete stuff and see what happens.So try it out. Project ID of one, it should be authorized now.Let me go ahead and execute, and I did get in there a data conflict,not entirely sure why. That's something for me to debug in the future,but let's try to makea project or POST to a project.And I think we have a project ID with two and try that outDid that work?Cool. Yeah, so I got a 201 that my new project was created.So it looks like our authorization flow is working correctly.I might have some weird stuff in my database that's causing those integrity errors,but I think we have a pretty good idea now ofhow authentication is working here.I'll just add this focused a lot on authentication.We've got a course from Kyle Cook on authorization that,it's in TypeScript, but the concepts are very applicable to any API.It migrates a role-based system to an attribute-based system and goes through kind of the pros and cons
