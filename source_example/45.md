> > Nina Zakharenko: All right, so starting with a fresh tasks.py,we're going to do a little bit of importing here,and I'm going to go kind of back and talk aboutthe CRUD methods that you would need, but I want to kind of talk a littlebit about the endpoints first, because that's a shape that we're already familiar with.So we implemented task.py, and now we're going to build outthat task router, and we're going to do a few imports here.We know that we're going to use Annotated and Any.I'm going to do some imports from FastAPI.I know that we're going to use APIRouter and response and statusWe're going to import CRUDfrom release_tracker, and somebody had asked about, you know,that dot model of kind of import syntax.That syntax relates to a relative kind of an importstructure. So when one of those modules was two directories up,we ended up using the dot dot format.I do want to introduce something a little different here.So we can also do from release_tracker, specify the whole module name,dot dependencies, import project_dep, session_dep, and we're going to do a task_dephere. So this is kind of the samething as saying from dot dependencies import project_depetc. Etc. I'm going tosay look to your team's style guide for which format is preferredSo if your team hasa style guide, this should be something that specifies,are relative or absolute imports preferred.Personally, I'm a bigger fan of this style because I think it makes refactoring easier and alsosearching for instances easier as well.So showing you kind of both styles here,but just know that they are equivalent.We're going to go ahead and import our CRUD, and then also from release_tracker.models,we're going to import our TaskCreateour TaskPriority, our TaskRead, TaskStatusas well as the TaskUpdateSomething to note here, youcan see my super long import statement.That's also another kind of flag or marker to think about.Could this use a refactoring? Because if I had release_tracker.models.task_modelsI could just import thatwhole module and get everything I needed in my namespace all at once.But because dot models has tasks and projects mixed together,kind of have to go and import them one at a time.We're going to go ahead and create a router for our tasks, and we're already familiar with that syntaxWe're going to say a router is an APIRouterand we're just going togive it some tags here.I'm not specifying the route here just yet,and you'll see why in a minute.That's because not all of the routes under tasks are going to live at slash tasks.But I'm going to tag them so that they're nicely organized in that Swagger UI.Let's kind of look at some of the simple routes first.We'll probably have a router.delete, and that path is going to be tasks,and then we're going to get a task ID.And for that we're going to have our statuscode of HTTP_204_NO_CONTENT, just being very specific about what responsewe're expecting. Delete_task is going to look very similar to delete_project.We get our task_dep, we get our session,and then our return type is going to be a Response.And not available yet, but let's pretend they are.We're going to talk about, we're going to use crud.delete_taskwhich is a method thatwe're going to build and pass in the session on the task,and I'm going to return a response again with that same statuscode. Our patch is going to be prettysimilar as well. We're going to do a router.patch, and we're going to use this samesyntax here, and most importantly, we need tospecify our response_model, and that's going to beour TaskRead. So update_task is going totake a task via our task dependency.It's going to take a payload, which is our TaskUpdate,and also a session. And it's going to returnfor our type hint, we're going to use Any. I'm going to talk about that a little bit later.And what we'd return is our crud.update_task, and it's going to take a session,a task, and our payload.Get for task looks very similar, so we'll do a router.getgoing to use that samepath there. We're going to set our response_model to TaskReadand we're just going todefine that to say get_task.We need a task dependency here, andwe're going to return that task.Now, the list tasks and the POST taskscreating a task is alittle bit more complicated. And for posting, whycan't if I wanted to do a @router.postand passed in this path, what information am I missing here?What project it's on. Exactly right.Yeah, we need to know what project to create that task under.So instead of this route, what we need to do is use projects asthe path, get that project ID, and thendo the POST with that information. Our response_model here is going tobe a TaskRead, and our status_code is going to be201 Created. This create_task needsto take a project. So we're going to use our project dependency there.We're going to have our payload of TaskCreate,and lastly, we also need our session.And here we're going to returncrud.create_task, take the session, the project ID, the payload.So our get for tasks is going to do a little bit of additionalfiltering because, you know, maybe we just want the overdue projects or the ones that don't haveoverdue tasks or the ones that don't have a due date.So we're going to do a little bit more filtering here than we did on project.And so for that, we're going to specify the router with a pathof get, and it's just going to be tasks,no ID or anything necessary. And our response_model here is actuallyyou would think it mightjust be TaskRead, but because this can return multiple values andwe explicitly want to support that, we're actually going to expect a list of TaskReadas the response_model.So a little bit more complexity here.What we're going to do is we need our session.We need a project_id, and that's going to be an int,or it could possibly be None, if we wanted to search,for example, across all projects.Same with the project_slug. So that could be a string orNone. Now the task_status, andsince we're just using this in one place, we're going to inline it.We can use that Annotated syntaxto provide a little bitof query overriding, so here I would use Annotated,I could say it could be a TaskStatus or it could be None.So that's kind of the expected type, and then what it's going to depend on is a Querywith an alias of status, and by default,that's also None. Same with our TaskPriority.We're going to use that Annotated syntax here, and we're going to use our TaskPriorityor it could be Noneand that's going to dependon a Query with an alias to priority,and the default for that is None as well.So Query here is missing an import, and that is something that we import from FastAPISo one more thing thatwe're missing here is our overdue.So let's say we want to have overdue only tasks get returned.That's going to be a boolean, and the default value for that is also going to be False.And again, we're going to say, this method has a return typehint of Any. All rightso we want to havekind of a matching method in CRUD that is going to accept all of these.So what that method signature is going to look like, it's going to be a crud.list_tasks,and it's going to take a session.It's going to take a project_id.It's going to take a project_slug, as well as the task_statusthe TaskPriority, and that overdue_onlyflag. So we talked aboutwhy we don't need a prefix here, and that's because not all routes aregoing to end up at that slash tasks endpoint.Creating a task needs to be associated with a project,and we also have this new list_tasks endpoint now that accepts those five different filterparameters that we could optionally provide.And having a task_status means that the API can just exposelike a query parameter and say, you know, question mark query equal question mark status equal doneand the Python parameter insidethat function is going to stay as a task_status,so it kind of all ties nicelytogether conceptually. And then in our main.py, weof course, now need toinclude a new router that's going to be our tasks router,and we need to go ahead and import that, andalso spell it correctly. With those CRUDroutes, could you pass all of those arguments with kwargs?Yes. Kind of one ofthe issues there is, we're trying to be explicit aboutwhat our filtering supports here.So if we did use keyword args, there is a possibility that maybean extra filter that's not actually supported on the database could be passed inso it kind of dependson where you want to handle that.This is one way of doing that, but yes, you could.Would it be considered a security risk if you passed it as kwargs because you could passin something that it would be interpretable as an attack like a SQL attack or something like that?One of the niceties of a SQL model isthere's a lot of scrubbing that happens unless you are passing in raw text to execute,which we will look at a little bit later, kind of how we can do that type of queryjust from our side, not accepting any sort of user input.But you would have to handle that rejection ofarguments that are not accepted by the API, you know,somewhere. So here we're being explicit, andbecause of this, the signature here, we kind of know exactly what to expect.So, yeah, that should be prevented at the FastAPIside. I don't know if it throws an error or just ignores it or what,but you can give it a try during the exercise and let me know.I generally always go named for, you know,every language I've ever worked with, that's considered a harder security barrier.So that's why I was wondering if it would be similar,like because you could just unpack it, you just don't know what's in there.Yeah, so here we are defining what we're expecting,and using this type hinting here as well also gives us somebut you know, not allguarantees that at least the type is going to match.So, yeah, try to avoid Bobby Tables here.
