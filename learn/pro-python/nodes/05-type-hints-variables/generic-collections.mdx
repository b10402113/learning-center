---
id: generic-collections
title: Generic collection types
subject: pro-python
sources:
  - "[[sources/pro-python/completed/20260903_09-type-hints.md#type-hints-syntax]]"
created: 2026-09-03
updated: 2026-09-03
---
## Learning goal

使用 `list[int]`、`dict[str, int]` 與 `tuple[str, ...]` 來描述集合的內容物。

## Lesson

寫 `items: list` 幾乎沒告訴讀者任何資訊。泛型讓你能宣告容器裡裝的是什麼 —— `list[str]` 代表「一個字串的 list」。這就像「一個箱子」跟「一箱螺絲」的差別。

最常使用的三個內建泛型是 `list`、`dict` 和 `tuple`。方括號裡放的是型別參數：`list` 放一個、`dict` 放兩個，`tuple` 則放固定位置或 `...`。

```python
# 一個整數的 list
scores: list[int] = [1, 2, 3]

# 鍵為字串、值為整數的 dict
ages: dict[str, int] = {"alice": 30, "bob": 25}

# 固定長度的 tuple —— 第一個是 str，第二個是 int
pair: tuple[str, int] = ("alice", 30)

# 可變長度的 tuple —— 所有項目都是 str
names: tuple[str, ...] = ("alice", "bob", "carol")
```

對 `tuple` 來說，`...` 代表「任意數量、皆為此型別的項目」。沒有它的話，tuple 就是固定長度，每個位置有各自的型別。

泛型可以自然巢狀。一個把字串映射到整數 list 的 dict，從左到右讀即可：

```python
groups: dict[str, list[int]] = {
    "odd": [1, 3, 5],
    "even": [2, 4, 6],
}
```

讀作「dict of str to list of int」。看到巢狀深度別慌 —— 結構本身會反映資料的形狀。

## Sources

- [[sources/pro-python/completed/20260903_09-type-hints.md#type-hints-syntax]]

## Course

[↗ 課程頁面](../../lessons/generic-collections.html)
