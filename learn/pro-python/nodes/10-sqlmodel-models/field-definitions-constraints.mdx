---
id: field-definitions-constraints
title: 欄位定義與 Annotated 可重用約束
subject: pro-python
sources:
  - "[[sources/pro-python/completed/20260903_28.md#field-definitions-and-constraints]]"
created: 2026-09-03
updated: 2026-09-03
---

<LearningGoal>
使用 Annotated 與 StringConstraints 定義可重用的欄位型別，並區分 SQLModel Field 與 Pydantic Field 的差異。
</LearningGoal>

## Lesson

假設你的系統裡有十個模型都需要「名稱」欄位：不能為空、長度 1-100。最笨的方法是在每個模型都寫一次 `Field(min_length=1, max_length=100)`。但這樣改規則的時候，你要改十個地方。

Python 的 `Annotated` 型別提示，搭配 SQLModel 的 `StringConstraints`，可以讓我們把這段驗證規則「打包」成一個型別，到時候直接重用。

```python
from typing import Annotated
from sqlmodel import StringConstraints

NameType = Annotated[str, StringConstraints(min_length=1, max_length=100)]

class Project(SQLModel, table=True):
    name: NameType
```

`Annotated[str, StringConstraints(min_length=1, max_length=100)]` 的意思是：這個欄位的基本型別是 `str`，但身上還附加了 `StringConstraints` 這組驗證規則。這樣一來，`name` 欄位就自動擁有「不能為空、最長 100 字」的驗證，而且其他模型也可以直接重用 `NameType`。

這裡有個容易踩坑的地方：SQLModel 的 `Field` 和 Pydantic 的 `Field` 雖然名字一樣，但功能不同。

| 來源 | 主要用途 |
| --- | --- |
| `pydantic.Field` | 設定驗證規則（min_length、max_length、regex） |
| `sqlmodel.Field` | 設定資料庫層級設定（primary_key、unique、index、sa_column） |

```python
from sqlmodel import SQLModel, Field, StringConstraints
from typing import Annotated

NameType = Annotated[str, StringConstraints(min_length=1, max_length=100)]

class Project(SQLModel, table=True):
    id: int | None = Field(default=None, primary_key=True)
    name: NameType = Field(unique=True)
```

把常用約束抽成型別別名，可以讓多個模型共用同一套規則。驗證規則集中管理，模型定義乾淨可讀，資料庫約束和應用驗證各司其職。

## Course

[↗ 課程頁面](../../lessons/field-definitions-constraints.html)

## Sources
